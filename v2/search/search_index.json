{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to the FastFuels Python SDK Documentation!","text":"<p>Beta</p> <p>You are reading the documentation for the v2 SDK, which targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 switch versions with the selector in the header. Import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p>"},{"location":"#what-is-fastfuels","title":"What is FastFuels?","text":"<p>FastFuels is a cloud-based platform for generating forest inventory data. It uses a combination of satellite imagery and machine learning to generate tabular tree data and voxelized 3D fuel models. These data products can be used to support wildfire risk assessment, fire behavior modeling, and other applications.</p> <p>The FastFuels documentation covers the platform itself: the web application, the HTTP API, and explanations of the core concepts (domains, grids, inventories, features). This site covers how to work with FastFuels from Python.</p>"},{"location":"#what-is-the-fastfuels-python-sdk","title":"What is the FastFuels Python SDK?","text":"<p>The FastFuels Python SDK is a Python package that provides a convenient interface for interacting with the FastFuels API. It can be used to create and manage FastFuels resources. It can also be used to download and process generated data products.</p>"},{"location":"#installation","title":"Installation","text":"<p>The FastFuels Python SDK can be installed using <code>pip</code>:</p> <pre><code>pip install fastfuels-sdk\n</code></pre>"},{"location":"#using-the-v2-sdk","title":"Using the v2 SDK","text":"<p>The v1 and v2 APIs are separate live services, and the SDK ships both as versioned subpackages, so you can use them side by side during migration:</p> <pre><code>from fastfuels_sdk.v1 import Domain   # v1 (current default)\nfrom fastfuels_sdk.v2 import Domain   # v2 (Beta)\n</code></pre> <p>Coming from v1? Start with Migrating from v1.</p> <p>The SDK authenticates with an existing API key. Creating applications and creating, rotating, or revoking keys are account-management operations handled outside the high-level Python SDK through the FastFuels console.</p>"},{"location":"SUMMARY/","title":"SUMMARY","text":"<ul> <li>Home</li> <li>Migrating from v1</li> <li>Tutorials<ul> <li>Export QUIC-Fire inputs</li> </ul> </li> <li>How-To Guides<ul> <li>Domains</li> <li>Features</li> <li>Grids<ul> <li>Creating grids</li> <li>Composing grids</li> <li>Working with grids</li> </ul> </li> <li>Inventories</li> <li>Modify and treat inventories</li> <li>Point clouds</li> <li>Exports</li> <li>Quotas and usage</li> </ul> </li> <li>Reference</li> </ul>"},{"location":"reference/","title":"Reference","text":""},{"location":"reference/#fastfuels_sdk.v2.domains","title":"fastfuels_sdk.v2.domains","text":"<p>fastfuels_sdk/v2/domains.py</p>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain","title":"Domain","text":"<p>               Bases: <code>Domain</code></p> <p>Domain resource for the FastFuels v2 API.</p> <p>Represents a spatial container that defines geographic boundaries for fire behavior modeling and analysis. A Domain includes metadata like name and description along with geometric data defining its spatial extent. Domains must specify a valid area between 0 and 16 square kilometers, located within CONUS.</p> <p>The Domain handles coordinate system transformations automatically:</p> <ol> <li>Geographic coordinates (e.g. EPSG:4326) are projected to the    appropriate UTM zone</li> <li>Projected coordinates are preserved in their original CRS</li> <li>Geometries are optionally padded to align with a grid resolution    (<code>pad_to_resolution</code>)</li> </ol> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Unique identifier for the domain.</p> <code>name</code> <code>str</code> <p>Human-readable name for the domain.</p> <code>description</code> <code>str</code> <p>Detailed description of the domain.</p> <code>type_</code> <code>str</code> <p>Always \"FeatureCollection\".</p> <code>features</code> <code>List[GeoJsonFeature]</code> <p>One GeoJSON feature named \"domain\": the projected working extent / bounding box.</p> <code>bbox</code> <code>List[float]</code> <p>Bounding box of the \"domain\" feature.</p> <code>crs</code> <code>GeoJsonCRS</code> <p>Coordinate reference system specification (always projected).</p> <code>tags</code> <code>(List[str], optional)</code> <p>User-defined tags for organization.</p> <code>pad_to_resolution</code> <code>(float, optional)</code> <p>Resolution in meters the domain bounding box was padded to align with.</p> <code>created_on</code> <code>datetime</code> <p>When the domain was created.</p> <code>modified_on</code> <code>datetime</code> <p>When the domain was last modified.</p> <p>Examples:</p> <p>Create a domain from a file:</p> <pre><code>&gt;&gt;&gt; domain = Domain.from_file(\"area.geojson\", name=\"my domain\")\n</code></pre> <p>Get a domain by ID:</p> <pre><code>&gt;&gt;&gt; domain = Domain.from_id(\"abc123\")\n&gt;&gt;&gt; print(domain.name)\n'my domain'\n</code></pre> See Also <p>Domain.from_geojson : Create a domain from GeoJSON data. Domain.from_geodataframe : Create a domain from a GeoPandas GeoDataFrame. Domain.from_file : Create a domain from a geospatial file. Domain.preview : Validate and project a domain without persisting it. Domain.get_lattice : Get the pixel lattice for a domain at a resolution. Domain.to_geodataframe : Convert a domain to a GeoPandas GeoDataFrame. list_domains : List available domains. reproject_geojson : Reproject a GeoJSON FeatureCollection (stateless).</p>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.from_id","title":"from_id  <code>classmethod</code>","text":"<pre><code>from_id(domain_id: str) -&gt; Domain\n</code></pre> <p>Retrieve an existing Domain resource by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain_id</code> <code>str</code> <p>The unique identifier of the domain to retrieve.</p> required <p>Returns:</p> Type Description <code>Domain</code> <p>The requested Domain object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no domain exists with the given ID, or the user does not have access to it.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; domain = Domain.from_id(\"abc123\")\n&gt;&gt;&gt; domain.id\n'abc123'\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.from_geojson","title":"from_geojson  <code>classmethod</code>","text":"<pre><code>from_geojson(geojson: dict, name: str = '', description: str = '', tags: Optional[List[str]] = None, pad_to_resolution: Optional[float] = None) -&gt; Domain\n</code></pre> <p>Create a new Domain from GeoJSON data.</p> <p>Parameters:</p> Name Type Description Default <code>geojson</code> <code>dict</code> <p>A GeoJSON FeatureCollection or Feature. (The v2 API accepts FeatureCollection input only; a single Feature is wrapped automatically.) The source CRS is read from the <code>crs</code> member if present; otherwise EPSG:4326 is assumed.</p> required <code>name</code> <code>str</code> <p>Name for the domain.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the domain.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the domain.</p> <code>None</code> <code>pad_to_resolution</code> <code>float</code> <p>Pad the domain bounding box outward so its extent is a multiple of this resolution (meters).</p> <code>None</code> <p>Returns:</p> Type Description <code>Domain</code> <p>The created Domain object.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the GeoJSON is not a Feature or FeatureCollection.</p> <code>UnprocessableEntityException</code> <p>If the geometry is invalid: zero area, larger than 16 square kilometers, outside CONUS, or an invalid CRS.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; with open(\"area.geojson\") as f:\n...     geojson = json.load(f)\n&gt;&gt;&gt; domain = Domain.from_geojson(geojson, name=\"my domain\")\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.from_geodataframe","title":"from_geodataframe  <code>classmethod</code>","text":"<pre><code>from_geodataframe(geodataframe: GeoDataFrame, name: str = '', description: str = '', tags: Optional[List[str]] = None, pad_to_resolution: Optional[float] = None) -&gt; Domain\n</code></pre> <p>Create a new Domain from a GeoPandas GeoDataFrame.</p> <p>The GeoDataFrame's CRS (when set) is forwarded to the API as the FeatureCollection's <code>crs</code> member, so projected inputs (e.g. EPSG:5070) are interpreted correctly.</p> <p>Parameters:</p> Name Type Description Default <code>geodataframe</code> <code>GeoDataFrame</code> <p>GeoDataFrame containing the domain geometry.</p> required <code>name</code> <code>str</code> <p>Name for the domain.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the domain.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the domain.</p> <code>None</code> <code>pad_to_resolution</code> <code>float</code> <p>Pad the domain bounding box outward so its extent is a multiple of this resolution (meters).</p> <code>None</code> <p>Returns:</p> Type Description <code>Domain</code> <p>The created Domain object.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the GeoDataFrame CRS has no authority code (e.g. a custom CRS).</p> <code>UnprocessableEntityException</code> <p>If the geometry is invalid: zero area, larger than 16 square kilometers, outside CONUS, or an invalid CRS.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; gdf = gpd.read_file(\"area.shp\")\n&gt;&gt;&gt; domain = Domain.from_geodataframe(gdf, name=\"my domain\")\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(path: Union[str, Path], name: str = '', description: str = '', tags: Optional[List[str]] = None, pad_to_resolution: Optional[float] = None) -&gt; Domain\n</code></pre> <p>Create a new Domain from a geospatial file.</p> <p>Reads any format geopandas supports (GeoJSON, Shapefile, KML, GeoPackage, ...) and creates a domain from its geometry.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str or Path</code> <p>Path to the geospatial file.</p> required <code>name</code> <code>str</code> <p>Name for the domain.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the domain.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the domain.</p> <code>None</code> <code>pad_to_resolution</code> <code>float</code> <p>Pad the domain bounding box outward so its extent is a multiple of this resolution (meters).</p> <code>None</code> <p>Returns:</p> Type Description <code>Domain</code> <p>The created Domain object.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; domain = Domain.from_file(\"area.geojson\", name=\"my domain\")\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.preview","title":"preview  <code>classmethod</code>","text":"<pre><code>preview(geojson: dict, name: str = '', description: str = '', tags: Optional[List[str]] = None, pad_to_resolution: Optional[float] = None) -&gt; Domain\n</code></pre> <p>Preview a domain without persisting it.</p> <p>Runs the same validation and projection pipeline as domain creation but writes nothing: use this to inspect the projected, padded bounding box before committing to a create. Takes the same arguments as :meth:<code>from_geojson</code>.</p> <p>Parameters:</p> Name Type Description Default <code>geojson</code> <code>dict</code> <p>A GeoJSON FeatureCollection or Feature (a single Feature is wrapped automatically).</p> required <code>name</code> <code>str</code> <p>Name for the domain.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the domain.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the domain.</p> <code>None</code> <code>pad_to_resolution</code> <code>float</code> <p>Pad the domain bounding box outward so its extent is a multiple of this resolution (meters).</p> <code>None</code> <p>Returns:</p> Type Description <code>Domain</code> <p>The previewed Domain object. Its <code>id</code> is always <code>\"preview\"</code> \u2014 not a real domain identifier, so API-backed instance methods (<code>refresh</code>, <code>update</code>, <code>delete</code>, <code>get_lattice</code>) will not work on it.</p> <p>Raises:</p> Type Description <code>UnprocessableEntityException</code> <p>Same validation errors as :meth:<code>from_geojson</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; previewed = Domain.preview(geojson, pad_to_resolution=2.0)\n&gt;&gt;&gt; previewed.id\n'preview'\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.refresh","title":"refresh","text":"<pre><code>refresh() -&gt; Domain\n</code></pre> <p>Update this Domain in place with the latest data from the API.</p> <p>Returns:</p> Type Description <code>Domain</code> <p><code>self</code>, updated with the latest data (so calls chain). To fetch a fresh, separate copy instead, use :meth:<code>Domain.from_id</code>.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the domain no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; domain = Domain.from_id(\"abc123\")\n&gt;&gt;&gt; domain.refresh()  # refresh in place\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.update","title":"update","text":"<pre><code>update(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; Domain\n</code></pre> <p>Update the domain's mutable properties (name, description, tags) in place.</p> <p>Only provided fields are sent in the PATCH body (the generated UNSET sentinel keeps omitted fields out of the request entirely). If no fields are provided, no API call is made.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>New name for the domain.</p> <code>None</code> <code>description</code> <code>str</code> <p>New description for the domain.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>New tags for the domain (replaces existing tags).</p> <code>None</code> <p>Returns:</p> Type Description <code>Domain</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the domain no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; domain.update(name=\"new name\", tags=[\"test\"])\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.delete","title":"delete","text":"<pre><code>delete(force: bool = False) -&gt; None\n</code></pre> <p>Delete this domain.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>If True, cascade-delete every child resource (features, grids, inventories, exports) in the domain. If False (default), the API rejects the request with 412 when the domain still has child resources.</p> <code>False</code> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the domain no longer exists.</p> <code>ApiException</code> <p>With <code>status_code</code> 412 if the domain has child resources and <code>force</code> is False.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; domain = Domain.from_id(\"abc123\")\n&gt;&gt;&gt; domain.delete(force=True)  # also deletes its grids and features\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.get_lattice","title":"get_lattice","text":"<pre><code>get_lattice(resolution: float, num_buffer_cells: int = 0) -&gt; DomainLattice\n</code></pre> <p>Get the pixel lattice for this domain at a given resolution.</p> <p>Returns the pixel lattice (affine transform + raster shape) for the domain at the requested resolution. Use this to align a GeoTIFF before uploading it as a custom grid.</p> <p>Parameters:</p> Name Type Description Default <code>resolution</code> <code>float</code> <p>Pixel size in meters (domain CRS units, always projected).</p> required <code>num_buffer_cells</code> <code>int</code> <p>Expand the lattice by N cells on each side (default 0). Mirrors the buffer semantics of the grid creation endpoints.</p> <code>0</code> <p>Returns:</p> Type Description <code>DomainLattice</code> <p>With attributes <code>crs</code> (authority string), <code>resolution</code>, <code>num_buffer_cells</code>, <code>transform</code> (affine coefficients <code>[a, b, c, d, e, f]</code>, rasterio convention), and <code>shape</code> (<code>[height, width]</code> in pixels).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the domain no longer exists.</p> <code>UnprocessableEntityException</code> <p>If <code>resolution</code> is non-positive or <code>num_buffer_cells</code> is negative.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; lattice = domain.get_lattice(resolution=2.0)\n&gt;&gt;&gt; lattice.shape\n[400, 400]\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; str\n</code></pre> <p>Serialize the complete Domain object to a JSON string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The Domain as a pretty-printed JSON string.</p>"},{"location":"reference/#fastfuels_sdk.v2.domains.Domain.to_geodataframe","title":"to_geodataframe","text":"<pre><code>to_geodataframe() -&gt; gpd.GeoDataFrame\n</code></pre> <p>Convert the Domain to a GeoPandas GeoDataFrame.</p> <p>The v2 API returns one named feature, \"domain\": the projected working extent / bounding box. The GeoDataFrame therefore has one row.</p> <p>Returns:</p> Type Description <code>GeoDataFrame</code> <p>GeoDataFrame in the domain CRS with the domain features as rows and domain metadata as columns.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; gdf = domain.to_geodataframe()\n&gt;&gt;&gt; gdf.loc[0, \"name\"]\n'domain'\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.list_domains","title":"list_domains","text":"<pre><code>list_domains(page: int = 0, size: int = 100, sort_by: Optional[str] = None, sort_order: Optional[str] = None) -&gt; List[Domain]\n</code></pre> <p>List domains belonging to the authenticated user (single page).</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <code>int</code> <p>The page number to retrieve, zero-indexed (default 0).</p> <code>0</code> <code>size</code> <code>int</code> <p>The number of domains per page (default 100).</p> <code>100</code> <code>sort_by</code> <code>str</code> <p>Field to sort by: \"name\", \"created_on\", or \"modified_on\".</p> <code>None</code> <code>sort_order</code> <code>str</code> <p>Sort direction: \"ascending\" or \"descending\".</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Domain]</code> <p>The requested page of Domain objects.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; domains = list_domains(sort_by=\"created_on\", sort_order=\"descending\")\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.domains.reproject_geojson","title":"reproject_geojson","text":"<pre><code>reproject_geojson(geojson: dict, target_epsg: int) -&gt; dict\n</code></pre> <p>Reproject a GeoJSON FeatureCollection to a target CRS.</p> <p>Stateless utility \u2014 no resource is created; the reprojected FeatureCollection is returned immediately.</p> <p>Parameters:</p> Name Type Description Default <code>geojson</code> <code>dict</code> <p>A GeoJSON FeatureCollection or Feature (a single Feature is wrapped automatically). The source CRS is read from the <code>crs</code> member if present; otherwise EPSG:4326 is assumed.</p> required <code>target_epsg</code> <code>int</code> <p>EPSG code of the target CRS (e.g. 4326 for WGS84, 32611 for UTM zone 11N).</p> required <p>Returns:</p> Type Description <code>dict</code> <p>The reprojected FeatureCollection, with original feature properties preserved and <code>crs</code> set to the target EPSG code.</p> <p>Raises:</p> Type Description <code>UnprocessableEntityException</code> <p>If the source CRS is invalid, the target EPSG is invalid, or a geometry cannot be reprojected.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; projected = reproject_geojson(geojson, target_epsg=5070)\n&gt;&gt;&gt; projected[\"crs\"][\"properties\"][\"name\"]\n'EPSG:5070'\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features","title":"fastfuels_sdk.v2.features","text":"<p>fastfuels_sdk/v2/features.py</p>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature","title":"Feature","text":"<p>               Bases: <code>Feature</code></p> <p>Feature resource for the FastFuels v2 API.</p> <p>Represents geographic features within a domain: roads and water bodies sourced from OpenStreetMap, or user-uploaded layersets of fuelbed polygons. Features are asynchronous job resources \u2014 OSM features are generated in the background and expose a <code>status</code>/<code>progress</code> lifecycle, while layerset uploads complete synchronously.</p> <p>The Feature resource itself is geometry-free; the generated geodata is retrieved through the data methods (:meth:<code>get_data_metadata</code>, :meth:<code>get_data_partition</code>, :meth:<code>get_data</code>, :meth:<code>to_geodataframe</code>) once the feature is completed.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Unique identifier for the feature.</p> <code>domain_id</code> <code>str</code> <p>Identifier of the domain the feature belongs to.</p> <code>type_</code> <code>FeatureType</code> <p>Type of geographic feature: \"road\", \"water\", or \"layerset\".</p> <code>status</code> <code>JobStatus</code> <p>Job status: \"pending\", \"running\", \"completed\", or \"failed\".</p> <code>source</code> <code>FeatureSource</code> <p>Where the feature data comes from (e.g. \"osm\").</p> <code>name</code> <code>str</code> <p>Human-readable name for the feature.</p> <code>description</code> <code>str</code> <p>Detailed description of the feature.</p> <code>progress</code> <code>(JobProgress, optional)</code> <p>Progress info while the job is running.</p> <code>georeference</code> <code>(FeatureGeoreference, optional)</code> <p>Spatial reference of the generated data; populated when the job completes.</p> <code>error</code> <code>(JobError, optional)</code> <p>Error details if the job failed.</p> <code>tags</code> <code>(List[str], optional)</code> <p>User-defined tags for organization.</p> <code>created_on</code> <code>datetime</code> <p>When the feature was created.</p> <code>modified_on</code> <code>datetime</code> <p>When the feature was last modified.</p> <p>Examples:</p> <p>Create a road feature and wait for it to complete:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk as ff\n&gt;&gt;&gt; feature = ff.features.create_road_feature_from_osm(domain)\n&gt;&gt;&gt; feature.wait()\n&gt;&gt;&gt; roads = feature.to_geodataframe()\n</code></pre> <p>Get a feature by ID:</p> <pre><code>&gt;&gt;&gt; feature = ff.get_feature(domain, \"def456\")\n</code></pre> See Also <p>create_road_feature_from_osm : Create a road feature from OpenStreetMap. create_water_feature_from_osm : Create a water feature from OpenStreetMap. create_layerset_feature_from_geojson : Upload a custom layerset of fuelbed polygons. Feature.to_geodataframe : Retrieve the feature data as a GeoDataFrame. Feature.rasterize : Rasterize a layerset feature into a grid. list_features : List features in a domain or across all domains.</p>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.from_id","title":"from_id  <code>classmethod</code>","text":"<pre><code>from_id(domain_id: str, feature_id: str) -&gt; Feature\n</code></pre> <p>Retrieve an existing Feature resource by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain_id</code> <code>str</code> <p>The unique identifier of the domain the feature belongs to.</p> required <code>feature_id</code> <code>str</code> <p>The unique identifier of the feature to retrieve.</p> required <p>Returns:</p> Type Description <code>Feature</code> <p>The requested Feature object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no feature exists with the given IDs, or the user does not have access to it.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature = Feature.from_id(\"abc123\", \"def456\")\n&gt;&gt;&gt; feature.id\n'def456'\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.refresh","title":"refresh","text":"<pre><code>refresh() -&gt; Feature\n</code></pre> <p>Update this Feature in place with the latest data from the API.</p> <p>Returns:</p> Type Description <code>Feature</code> <p><code>self</code>, updated with the latest data (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the feature no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature.refresh()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.wait","title":"wait","text":"<pre><code>wait(timeout: Optional[float] = None, verbose: bool = False) -&gt; Feature\n</code></pre> <p>Poll the feature job until it reaches a terminal status.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Maximum seconds to wait. <code>None</code> (default) waits indefinitely; the job runs server-side regardless, so a bounded wait is resumable.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If True, print the job status at each poll.</p> <code>False</code> <p>Returns:</p> Type Description <code>Feature</code> <p><code>self</code>, updated to its terminal state (so calls chain).</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If <code>timeout</code> is set and elapses before a terminal status.</p> <code>JobFailedError</code> <p>If the job finished with status \"failed\".</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature = create_road_feature_from_osm(domain)\n&gt;&gt;&gt; feature.wait(verbose=True)\nFeature def456: JobStatus.COMPLETED (5s)\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.update","title":"update","text":"<pre><code>update(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; Feature\n</code></pre> <p>Update the feature's mutable metadata (name, description, tags) in place.</p> <p>Only provided fields are sent. If no fields are provided, no API call is made.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>New name for the feature.</p> <code>None</code> <code>description</code> <code>str</code> <p>New description for the feature.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>New tags for the feature (replaces existing tags).</p> <code>None</code> <p>Returns:</p> Type Description <code>Feature</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the feature no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature.update(name=\"new name\", tags=[\"test\"])\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.delete","title":"delete","text":"<pre><code>delete() -&gt; None\n</code></pre> <p>Delete this feature and its generated data.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the feature no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature = Feature.from_id(\"abc123\", \"def456\")\n&gt;&gt;&gt; feature.delete()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.rasterize","title":"rasterize","text":"<pre><code>rasterize(output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, overlap_method: Optional[str] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None)\n</code></pre> <p>Rasterize this layerset feature into a grid.</p> <p>Only valid for layerset features (uploaded fuelbed polygons).</p> <p>Parameters:</p> Name Type Description Default <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"bilinear\", \"nearest\").</p> <code>None</code> <code>overlap_method</code> <code>str</code> <p>Per-cell reduction when polygons of the same fuel type overlap: \"max\", \"mean\", or \"min\".</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new (pending) rasterized Grid.</p>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.get_data_metadata","title":"get_data_metadata","text":"<pre><code>get_data_metadata() -&gt; FeatureDataMetadata\n</code></pre> <p>Get the partition layout of the feature's generated data.</p> <p>The generated geodata is served in fixed-size partitions. Use this to discover how many partitions exist before retrieving them with :meth:<code>get_data_partition</code> (or call :meth:<code>get_data</code> / :meth:<code>to_geodataframe</code> to retrieve everything at once).</p> <p>Returns:</p> Type Description <code>FeatureDataMetadata</code> <p>With attributes <code>total_features</code> (count across all partitions), <code>partition_count</code> (number of valid partition indices), and <code>partitions</code> (per-partition feature counts). A feature with no data has <code>partition_count</code> 0.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the feature no longer exists.</p> <code>UnprocessableEntityException</code> <p>If the feature is not in \"completed\" status.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; metadata = feature.get_data_metadata()\n&gt;&gt;&gt; metadata.partition_count\n1\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.get_data_partition","title":"get_data_partition","text":"<pre><code>get_data_partition(partition_index: int) -&gt; Dict[str, Any]\n</code></pre> <p>Get one partition of the feature's generated data.</p> <p>Parameters:</p> Name Type Description Default <code>partition_index</code> <code>int</code> <p>Zero-indexed partition number. Must be less than the <code>partition_count</code> reported by :meth:<code>get_data_metadata</code>.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>A self-contained GeoJSON FeatureCollection with this partition's features.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the feature no longer exists.</p> <code>UnprocessableEntityException</code> <p>If <code>partition_index</code> is past the last partition, or the feature is not in \"completed\" status.</p> <code>ApiException</code> <p>With <code>status_code</code> 413 if the serialized partition exceeds the 30 MB response cap.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; partition = feature.get_data_partition(0)\n&gt;&gt;&gt; partition[\"type\"]\n'FeatureCollection'\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.get_data","title":"get_data","text":"<pre><code>get_data() -&gt; Dict[str, Any]\n</code></pre> <p>Get the feature's complete generated data as GeoJSON.</p> <p>Retrieves every partition and concatenates them into a single FeatureCollection, preserving source order.</p> <p>Returns:</p> Type Description <code>dict</code> <p>A GeoJSON FeatureCollection with all of the feature's data. Empty (no features) if the source data contained nothing within the domain extent.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the feature no longer exists.</p> <code>UnprocessableEntityException</code> <p>If the feature is not in \"completed\" status.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; data = feature.get_data()\n&gt;&gt;&gt; len(data[\"features\"])\n42\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.to_geodataframe","title":"to_geodataframe","text":"<pre><code>to_geodataframe() -&gt; gpd.GeoDataFrame\n</code></pre> <p>Retrieve the feature's generated data as a GeoPandas GeoDataFrame.</p> <p>Returns:</p> Type Description <code>GeoDataFrame</code> <p>GeoDataFrame with one row per generated feature. Empty if the source data contained nothing within the domain extent.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the feature no longer exists.</p> <code>UnprocessableEntityException</code> <p>If the feature is not in \"completed\" status.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature.wait()\n&gt;&gt;&gt; gdf = feature.to_geodataframe()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.Feature.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; str\n</code></pre> <p>Serialize the complete Feature object to a JSON string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The Feature as a pretty-printed JSON string.</p>"},{"location":"reference/#fastfuels_sdk.v2.features.create_road_feature_from_osm","title":"create_road_feature_from_osm","text":"<pre><code>create_road_feature_from_osm(domain, name: str = '', description: str = '', tags: Optional[List[str]] = None, extent_buffer_m: float = 0.0) -&gt; Feature\n</code></pre> <p>Create a road feature from OpenStreetMap data.</p> <p>Starts a background job that extracts road geometries from OpenStreetMap within the domain extent. Use :meth:<code>Feature.wait</code> to block until the data is ready.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the feature in.</p> required <code>name</code> <code>str</code> <p>Name for the feature.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the feature.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the feature.</p> <code>None</code> <code>extent_buffer_m</code> <code>float</code> <p>Distance in meters (0-100, default 0) to buffer the domain extent outward when querying OpenStreetMap.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>Feature</code> <p>The created Feature object (job status \"pending\" or \"running\").</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the domain does not exist.</p> <code>UnprocessableEntityException</code> <p>If <code>extent_buffer_m</code> is outside the 0-100 meter range.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature = create_road_feature_from_osm(domain, name=\"roads\")\n&gt;&gt;&gt; feature.type_\n&lt;FeatureType.ROAD: 'road'&gt;\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.create_water_feature_from_osm","title":"create_water_feature_from_osm","text":"<pre><code>create_water_feature_from_osm(domain, name: str = '', description: str = '', tags: Optional[List[str]] = None, extent_buffer_m: float = 0.0) -&gt; Feature\n</code></pre> <p>Create a water feature from OpenStreetMap data.</p> <p>Starts a background job that extracts water-body geometries from OpenStreetMap within the domain extent. Use :meth:<code>Feature.wait</code> to block until the data is ready.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the feature in.</p> required <code>name</code> <code>str</code> <p>Name for the feature.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the feature.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the feature.</p> <code>None</code> <code>extent_buffer_m</code> <code>float</code> <p>Distance in meters (0-100, default 0) to buffer the domain extent outward when querying OpenStreetMap.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>Feature</code> <p>The created Feature object (job status \"pending\" or \"running\").</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the domain does not exist.</p> <code>UnprocessableEntityException</code> <p>If <code>extent_buffer_m</code> is outside the 0-100 meter range.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature = create_water_feature_from_osm(domain, name=\"water\")\n&gt;&gt;&gt; feature.type_\n&lt;FeatureType.WATER: 'water'&gt;\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.create_layerset_feature_from_geojson","title":"create_layerset_feature_from_geojson","text":"<pre><code>create_layerset_feature_from_geojson(domain, geojson: dict, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Feature\n</code></pre> <p>Upload a custom layerset of fuelbed polygons from GeoJSON.</p> <p>Uploads a GeoJSON FeatureCollection where each feature is one fuelbed polygon. The upload is validated and stored synchronously: the returned Feature has status \"completed\" and its data is immediately available through the data methods.</p> <p>Each feature's <code>properties</code> must carry the fuelbed input columns:</p> <ul> <li><code>fuel_type</code> (str)</li> <li><code>fuel_loading</code> (float)</li> <li><code>fuel_height</code> (float)</li> <li><code>percent_cover</code> (float)</li> <li><code>distribution</code> (str): \"homogeneous\", \"random_clusters\", or   \"uniform_random\"</li> </ul> <p>Optional columns: <code>strata_fb</code>, <code>patch_size</code>, <code>live_fuel_moisture</code>, <code>dead_fuel_moisture</code>, <code>heat_of_combustion</code>, <code>patch_std_dev</code>.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the feature in.</p> required <code>geojson</code> <code>dict</code> <p>A GeoJSON FeatureCollection or Feature (a single Feature is wrapped automatically) with Polygon or MultiPolygon geometries. The <code>crs</code> member must declare a projected CRS (e.g. EPSG:5070); geographic coordinates are rejected.</p> required <code>name</code> <code>str</code> <p>Name for the feature.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the feature.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the feature.</p> <code>None</code> <p>Returns:</p> Type Description <code>Feature</code> <p>The created Feature object with status \"completed\".</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the GeoJSON is not a Feature or FeatureCollection.</p> <code>NotFoundException</code> <p>If the domain does not exist.</p> <code>UnprocessableEntityException</code> <p>If the CRS is missing or geographic, a geometry is not a Polygon/MultiPolygon, or a feature is missing required fuelbed properties.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; feature = create_layerset_feature_from_geojson(domain, geojson)\n&gt;&gt;&gt; feature.status\n&lt;JobStatus.COMPLETED: 'completed'&gt;\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.create_layerset_feature_from_geodataframe","title":"create_layerset_feature_from_geodataframe","text":"<pre><code>create_layerset_feature_from_geodataframe(domain, geodataframe: GeoDataFrame, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Feature\n</code></pre> <p>Upload a custom layerset from a GeoPandas GeoDataFrame.</p> <p>The GeoDataFrame's CRS is forwarded to the API as the FeatureCollection's <code>crs</code> member. The CRS must be projected (e.g. EPSG:5070); the GeoDataFrame is not reprojected \u2014 use <code>geodataframe.to_crs(...)</code> first if needed. Each row's columns must carry the fuelbed properties described in :func:<code>create_layerset_feature_from_geojson</code>.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the feature in.</p> required <code>geodataframe</code> <code>GeoDataFrame</code> <p>GeoDataFrame with one fuelbed polygon per row.</p> required <code>name</code> <code>str</code> <p>Name for the feature.</p> <code>''</code> <code>description</code> <code>str</code> <p>Description of the feature.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for organizing the feature.</p> <code>None</code> <p>Returns:</p> Type Description <code>Feature</code> <p>The created Feature object with status \"completed\".</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the GeoDataFrame CRS has no authority code (e.g. a custom CRS).</p> <code>UnprocessableEntityException</code> <p>Same validation errors as :func:<code>create_layerset_feature_from_geojson</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; gdf = gpd.read_file(\"fuelbeds.shp\").to_crs(epsg=5070)\n&gt;&gt;&gt; feature = create_layerset_feature_from_geodataframe(domain, gdf)\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.features.get_feature","title":"get_feature","text":"<pre><code>get_feature(domain, feature_id: str) -&gt; Feature\n</code></pre> <p>Retrieve a single feature by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) the feature belongs to.</p> required <code>feature_id</code> <code>str</code> <p>The unique identifier of the feature.</p> required <p>Returns:</p> Type Description <code>Feature</code> <p>The requested Feature object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no feature exists with the given IDs, or the user does not have access to it.</p>"},{"location":"reference/#fastfuels_sdk.v2.features.list_features","title":"list_features","text":"<pre><code>list_features(domain=None, page: int = 0, size: int = 100, sort_by: Optional[str] = None, sort_order: Optional[str] = None, feature_type: Optional[str] = None, product: Optional[str] = None, tag: Optional[str] = None) -&gt; List[Feature]\n</code></pre> <p>List features in a domain, or across all domains (single page).</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to list features in. If omitted, features from all the user's domains are listed.</p> <code>None</code> <code>page</code> <code>int</code> <p>The page number to retrieve, zero-indexed (default 0).</p> <code>0</code> <code>size</code> <code>int</code> <p>The number of features per page (default 100).</p> <code>100</code> <code>sort_by</code> <code>str</code> <p>Field to sort by: \"name\", \"created_on\", or \"modified_on\".</p> <code>None</code> <code>sort_order</code> <code>str</code> <p>Sort direction: \"ascending\" or \"descending\".</p> <code>None</code> <code>feature_type</code> <code>str</code> <p>Only return features of this type: \"road\", \"water\", or \"layerset\".</p> <code>None</code> <code>product</code> <code>str</code> <p>Only return features from this data product (e.g. \"osm\").</p> <code>None</code> <code>tag</code> <code>str</code> <p>Only return features carrying this tag.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Feature]</code> <p>The requested page of Feature objects.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; features = list_features(domain, feature_type=\"road\")\n&gt;&gt;&gt; all_features = list_features()  # across all domains\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.grids","title":"fastfuels_sdk.v2.grids","text":"<p>fastfuels_sdk/v2/grids.py</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid","title":"Grid","text":"<p>               Bases: <code>Grid</code></p> <p>Grid resource for the FastFuels v2 API.</p> <p>A grid is a raster (2D) or voxel (3D) dataset within a domain: topography, canopy, surface fuel models, uploaded rasters, or grids derived from other grids. Grids are asynchronous job resources \u2014 creation starts a background job and returns a pending record; call :meth:<code>wait</code> to block until it is ready.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Unique identifier for the grid.</p> <code>domain_id</code> <code>str</code> <p>Identifier of the domain the grid belongs to.</p> <code>status</code> <code>JobStatus</code> <p>Job status: \"pending\", \"running\", \"completed\", or \"failed\".</p> <code>source</code> <code>GridSource</code> <p>Where the grid data comes from (e.g. \"landfire\", \"3dep\").</p> <code>bands</code> <code>List[Band]</code> <p>The grid's data bands (keys, types, units).</p> <code>name</code> <code>str</code> <p>Human-readable name for the grid.</p> <code>description</code> <code>str</code> <p>Detailed description of the grid.</p> <code>progress</code> <code>(JobProgress, optional)</code> <p>Progress info while the job is running.</p> <code>georeference</code> <code>(Georeference or Georeference3D, optional)</code> <p>Spatial reference of the data; populated when the job completes.</p> <code>error</code> <code>(JobError, optional)</code> <p>Error details if the job failed.</p> <code>chunks</code> <code>(Chunks, optional)</code> <p>Chunk layout; populated when processing completes.</p> <code>tags</code> <code>(List[str], optional)</code> <p>User-defined tags for organization.</p> <code>created_on</code> <code>datetime</code> <p>When the grid was created.</p> <code>modified_on</code> <code>datetime</code> <p>When the grid was last modified.</p> <p>Examples:</p> <p>Create a topography grid and wait for it to complete:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk as ff\n&gt;&gt;&gt; grid = ff.grids.create_topography_grid_from_3dep(domain, output_resolution_m=10)\n&gt;&gt;&gt; grid.wait()\n</code></pre> <p>Get a grid by ID:</p> <pre><code>&gt;&gt;&gt; grid = ff.get_grid(domain, \"def456\")\n</code></pre> See Also <p>create_topography_grid_from_3dep : Create a topography grid from USGS 3DEP. create_fuel_model_grid_from_landfire_fbfm40 : Create an FBFM40 fuel model grid. create_uniform_grid : Create a constant-value grid. list_grids : List grids in a domain or across all domains.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.represented_year","title":"represented_year  <code>property</code>","text":"<pre><code>represented_year: Optional[int]\n</code></pre> <p>Calendar year the fuel data represents, as reported by the API.</p> <p>Populated on LANDFIRE fuel model grids. For an annual product this is the landscape vintage (the <code>version</code>); for a seasonal product it is the projected season year read from the LANDFIRE Product Service catalog. <code>None</code> when the source does not report a year.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.from_id","title":"from_id  <code>classmethod</code>","text":"<pre><code>from_id(domain_id: str, grid_id: str) -&gt; Grid\n</code></pre> <p>Retrieve an existing Grid resource by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain_id</code> <code>str</code> <p>The unique identifier of the domain the grid belongs to.</p> required <code>grid_id</code> <code>str</code> <p>The unique identifier of the grid to retrieve.</p> required <p>Returns:</p> Type Description <code>Grid</code> <p>The requested Grid object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no grid exists with the given IDs, or the user does not have access to it.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.refresh","title":"refresh","text":"<pre><code>refresh() -&gt; Grid\n</code></pre> <p>Update this Grid in place with the latest data from the API.</p> <p>Returns:</p> Type Description <code>Grid</code> <p><code>self</code>, updated with the latest data (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the grid no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.wait","title":"wait","text":"<pre><code>wait(timeout: Optional[float] = None, verbose: bool = False) -&gt; Grid\n</code></pre> <p>Poll the grid job until it reaches a terminal status.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Maximum seconds to wait. <code>None</code> (default) waits indefinitely; the job runs server-side regardless, so a bounded wait is resumable.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If True, print the job status at each poll.</p> <code>False</code> <p>Returns:</p> Type Description <code>Grid</code> <p><code>self</code>, updated to its terminal state (so calls chain).</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If <code>timeout</code> is set and elapses before a terminal status.</p> <code>JobFailedError</code> <p>If the job finished with status \"failed\".</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.update","title":"update","text":"<pre><code>update(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Update the grid's mutable metadata (name, description, tags) in place.</p> <p>Only provided fields are sent. If no fields are provided, no API call is made.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>New name for the grid.</p> <code>None</code> <code>description</code> <code>str</code> <p>New description for the grid.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>New tags for the grid (replaces existing tags).</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the grid no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.delete","title":"delete","text":"<pre><code>delete() -&gt; None\n</code></pre> <p>Delete this grid and its data.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the grid no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.duplicate","title":"duplicate","text":"<pre><code>duplicate(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Create an independent copy of this grid under a new ID.</p> <p>The copy is a true clone \u2014 the finished data is byte-copied, not re-derived \u2014 carrying over the source's <code>source</code>, <code>modifications</code>, <code>georeference</code>, and <code>checksum</code> verbatim; only its <code>id</code> and timestamps differ. Use this to branch from a grid before modifying the copy while the original stays untouched.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name for the copy. Defaults to the source's name.</p> <code>None</code> <code>description</code> <code>str</code> <p>Description for the copy. Defaults to the source's description.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>Tags for the copy. Defaults to the source's tags.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new Grid object (job status \"pending\" while the data is copied; call :meth:<code>wait</code> before using it).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the grid no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.resample","title":"resample","text":"<pre><code>resample(output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, method_overrides=None, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a new grid by resampling this grid onto a different lattice.</p> <p>Parameters:</p> Name Type Description Default <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep this grid's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"bilinear\", \"nearest\", \"average\").</p> <code>None</code> <code>method_overrides</code> <code>CreateResampleRequestMethodOverrides</code> <p>Per-band resampling method overrides.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new (pending) resampled Grid.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.apply_modifications","title":"apply_modifications","text":"<pre><code>apply_modifications(modifications: list) -&gt; Grid\n</code></pre> <p>Apply modification rules to this grid in place.</p> <p>The grid keeps its ID; the submitted rules are appended to its cumulative <code>modifications</code> list and the data is re-derived as a background job \u2014 the grid returns to \"pending\" status, so call :meth:<code>wait</code> before using its data. Unlike a creator's <code>modifications=</code> argument (applied while the grid is first built), this modifies a grid you already hold.</p> <p>Parameters:</p> Name Type Description Default <code>modifications</code> <code>list</code> <p>Modification rules (<code>GridModification</code>). Build feature masks with :func:<code>fastfuels_sdk.v2.modifications.mask</code> (<code>ff.mask</code>); each rule pairs conditions (spatial or band-value tests, ANDed) with actions that rewrite band values for the matching cells.</p> required <p>Returns:</p> Type Description <code>Grid</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the grid is not completed.</p> <code>NotFoundException</code> <p>If the grid no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; grid.apply_modifications([ff.mask(roads, \"fbfm\", 91, buffer_m=5)])\n&gt;&gt;&gt; grid.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.export","title":"export","text":"<pre><code>export(format: str = 'geotiff', bands: Optional[List[str]] = None, expiration_days: int = 7, name: str = '', description: str = '', tags: Optional[List[str]] = None)\n</code></pre> <p>Export this grid to a downloadable file format.</p> <p>Parameters:</p> Name Type Description Default <code>format</code> <code>str</code> <p>Export format: \"geotiff\" (2D only), \"netcdf\", or \"zarr\" (default \"geotiff\").</p> <code>'geotiff'</code> <code>bands</code> <code>List[str]</code> <p>Band keys to include (default: all bands).</p> <code>None</code> <code>expiration_days</code> <code>int</code> <p>Days until the signed download URL expires (max 7, default 7).</p> <code>7</code> <code>name</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the export.</p> <code>None</code> <p>Returns:</p> Type Description <code>Export</code> <p>The created Export object (job status \"pending\"). Call :meth:<code>Export.wait</code> and then :meth:<code>Export.to_file</code> to download the file.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.band_summary","title":"band_summary","text":"<pre><code>band_summary(band: str)\n</code></pre> <p>Return summary statistics for one band, without downloading the data.</p> <p>The server computes a per-band summary when the grid completes, so this is a cheap overview that does not fetch the grid's cells (unlike :meth:<code>to_numpy</code>).</p> <p>Parameters:</p> Name Type Description Default <code>band</code> <code>str</code> <p>The band key to summarize (see :attr:<code>bands</code> for available keys).</p> required <p>Returns:</p> Type Description <code>ContinuousBandSummary or CategoricalBandSummary or None</code> <p>The band's summary, discriminated by its <code>type_</code>: <code>\"continuous\"</code> carries <code>count</code>, <code>nodata_count</code>, <code>min_</code>, <code>max_</code>, <code>mean</code>, and <code>std</code>; <code>\"categorical\"</code> carries <code>count</code>, <code>nodata_count</code>, and <code>unique_count</code>. <code>None</code> until the grid completes (call :meth:<code>wait</code> first).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>band</code> is not one of the grid's bands.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; grid = ff.get_grid(domain, \"def456\").wait()\n&gt;&gt;&gt; grid.band_summary(\"elevation\").mean\n2143.7\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.to_numpy","title":"to_numpy","text":"<pre><code>to_numpy(band: str) -&gt; np.ndarray\n</code></pre> <p>Read one band of this grid into an in-memory NumPy array.</p> <p>Fetches every chunk of <code>band</code> and reassembles them into a single array shaped like the full grid: 2D <code>(y, x)</code> for rasters, 3D <code>(z, y, x)</code> for voxel grids. Chunks are requested in the dense encoding for 2D grids and the sparse encoding for 3D grids, matching how the data is stored.</p> <p>Parameters:</p> Name Type Description Default <code>band</code> <code>str</code> <p>The band key to read (see :attr:<code>bands</code> for available keys).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>The band's values. Cells with no data carry the band's <code>nodata</code> value, or NaN (floating dtypes) / 0 (integer dtypes) when the band defines none.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the grid is not completed, or <code>band</code> is not one of its bands.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; grid = ff.get_grid(domain, \"def456\").wait()\n&gt;&gt;&gt; elevation = grid.to_numpy(\"elevation\")\n&gt;&gt;&gt; elevation.shape\n(1200, 1600)\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.to_xarray","title":"to_xarray","text":"<pre><code>to_xarray()\n</code></pre> <p>Read this grid's bands into an in-memory :class:<code>xarray.Dataset</code>.</p> <p>Each band becomes a data variable over <code>(y, x)</code> (2D) or <code>(z, y, x)</code> (3D), with <code>x</code>/<code>y</code> (and <code>z</code>) coordinate vectors derived from the grid's affine transform and the CRS recorded on the dataset's <code>crs</code> attribute.</p> <p>Returns:</p> Type Description <code>Dataset</code> <p>All bands of the grid as aligned data variables.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the grid is not completed.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; grid = ff.get_grid(domain, \"def456\").wait()\n&gt;&gt;&gt; ds = grid.to_xarray()\n&gt;&gt;&gt; ds[\"elevation\"].mean().item()\n2143.7\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.grids.Grid.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; str\n</code></pre> <p>Serialize the complete Grid object to a JSON string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The Grid as a pretty-printed JSON string.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_topography_grid_from_3dep","title":"create_topography_grid_from_3dep","text":"<pre><code>create_topography_grid_from_3dep(domain, source_resolution_m: int = 10, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, bands: Optional[list] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a topography grid from USGS 3DEP elevation data.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>source_resolution_m</code> <code>int</code> <p>The 3DEP product family to draw from: 1, 10, or 30 meters (default 10). Use :func:<code>check_3dep_coverage</code> first for 1 m data.</p> <code>10</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin. By default the source resolution is kept.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"bilinear\", \"cubic\").</p> <code>None</code> <code>bands</code> <code>list</code> <p>Topography bands to produce (<code>TopographyBand</code> members or their string keys, e.g. \"elevation\", \"slope\", \"aspect\"). Defaults to all.</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_topography_grid_from_landfire","title":"create_topography_grid_from_landfire","text":"<pre><code>create_topography_grid_from_landfire(domain, version: Optional[str] = None, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, bands: Optional[list] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a topography grid from LANDFIRE elevation data.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>version</code> <code>str</code> <p>LANDFIRE version (see <code>LandfireTopographyVersion</code>). Defaults to the API's current version.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"bilinear\", \"cubic\").</p> <code>None</code> <code>bands</code> <code>list</code> <p>Topography bands to produce (<code>TopographyBand</code> members or their string keys). Defaults to all.</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_canopy_fuel_grid_from_landfire","title":"create_canopy_fuel_grid_from_landfire","text":"<pre><code>create_canopy_fuel_grid_from_landfire(domain, version: Optional[str] = None, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, bands: Optional[list] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a canopy fuel grid from LANDFIRE data.</p> <p>Produces canopy fuel bands (e.g. canopy bulk density, canopy base height, canopy cover, canopy height) within the domain.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>version</code> <code>str</code> <p>LANDFIRE version (see <code>LandfireCanopyVersion</code>). Defaults to the API's current version.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"bilinear\", \"cubic\").</p> <code>None</code> <code>bands</code> <code>list</code> <p>Canopy fuel bands to produce (<code>LandfireCanopyFuelBand</code> members or their string keys). Defaults to all.</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_canopy_fuel_grid_from_inventory","title":"create_canopy_fuel_grid_from_inventory","text":"<pre><code>create_canopy_fuel_grid_from_inventory(inventory, bands: Optional[list] = None, biomass_equations: Optional[str] = None, biomass_column: Optional[str] = None, foliage_fraction: Optional[float] = None, branchwood_fraction: Optional[float] = None, branchwood_size_partition: Optional[str] = None, species_inclusion: Optional[str] = None, crown_class_adjustment: Optional[str] = None, min_tree_height: Optional[float] = None, vertical_distribution: Optional[str] = None, layer_depth: Optional[float] = None, horizontal_distribution: Optional[str] = None, max_crown_radius_equations: Optional[str] = None, max_crown_radius_column: Optional[str] = None, cbd=None, cbh=None, chm=None, cc=None, output_resolution_m: Optional[float] = None, align_to=None, resampling: Optional[str] = None, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Create a canopy fuel grid from a completed tree inventory.</p> <p>Derives the canopy metrics fire models consume \u2014 canopy bulk density (<code>cbd</code>), canopy base height (<code>cbh</code>), canopy height (<code>chm</code>), canopy cover (<code>cc</code>), and optionally canopy fuel load (<code>cfl</code>) \u2014 for each cell directly from the inventory's trees. Only live trees contribute canopy fuel. The bands share keys and units with the LANDFIRE canopy source.</p> <p>Parameters:</p> Name Type Description Default <code>inventory</code> <code>Inventory or str</code> <p>A completed tree inventory (or its id) to derive canopy metrics from. Passing the id alone requires the inventory to be reachable for its domain; pass the Inventory object to avoid an extra lookup.</p> required <code>bands</code> <code>list</code> <p>Output bands (<code>InventoryCanopyBand</code> members or their string keys: \"cbd\", \"cbh\", \"chm\", \"cc\", \"cfl\"). Defaults to the four canopy bands <code>cbd</code>, <code>cbh</code>, <code>chm</code>, and <code>cc</code>.</p> <code>None</code> <code>biomass_equations</code> <code>str</code> <p>Allometric equations for crown biomass: \"nsvb\" (default), \"jenkins\", or \"brown_1978\". Mutually exclusive with <code>biomass_column</code>.</p> <code>None</code> <code>biomass_column</code> <code>str</code> <p>Inventory column holding precomputed per-tree available canopy fuel, used in place of allometry. Mutually exclusive with <code>biomass_equations</code>; when set, <code>available_fuel</code>, species inclusion, and crown-class adjustment do not affect fuel magnitude.</p> <code>None</code> <code>foliage_fraction</code> <code>float</code> <p>Fraction of foliage biomass counted as available fuel (allometry only, default 1.0).</p> <code>None</code> <code>branchwood_fraction</code> <code>float</code> <p>Fraction of the branchwood size basis counted as available fuel (allometry only).</p> <code>None</code> <code>branchwood_size_partition</code> <code>str</code> <p>Size basis for the branchwood fraction: \"equations\", \"brown_proportions\", or \"none\" (allometry only).</p> <code>None</code> <code>species_inclusion</code> <code>str</code> <p>Which species contribute: \"all_species\" or \"fuelcalc_default\" (which excludes most hardwoods).</p> <code>None</code> <code>crown_class_adjustment</code> <code>str</code> <p>Crown-weight adjustment for canopy position: \"none\" (default) or \"fuelcalc_table\".</p> <code>None</code> <code>min_tree_height</code> <code>float</code> <p>Trees shorter than this height in meters contribute no canopy fuel (default 0.0).</p> <code>None</code> <code>vertical_distribution</code> <code>str</code> <p>How each tree's fuel stacks over its crown: \"reinhardt_2006\" or \"uniform\".</p> <code>None</code> <code>layer_depth</code> <code>float</code> <p>Vertical profile layer depth in meters (default 0.3048).</p> <code>None</code> <code>horizontal_distribution</code> <code>str</code> <p>How a tree's fuel is attributed to cells: \"crown_projected\" (default) or \"stem\".</p> <code>None</code> <code>max_crown_radius_equations</code> <code>str</code> <p>Allometric equations for maximum crown radius: \"purves\" (default) or \"crookston_stage\". Mutually exclusive with <code>max_crown_radius_column</code>.</p> <code>None</code> <code>max_crown_radius_column</code> <code>str</code> <p>Inventory column holding per-tree maximum crown radius in meters, used in place of allometry. Mutually exclusive with <code>max_crown_radius_equations</code>.</p> <code>None</code> <code>cbd</code> <code>str or CanopyCbdLoadOverDepth or CanopyCbdRunningMean</code> <p>Canopy bulk density method: \"load_over_depth\", \"running_mean\", or a method object for non-default parameters.</p> <code>None</code> <code>cbh</code> <code>str or method object</code> <p>Canopy base height method: \"mean\", \"minimum\", \"percentile\", \"threshold\", or a method object (<code>CanopyCbhMean</code>, <code>CanopyCbhMinimum</code>, <code>CanopyCbhPercentile</code>, or <code>CanopyProfileThreshold</code>) for non-default parameters.</p> <code>None</code> <code>chm</code> <code>str or method object</code> <p>Canopy height method: \"percentile\", \"threshold\", or a method object (<code>CanopyChmHeightPercentile</code> or <code>CanopyProfileThreshold</code>).</p> <code>None</code> <code>cc</code> <code>str or method object</code> <p>Canopy cover method: \"cover_fraction\", \"crown_overlap\", \"crown_union\", or a method object for non-default parameters.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin. Defaults to 30 m when no alignment target is given.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method for the alignment target.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>inventory</code> is passed as a bare id with no resolvable domain, if mutually exclusive kwargs are combined, or if a method name is unknown.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; grid = ff.grids.create_canopy_fuel_grid_from_inventory(\n...     inventory, bands=[\"cbd\", \"cbh\", \"chm\", \"cc\", \"cfl\"]\n... )\n&gt;&gt;&gt; grid.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_canopy_height_grid_from_meta","title":"create_canopy_height_grid_from_meta","text":"<pre><code>create_canopy_height_grid_from_meta(domain, version: Optional[str] = None, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a canopy height grid from the Meta Canopy Height Model.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>version</code> <code>str</code> <p>Meta CHM version (see <code>MetaCHMVersion</code>). Defaults to the API's current version.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"bilinear\", \"cubic\").</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_canopy_height_grid_from_naip_chm","title":"create_canopy_height_grid_from_naip_chm","text":"<pre><code>create_canopy_height_grid_from_naip_chm(domain, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a canopy height grid from the NAIP-CHM model.</p> <p>NAIP-CHM is a 0.6 m canopy height and structure model covering the contiguous US (CONUS). The grid carries a single continuous <code>chm</code> band (above-ground height in meters).</p> <p>NAIP-CHM is a surface model (nDSM): it captures the top of all above-ground structure \u2014 vegetation and buildings/infrastructure \u2014 not vegetation alone. To model vegetative fuels only, subtract built structures with <code>modifications=</code> (e.g. a building-footprint mask). Coverage is CONUS-only; domains outside it return no data.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin. The source is 0.6 m; coarser outputs are resampled.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method for the continuous height band (e.g. \"bilinear\", \"cubic\").</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p> References <p>Morford, S. L., Allred, B. W., Coons, S. P., Marcozzi, A. A., McCord, S. E., Smith, J. T., &amp; Naugle, D. E. (2025). A 0.6-meter resolution canopy height and structure model for the contiguous United States. bioRxiv. https://doi.org/10.64898/2025.12.12.694075</p> <p>Dataset and model code: https://github.com/smorf-ntsg/naip-chm</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_canopy_height_grid_from_point_cloud","title":"create_canopy_height_grid_from_point_cloud","text":"<pre><code>create_canopy_height_grid_from_point_cloud(point_cloud: PointCloud, output_resolution_m: Optional[float] = None, align_to=None, resampling: Optional[str] = None, aggregation: Optional[str] = None, percentile: Optional[float] = None, spike_filter=None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a canopy height grid from a completed airborne point cloud.</p> <p>Parameters:</p> Name Type Description Default <code>point_cloud</code> <code>PointCloud</code> <p>The completed airborne point cloud to rasterize.</p> required <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin. Defaults to 1 meter when no alignment target is provided.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method for the continuous canopy-height band.</p> <code>None</code> <code>aggregation</code> <code>str</code> <p>Statistic each cell reduces its above-ground return heights with: one of <code>\"max\"</code>, <code>\"mean\"</code>, <code>\"median\"</code>, or <code>\"percentile\"</code>. Defaults to the API's <code>\"max\"</code>.</p> <code>None</code> <code>percentile</code> <code>float</code> <p>Rank to take (0-100), used only with <code>aggregation=\"percentile\"</code>. 100 is the tallest return and 50 the median.</p> <code>None</code> <code>spike_filter</code> <code>bool, Mapping, or ChmSpikeFilter</code> <p>Removal of lone spurious returns. <code>None</code> (the default) applies the API's default filter; <code>False</code> disables it, keeping every return; <code>True</code> applies a default <code>ChmSpikeFilter</code>; a mapping of filter fields (<code>min_canopy_footprint_m</code>, <code>min_prominence_m</code>) or a <code>ChmSpikeFilter</code> sets custom thresholds.</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created canopy-height Grid (job status \"pending\" or \"running\").</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the point cloud is not completed or is not airborne, or if the aggregation and percentile arguments are inconsistent.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_fuel_model_grid_from_landfire_fbfm13","title":"create_fuel_model_grid_from_landfire_fbfm13","text":"<pre><code>create_fuel_model_grid_from_landfire_fbfm13(domain, version: Optional[str] = None, remove_non_burnable: Optional[list] = None, boundary_scatter=None, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a fuel model grid from LANDFIRE Anderson 13 fuel models.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>version</code> <code>str</code> <p>LANDFIRE version (<code>\"2023\"</code> or <code>\"2024\"</code>). Defaults to the API's current version.</p> <code>None</code> <code>remove_non_burnable</code> <code>list</code> <p>Non-burnable fuel models to drop (<code>NonBurnableFuelModel</code> members or their string keys, e.g. <code>\"NB1\"</code>, <code>\"NB2\"</code>).</p> <code>None</code> <code>boundary_scatter</code> <code>bool, dict, or BoundaryScatter</code> <p>Stochastic scattering of fuel model boundaries to create ragged, natural-looking transitions instead of staircase edges from nearest-neighbor resampling. <code>None</code> (default) disables scatter. <code>True</code> or <code>{}</code> enables scatter with defaults (<code>depth=10</code>, <code>seed=42</code>). A dict (e.g. <code>{\"depth\": 5, \"seed\": 123}</code>) or a <code>BoundaryScatter</code> instance sets explicit parameters.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. <code>\"nearest\"</code> for categorical fuel models).</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status <code>\"pending\"</code> or <code>\"running\"</code>).</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_fuel_model_grid_from_landfire_fbfm40","title":"create_fuel_model_grid_from_landfire_fbfm40","text":"<pre><code>create_fuel_model_grid_from_landfire_fbfm40(domain, version: Optional[str] = None, season: Optional[str] = None, remove_non_burnable: Optional[list] = None, boundary_scatter=None, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a fuel model grid from LANDFIRE 40 Scott &amp; Burgan fuel models.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>version</code> <code>str</code> <p>LANDFIRE version (see <code>LandfireFbfm40Version</code>). Defaults to the API's current version. Seasonal versions (see <code>season</code>) are only available for the LANDFIRE Seasonal Fuels vintages (e.g. \"2025\").</p> <code>None</code> <code>season</code> <code>str</code> <p>LANDFIRE Seasonal Fuels window (see <code>LandfireSeason</code>): \"ES\" (early spring), \"SP\" (spring), \"SU\" (summer), or \"FA\" (fall). When set, the grid is fetched from the LANDFIRE Product Service for that season rather than the staged annual release, and <code>version</code> must be a seasonal vintage (e.g. \"2025\"). When omitted, <code>version</code> must be an annual vintage. <code>represented_year</code> reflects the projected season year.</p> <code>None</code> <code>remove_non_burnable</code> <code>list</code> <p>Non-burnable fuel models to drop (<code>NonBurnableFuelModel</code> members or their string keys, e.g. \"NB1\", \"NB2\").</p> <code>None</code> <code>boundary_scatter</code> <code>bool, dict, or BoundaryScatter</code> <p>Stochastic scattering of fuel model boundaries to create ragged, natural-looking transitions instead of staircase edges from nearest-neighbor resampling. <code>None</code> (default) disables scatter. <code>True</code> or <code>{}</code> enables scatter with defaults (<code>depth=10</code>, <code>seed=42</code>). A dict (e.g. <code>{\"depth\": 5, \"seed\": 123}</code>) or a <code>BoundaryScatter</code> instance sets explicit parameters.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"nearest\" for categorical fuel models).</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_fuel_model_grid_from_landfire_fccs","title":"create_fuel_model_grid_from_landfire_fccs","text":"<pre><code>create_fuel_model_grid_from_landfire_fccs(domain, version: Optional[str] = None, remove_bare_ground: bool = False, boundary_scatter=None, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a fuel model grid from LANDFIRE FCCS fuelbeds.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>version</code> <code>str</code> <p>LANDFIRE version (see <code>LandfireFccsVersion</code>). Defaults to the API's current version.</p> <code>None</code> <code>remove_bare_ground</code> <code>bool</code> <p>Drop bare-ground fuelbeds (default False).</p> <code>False</code> <code>boundary_scatter</code> <code>bool, dict, or BoundaryScatter</code> <p>Stochastic scattering of fuelbed boundaries to create ragged, natural-looking transitions instead of staircase edges from nearest-neighbor resampling. <code>None</code> (default) disables scatter. <code>True</code> or <code>{}</code> enables scatter with defaults (<code>depth=10</code>, <code>seed=42</code>). A dict (e.g. <code>{\"depth\": 5, \"seed\": 123}</code>) or a <code>BoundaryScatter</code> instance sets explicit parameters.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method (e.g. \"nearest\" for categorical fuelbeds).</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_pim_grid_from_treemap","title":"create_pim_grid_from_treemap","text":"<pre><code>create_pim_grid_from_treemap(domain, version: Optional[str] = None, output_resolution_m: Optional[float] = None, align_to=None, align: Optional[str] = None, resampling: Optional[str] = None, bands: Optional[list] = None, extent_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a Plot Imputation Map (PIM) grid from TreeMap.</p> <p>TreeMap imputes an FIA plot to every forested 30 m pixel across the conterminous US. The resulting grid carries up to two categorical bands:</p> <ul> <li><code>tm_id</code>: the TreeMap raster pixel value (always present)</li> <li><code>plt_cn</code>: the FIA plot condition number (request via <code>bands</code>)</li> </ul> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>version</code> <code>str</code> <p>TreeMap version (see <code>TreeMapVersion</code>). Defaults to the API's current version.</p> <code>None</code> <code>output_resolution_m</code> <code>float</code> <p>Output cell size in meters, anchored to the domain origin.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match the lattice of an existing grid (or its id).</p> <code>None</code> <code>align</code> <code>str</code> <p>Pass <code>\"native\"</code> to keep the source raster's pixel anchor.</p> <code>None</code> <code>resampling</code> <code>str</code> <p>Resampling method. TreeMap bands are categorical, so use \"nearest\" (interpolating plot ids is meaningless).</p> <code>None</code> <code>bands</code> <code>list</code> <p>TreeMap bands to produce (<code>TreeMapBand</code> members or their string keys: \"tm_id\", \"plt_cn\"). Defaults to <code>tm_id</code> only.</p> <code>None</code> <code>extent_buffer_cells</code> <code>int</code> <p>Result-grid cells to buffer around the domain extent (0-10, default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_grid_from_geotiff","title":"create_grid_from_geotiff","text":"<pre><code>create_grid_from_geotiff(domain, path: str, bands: list, num_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Create a grid by uploading a local GeoTIFF.</p> <p>Creates the grid resource, uploads the file to the returned signed URL, and returns the (pending) Grid. The GeoTIFF's CRS must match the domain CRS.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>path</code> <code>str</code> <p>Path to the local <code>.tif</code>/<code>.tiff</code> file (max 1 GB).</p> required <code>bands</code> <code>list</code> <p>Band definitions (<code>UploadBandDefinition</code>) mapping 1:1 to the GeoTIFF raster bands in order.</p> required <code>num_buffer_cells</code> <code>int</code> <p>Cells kept around the domain extent in the stored grid (default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\"). Call :meth:<code>Grid.wait</code> to block until the uploaded file is processed.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_grid_from_netcdf","title":"create_grid_from_netcdf","text":"<pre><code>create_grid_from_netcdf(domain, path: str, num_buffer_cells: int = 0, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Create a grid by uploading a local NetCDF file.</p> <p>Creates the grid resource, uploads the file to the returned signed URL, and returns the (pending) Grid.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>path</code> <code>str</code> <p>Path to the local NetCDF file.</p> required <code>num_buffer_cells</code> <code>int</code> <p>Cells kept around the domain extent in the stored grid (default 0).</p> <code>0</code> <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\"). Call :meth:<code>Grid.wait</code> to block until the uploaded file is processed.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_uniform_grid","title":"create_uniform_grid","text":"<pre><code>create_uniform_grid(domain, resolution_m: float, bands: Dict[str, Union[float, int]], name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a uniform (constant-value) grid.</p> <p>Each band fills the entire domain with a single value at the given resolution.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in.</p> required <code>resolution_m</code> <code>float</code> <p>Grid cell size in meters (required \u2014 uniform grids have no native resolution).</p> required <code>bands</code> <code>dict</code> <p>Mapping of band key to constant value, e.g. <code>{\"fuel_load\": 0.5, \"fuel_moisture\": 15.0}</code>. Keys must be valid <code>UniformBand</code> values.</p> required <code>name</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The created Grid object (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_grid_from_compose","title":"create_grid_from_compose","text":"<pre><code>create_grid_from_compose(inputs: Mapping[str, Grid], *, select: Optional[list] = None, compute: Optional[list] = None, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a grid by selecting and computing bands from aligned grids.</p> <p>Parameters:</p> Name Type Description Default <code>inputs</code> <code>mapping of str to Grid</code> <p>Alias-to-grid mapping for completed, aligned 2D grids in one domain. Operations refer to their bands as <code>\"alias.band_key\"</code>.</p> required <code>select</code> <code>list of ComposeSelect</code> <p>Bands to copy, built with :func:<code>fastfuels_sdk.v2.compose.select</code>.</p> <code>None</code> <code>compute</code> <code>list of ComposeCompute</code> <p>Bands to calculate, built with :func:<code>fastfuels_sdk.v2.compose.compute</code>.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is composed.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new pending composed Grid.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If inputs or operations have the wrong shape.</p> <code>ValueError</code> <p>If inputs are empty, incomplete, duplicated, or from different domains; if aliases or band references are invalid; or if operation outputs are empty or duplicated.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; composed = ff.grids.create_grid_from_compose(\n...     {\"fuels\": fuel_grid},\n...     select=[ff.compose.select(\"fuel_depth\", \"fuels.fuel_depth\")],\n...     compute=[\n...         ff.compose.compute(\n...             \"fuel_load.1hr\",\n...             \"multiply\",\n...             [\"fuels.fuel_load.1hr\", 0.5],\n...         )\n...     ],\n... )\n&gt;&gt;&gt; composed.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_fuel_grid_from_fccs_lookup","title":"create_fuel_grid_from_fccs_lookup","text":"<pre><code>create_fuel_grid_from_fccs_lookup(source_grid: Grid, bands: list, source_band: str = 'fccs', name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a fuel-parameter grid by looking up FCCS codes in a grid.</p> <p>Parameters:</p> Name Type Description Default <code>source_grid</code> <code>Grid</code> <p>A completed grid carrying FCCS codes (produced by :func:<code>create_fuel_model_grid_from_landfire_fccs</code>).</p> required <code>bands</code> <code>list</code> <p>The FCCS lookup bands to produce (<code>FccsLookupBand</code> members or string keys such as <code>\"fuel_load.duff\"</code>, <code>\"duff_depth\"</code>, and <code>\"fuel_load.live_shrub\"</code>).</p> required <code>source_band</code> <code>str</code> <p>The band in <code>source_grid</code> that holds FCCS codes (default <code>\"fccs\"</code>).</p> <code>'fccs'</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new pending fuel-parameter Grid.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>source_grid</code> is not completed or has no <code>source_band</code> band.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_fuel_grid_from_fbfm13_lookup","title":"create_fuel_grid_from_fbfm13_lookup","text":"<pre><code>create_fuel_grid_from_fbfm13_lookup(source_grid: Grid, bands: list, source_band: str = 'fbfm13', name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a fuel-parameter grid by looking up FBFM13 codes in a grid.</p> <p>Parameters:</p> Name Type Description Default <code>source_grid</code> <code>Grid</code> <p>A completed grid carrying FBFM13 codes (produced by :func:<code>create_fuel_model_grid_from_landfire_fbfm13</code>).</p> required <code>bands</code> <code>list</code> <p>The FBFM13 lookup bands to produce (<code>Fbfm13LookupBand</code> members or string keys such as <code>\"fuel_load.1hr\"</code> and <code>\"fuel_depth\"</code>).</p> required <code>source_band</code> <code>str</code> <p>The band in <code>source_grid</code> that holds FBFM13 codes (default <code>\"fbfm13\"</code>).</p> <code>'fbfm13'</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new pending fuel-parameter Grid.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>source_grid</code> is not completed or has no <code>source_band</code> band.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_fuel_grid_from_fbfm40_lookup","title":"create_fuel_grid_from_fbfm40_lookup","text":"<pre><code>create_fuel_grid_from_fbfm40_lookup(source_grid: Grid, bands: list, source_band: str = 'fbfm', name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None) -&gt; Grid\n</code></pre> <p>Create a fuel-parameter grid by looking up FBFM40 codes in a grid.</p> <p>The <code>fbfm</code> band of an FBFM40 fuel model grid holds categorical fuel-model codes, not the quantities a fire model consumes. This translates those codes into fuel parameters (loadings by size class, fuel-bed depth, surface-area-to-volume ratios), returning a new grid whose bands are the requested parameters.</p> <p>Parameters:</p> Name Type Description Default <code>source_grid</code> <code>Grid</code> <p>A completed grid carrying FBFM40 codes (produced by :func:<code>create_fuel_model_grid_from_landfire_fbfm40</code>).</p> required <code>bands</code> <code>list</code> <p>The FBFM40 lookup bands to produce (<code>Fbfm40LookupBand</code> members or their string keys, e.g. \"fuel_load.1hr\", \"fuel_depth\").</p> required <code>source_band</code> <code>str</code> <p>The band in <code>source_grid</code> that holds FBFM40 codes (default \"fbfm\").</p> <code>'fbfm'</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules applied after the grid is built.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new (pending) fuel-parameter Grid.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>source_grid</code> is not completed, or carries no <code>source_band</code> band to look up (i.e. it is not an FBFM40 fuel model grid).</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_surface_fuel_grid_from_duet","title":"create_surface_fuel_grid_from_duet","text":"<pre><code>create_surface_fuel_grid_from_duet(source_grid: Grid, years_since_burn: int, wind_direction: int = 270, wind_variability: int = 30, bands: Optional[list] = None, calibration: Optional[DuetCalibration] = None, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Create a 2D DUET surface-fuel grid from a 3D tree grid.</p> <p>Parameters:</p> Name Type Description Default <code>source_grid</code> <code>Grid</code> <p>A completed 3D tree grid carrying <code>bulk_density.foliage.live</code>, <code>spcd</code>, and <code>fuel_moisture.live</code> bands.</p> required <code>years_since_burn</code> <code>int</code> <p>Years of litter accumulation to simulate, from 1 through 100.</p> required <code>wind_direction</code> <code>int</code> <p>Prevailing wind direction in whole degrees clockwise from north (0-359, default 270).</p> <code>270</code> <code>wind_variability</code> <code>int</code> <p>Angular spread of wind direction in whole degrees (0-180, default 30).</p> <code>30</code> <code>bands</code> <code>list</code> <p>DUET output bands (<code>DuetBand</code> members or string keys). Defaults to <code>fuel_load.grass</code> and <code>fuel_load.litter</code>.</p> <code>None</code> <code>calibration</code> <code>DuetCalibration</code> <p>Per-parameter and per-fuel-type targets from :func:<code>fastfuels_sdk.v2.calibrations.duet_calibration</code>. If omitted, the grid stores raw DUET values.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new pending DUET surface-fuel Grid.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If a DUET time or wind parameter is not a whole number.</p> <code>ValueError</code> <p>If a parameter is out of range, the source is not completed, or the source lacks a required tree band.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_dead_fuel_moisture_grid_from_fosberg","title":"create_dead_fuel_moisture_grid_from_fosberg","text":"<pre><code>create_dead_fuel_moisture_grid_from_fosberg(source_topography_grid, source_irradiance_grid, dry_bulb_temp: float, relative_humidity: float, time: int, month, elevation=None, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Create a Fosberg 1-hour dead fuel moisture grid from two grids.</p> <p>Derives a grid with a single <code>fuel_moisture.dead.1hr</code> band (percent) using the Fosberg &amp; Deeming 1-hour dead fuel moisture model. The output inherits the topography grid's domain, CRS, transform, and georeference.</p> <p>Parameters:</p> Name Type Description Default <code>source_topography_grid</code> <code>Grid or str</code> <p>A completed 2D topography grid (or its id) carrying <code>slope</code> and <code>aspect</code> bands, both in degrees.</p> required <code>source_irradiance_grid</code> <code>Grid or str</code> <p>A completed Leaflux irradiance grid (or its id) carrying an <code>irradiance.surface.relative</code> band; per-cell shading is derived from it. Must belong to the same domain as the topography grid.</p> required <code>dry_bulb_temp</code> <code>float</code> <p>Dry-bulb air temperature in degrees Fahrenheit. Must be &gt;= 10.</p> required <code>relative_humidity</code> <code>float</code> <p>Relative humidity as a percent, from 0 through 100.</p> required <code>time</code> <code>int</code> <p>Local time of day in 24-hour HHMM form (e.g. 1200 for noon), from 0800 through 1959.</p> required <code>month</code> <code>FuelMoistureMonth or str</code> <p>Month of the burn scenario, selecting the Fosberg correction table (<code>FuelMoistureMonth</code> member or a month name such as <code>\"July\"</code>).</p> required <code>elevation</code> <code>RelativeElevation or str</code> <p>Site elevation relative to the reference weather station (<code>RelativeElevation</code> member or one of <code>\"below\"</code>, <code>\"near\"</code>, <code>\"above\"</code>): <code>below</code> = 1000-2000 ft below the station, <code>near</code> = within 1000 ft (no correction), <code>above</code> = 1000-2000 ft above. This is a Fosberg correction category, not the topography elevation band.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new pending Fosberg dead fuel moisture Grid.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>dry_bulb_temp</code>, <code>relative_humidity</code>, or <code>time</code> is not a number, or <code>time</code> is not a whole number.</p> <code>ValueError</code> <p>If a parameter is out of range, a source grid passed as a Grid is not completed or lacks a required band, or the domain cannot be resolved because both source grids were passed as bare ids.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.create_irradiance_grid_from_leaflux","title":"create_irradiance_grid_from_leaflux","text":"<pre><code>create_irradiance_grid_from_leaflux(source_grid, date_time: datetime, source_terrain_grid=None, bands: Optional[list] = None, extinction_coefficient: float = 0.5, domain=None, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Grid\n</code></pre> <p>Create a 3D LeafLux solar-irradiance grid from a source fuel grid.</p> <p>Computes solar irradiance through the canopy at a single instant, attenuating light through the source grid's leaf area density with a Beer-Lambert extinction coefficient. An optional 2D terrain grid supplies surface elevation for the surface-irradiance band.</p> <p>Parameters:</p> Name Type Description Default <code>source_grid</code> <code>Grid or str</code> <p>A completed 3D grid (or its id) carrying a <code>leaf_area_density</code> band.</p> required <code>date_time</code> <code>datetime</code> <p>The UTC instant at which to compute irradiance.</p> required <code>source_terrain_grid</code> <code>Grid or str</code> <p>A 2D terrain grid (or its id) in the same domain, used for the surface-irradiance band.</p> <code>None</code> <code>bands</code> <code>list</code> <p>Irradiance bands to produce (<code>LeafluxBand</code> members or their string keys, e.g. \"irradiance.surface.relative\", \"irradiance.canopy.relative\"). Defaults to <code>irradiance.surface.relative</code>.</p> <code>None</code> <code>extinction_coefficient</code> <code>float</code> <p>Beer-Lambert extinction coefficient applied to the leaf area density (default 0.5).</p> <code>0.5</code> <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the grid in. Required only when <code>source_grid</code> is given as an id; inferred from the grid otherwise.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new pending irradiance Grid.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>source_grid</code> is a Grid that is not completed or lacks a <code>leaf_area_density</code> band, or if <code>domain</code> is required but not given.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.list_grids","title":"list_grids","text":"<pre><code>list_grids(domain=None, page: int = 0, size: int = 100, sort_by: Optional[str] = None, sort_order: Optional[str] = None, source: Optional[str] = None, product: Optional[str] = None, tag: Optional[str] = None) -&gt; List[Grid]\n</code></pre> <p>List grids in a domain, or across all domains (single page).</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to list grids in. If omitted, grids from all the user's domains are listed.</p> <code>None</code> <code>page</code> <code>int</code> <p>The page number to retrieve, zero-indexed (default 0).</p> <code>0</code> <code>size</code> <code>int</code> <p>The number of grids per page (default 100).</p> <code>100</code> <code>sort_by</code> <code>str</code> <p>Field to sort by: \"name\", \"created_on\", or \"modified_on\".</p> <code>None</code> <code>sort_order</code> <code>str</code> <p>Sort direction: \"ascending\" or \"descending\".</p> <code>None</code> <code>source</code> <code>str</code> <p>Only return grids from this source (e.g. \"landfire\", \"3dep\").</p> <code>None</code> <code>product</code> <code>str</code> <p>Only return grids from this source product (e.g. \"fbfm40\", \"topography\"). Requires <code>source</code>.</p> <code>None</code> <code>tag</code> <code>str</code> <p>Only return grids carrying this tag.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Grid]</code> <p>The requested page of Grid objects.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.get_grid","title":"get_grid","text":"<pre><code>get_grid(domain, grid_id: str) -&gt; Grid\n</code></pre> <p>Retrieve a single grid by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) the grid belongs to.</p> required <code>grid_id</code> <code>str</code> <p>The unique identifier of the grid.</p> required <p>Returns:</p> Type Description <code>Grid</code> <p>The requested Grid object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no grid exists with the given IDs, or the user does not have access.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.check_3dep_coverage","title":"check_3dep_coverage","text":"<pre><code>check_3dep_coverage(domain, resolution_m: Optional[int] = None) -&gt; TopographyThreeDepCoverageResponse\n</code></pre> <p>Check USGS 3DEP tile coverage for a domain before creating a grid.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to check.</p> required <code>resolution_m</code> <code>int</code> <p>Resolution to check: 1, 10, or 30 meters. Defaults to the API's default.</p> <code>None</code> <p>Returns:</p> Type Description <code>TopographyThreeDepCoverageResponse</code> <p>Tile availability, count, URLs, and (for 1 m) acquisition dates.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.check_landfire_fbfm40_coverage","title":"check_landfire_fbfm40_coverage","text":"<pre><code>check_landfire_fbfm40_coverage(domain) -&gt; LandfireCoverageResponse\n</code></pre> <p>Check LANDFIRE FBFM40 release coverage for a domain.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to check.</p> required <p>Returns:</p> Type Description <code>LandfireCoverageResponse</code> <p>Per-release coverage, the latest fully-covering release, and ready-made create-request links.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.check_landfire_fbfm13_coverage","title":"check_landfire_fbfm13_coverage","text":"<pre><code>check_landfire_fbfm13_coverage(domain) -&gt; LandfireCoverageResponse\n</code></pre> <p>Check LANDFIRE FBFM13 release coverage for a domain.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to check.</p> required <p>Returns:</p> Type Description <code>LandfireCoverageResponse</code> <p>Per-release coverage, the latest fully-covering release, and ready-made create-request links.</p>"},{"location":"reference/#fastfuels_sdk.v2.grids.check_landfire_fccs_coverage","title":"check_landfire_fccs_coverage","text":"<pre><code>check_landfire_fccs_coverage(domain) -&gt; LandfireCoverageResponse\n</code></pre> <p>Check LANDFIRE FCCS release coverage for a domain.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to check.</p> required <p>Returns:</p> Type Description <code>LandfireCoverageResponse</code> <p>Per-release coverage, the latest fully-covering release, and ready-made create-request links.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories","title":"fastfuels_sdk.v2.inventories","text":"<p>fastfuels_sdk/v2/inventories.py</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory","title":"Inventory","text":"<p>               Bases: <code>Inventory</code></p> <p>Inventory resource for the FastFuels v2 API.</p> <p>An inventory is a table of individual entities \u2014 trees \u2014 within a domain, generated from a PIM (TreeMap) grid, derived from a canopy height model, or uploaded from your own data. Inventories are asynchronous job resources \u2014 creation starts a background job and returns a pending record; call :meth:<code>wait</code> to block until it is ready.</p> <p>The tree records themselves are retrieved through the data methods (:meth:<code>get_data_metadata</code>, :meth:<code>get_data_partition</code>, :meth:<code>to_dataframe</code>) once the inventory is completed.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Unique identifier for the inventory.</p> <code>domain_id</code> <code>str</code> <p>Identifier of the domain the inventory belongs to.</p> <code>type_</code> <code>InventoryType</code> <p>Type of entities in the inventory (currently always \"tree\").</p> <code>status</code> <code>JobStatus</code> <p>Job status: \"pending\", \"running\", \"completed\", or \"failed\".</p> <code>source</code> <code>InventorySource</code> <p>Where the inventory data comes from (e.g. a PIM grid, a CHM grid, or an upload).</p> <code>name</code> <code>str</code> <p>Human-readable name for the inventory.</p> <code>description</code> <code>str</code> <p>Detailed description of the inventory.</p> <code>progress</code> <code>(JobProgress, optional)</code> <p>Progress info while the job is running.</p> <code>checksum</code> <code>(str, optional)</code> <p>Version marker for the inventory's content; changes each time the data is rebuilt, unaffected by metadata-only edits.</p> <code>modifications</code> <code>(List[InventoryModification], optional)</code> <p>Cumulative modification rules applied to the inventory.</p> <code>treatments</code> <code>(list, optional)</code> <p>Silvicultural treatments applied to the inventory.</p> <code>columns</code> <code>(List[Column], optional)</code> <p>The columns of the inventory's tabular data.</p> <code>forestry_metrics</code> <code>(TreeForestryMetrics, optional)</code> <p>Stand-level tree count, basal area per acre, trees per acre, quadratic mean diameter, and dominant FIA species groups. Populated when a tree inventory completes; <code>None</code> when unavailable.</p> <code>georeference</code> <code>(InventoryGeoreference, optional)</code> <p>Spatial reference of the generated data; populated when the job completes.</p> <code>error</code> <code>(JobError, optional)</code> <p>Error details if the job failed.</p> <code>tags</code> <code>(List[str], optional)</code> <p>User-defined tags for organization.</p> <code>created_on</code> <code>datetime</code> <p>When the inventory was created.</p> <code>modified_on</code> <code>datetime</code> <p>When the inventory was last modified.</p> <p>Examples:</p> <p>Create a tree inventory from a PIM grid and load it as a DataFrame:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk as ff\n&gt;&gt;&gt; pim = ff.grids.create_pim_grid_from_treemap(domain)\n&gt;&gt;&gt; inventory = ff.inventories.create_tree_inventory_from_pim_grid(\n...     domain, pim.wait()\n... )\n&gt;&gt;&gt; trees = inventory.wait().to_dataframe()\n</code></pre> <p>Get an inventory by ID:</p> <pre><code>&gt;&gt;&gt; inventory = ff.get_inventory(domain, \"abc123\")\n</code></pre> See Also <p>create_tree_inventory_from_pim_grid : Expand a PIM grid into trees. create_tree_inventory_from_chm_grid : Derive trees from a canopy height model. create_tree_inventory_from_file : Upload your own tree records. list_inventories : List inventories in a domain or across all domains.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.from_id","title":"from_id  <code>classmethod</code>","text":"<pre><code>from_id(domain_id: str, inventory_id: str) -&gt; Inventory\n</code></pre> <p>Retrieve an existing Inventory resource by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain_id</code> <code>str</code> <p>The unique identifier of the domain the inventory belongs to.</p> required <code>inventory_id</code> <code>str</code> <p>The unique identifier of the inventory to retrieve.</p> required <p>Returns:</p> Type Description <code>Inventory</code> <p>The requested Inventory object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no inventory exists with the given IDs, or the user does not have access to it.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.refresh","title":"refresh","text":"<pre><code>refresh() -&gt; Inventory\n</code></pre> <p>Update this Inventory in place with the latest data from the API.</p> <p>Returns:</p> Type Description <code>Inventory</code> <p><code>self</code>, updated with the latest data (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.wait","title":"wait","text":"<pre><code>wait(timeout: Optional[float] = None, verbose: bool = False) -&gt; Inventory\n</code></pre> <p>Poll the inventory job until it reaches a terminal status.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Maximum seconds to wait. <code>None</code> (default) waits indefinitely; the job runs server-side regardless, so a bounded wait is resumable.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If True, print the job status at each poll.</p> <code>False</code> <p>Returns:</p> Type Description <code>Inventory</code> <p><code>self</code>, updated to its terminal state (so calls chain).</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If <code>timeout</code> is set and elapses before a terminal status.</p> <code>JobFailedError</code> <p>If the job finished with status \"failed\".</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.update","title":"update","text":"<pre><code>update(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; Inventory\n</code></pre> <p>Update the inventory's mutable metadata (name, description, tags) in place.</p> <p>Only provided fields are sent. If no fields are provided, no API call is made.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>New name for the inventory.</p> <code>None</code> <code>description</code> <code>str</code> <p>New description for the inventory.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>New tags for the inventory (replaces existing tags).</p> <code>None</code> <p>Returns:</p> Type Description <code>Inventory</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.delete","title":"delete","text":"<pre><code>delete() -&gt; None\n</code></pre> <p>Delete this inventory and its data.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.duplicate","title":"duplicate","text":"<pre><code>duplicate(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; Inventory\n</code></pre> <p>Create an independent copy of this inventory under a new ID.</p> <p>The copy is a true clone \u2014 the finished data is byte-copied, not regenerated \u2014 carrying over the source's <code>source</code>, <code>modifications</code>, <code>treatments</code>, <code>columns</code>, <code>georeference</code>, and <code>checksum</code> verbatim. Use this to branch a scenario: duplicate, then modify the copy while the original stays untouched.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name for the copy. Defaults to the source's name.</p> <code>None</code> <code>description</code> <code>str</code> <p>Description for the copy. Defaults to the source's description.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>Tags for the copy. Defaults to the source's tags.</p> <code>None</code> <p>Returns:</p> Type Description <code>Inventory</code> <p>The new Inventory object (job status \"pending\" while the data is copied; call :meth:<code>wait</code> before using it).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p> <code>UnprocessableEntityException</code> <p>If the inventory is not in \"completed\" status.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.apply_modifications","title":"apply_modifications","text":"<pre><code>apply_modifications(modifications: list) -&gt; Inventory\n</code></pre> <p>Apply modification rules to this inventory in place.</p> <p>The inventory keeps its ID; the submitted rules are queued while the tree data is re-derived as a background job. The inventory returns to \"pending\" status, and the rules are appended to its cumulative <code>modifications</code> list once processing completes. Call :meth:<code>wait</code> before using its data. To keep the original data, :meth:<code>duplicate</code> first and modify the copy.</p> <p>Parameters:</p> Name Type Description Default <code>modifications</code> <code>list</code> <p>Modification rules (<code>InventoryModification</code>). Each rule filters trees by conditions (ANDed together) and applies actions \u2014 remove, multiply, divide, add, subtract, or replace \u2014 to the matching rows.</p> required <p>Returns:</p> Type Description <code>Inventory</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.apply_treatments","title":"apply_treatments","text":"<pre><code>apply_treatments(treatments: list) -&gt; Inventory\n</code></pre> <p>Apply silvicultural treatments to this inventory in place.</p> <p>The inventory keeps its ID; the submitted treatments are queued while the tree data is re-derived as a background job. The inventory returns to \"pending\" status, and the treatments are appended to its cumulative <code>treatments</code> list once processing completes. Call :meth:<code>wait</code> before using its data. To keep the original data, :meth:<code>duplicate</code> first and treat the copy.</p> <p>Parameters:</p> Name Type Description Default <code>treatments</code> <code>list</code> <p>Treatments thinning the stand to a target. Build them with :func:<code>fastfuels_sdk.v2.treatments.basal_area_treatment</code> (residual basal area) or :func:<code>fastfuels_sdk.v2.treatments.diameter_treatment</code> (diameter limit), available as <code>ff.basal_area_treatment</code> / <code>ff.diameter_treatment</code>.</p> required <p>Returns:</p> Type Description <code>Inventory</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; inventory.apply_treatments([ff.basal_area_treatment(\"from_below\", 25.0)])\n&gt;&gt;&gt; inventory.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.voxelize","title":"voxelize","text":"<pre><code>voxelize(horizontal_resolution_m: Optional[float] = None, vertical_resolution_m: Optional[float] = None, bands: Optional[list] = None, crown_profile_model: Optional[str] = None, biomass_source=None, max_crown_radius_source=None, moisture_model=None, seed: Optional[int] = None, name: str = '', description: str = '', tags: Optional[List[str]] = None)\n</code></pre> <p>Create a 3D voxelized tree fuel grid from this inventory.</p> <p>Distributes each tree's crown biomass into 3D cells, producing a voxel grid of canopy fuel properties.</p> <p>Parameters:</p> Name Type Description Default <code>horizontal_resolution_m</code> <code>float</code> <p>Horizontal (x and y) cell size in meters. Must be given together with <code>vertical_resolution_m</code>; if both are omitted the API default resolution is used.</p> <code>None</code> <code>vertical_resolution_m</code> <code>float</code> <p>Vertical (z) cell size in meters.</p> <code>None</code> <code>bands</code> <code>list</code> <p>Voxel bands to produce (<code>TreeBand</code> members or their string keys, e.g. \"bulk_density.foliage.live\", \"fuel_moisture.dead\", \"savr.foliage\", \"spcd\", \"tree_id\", \"volume_fraction\"). Defaults to the API's standard band set.</p> <code>None</code> <code>crown_profile_model</code> <code>str</code> <p>Crown shape model used to distribute biomass: \"beta\" or \"purves\".</p> <code>None</code> <code>biomass_source</code> <code>AllometryBiomassSource or InventoryColumnsBiomassSource</code> <p>Where crown biomass values come from: allometry equations or columns of this inventory.</p> <code>None</code> <code>max_crown_radius_source</code> <code>optional</code> <p>Where each tree's maximum crown radius comes from.</p> <code>None</code> <code>moisture_model</code> <code>MoistureModel</code> <p>Fuel moisture values assigned to the voxelized fuels.</p> <code>None</code> <code>seed</code> <code>int</code> <p>Random seed for reproducibility. Generated randomly if omitted.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the new grid.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the new grid.</p> <code>None</code> <p>Returns:</p> Type Description <code>Grid</code> <p>The new (pending) 3D voxel Grid.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If only one of <code>horizontal_resolution_m</code> / <code>vertical_resolution_m</code> is given, or the inventory is not completed.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; inventory.wait()\n&gt;&gt;&gt; voxels = inventory.voxelize(\n...     horizontal_resolution_m=2.0, vertical_resolution_m=1.0\n... )\n&gt;&gt;&gt; voxels.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.export","title":"export","text":"<pre><code>export(format: str = 'parquet', columns: Optional[List[str]] = None, expiration_days: int = 7, name: str = '', description: str = '', tags: Optional[List[str]] = None)\n</code></pre> <p>Export this inventory to a downloadable file format.</p> <p>Parameters:</p> Name Type Description Default <code>format</code> <code>str</code> <p>Export format: \"parquet\" (zipped, default), \"csv\", \"geojson\", or \"geopackage\".</p> <code>'parquet'</code> <code>columns</code> <code>List[str]</code> <p>Columns to include (default: all columns).</p> <code>None</code> <code>expiration_days</code> <code>int</code> <p>Days until the signed download URL expires (max 7, default 7).</p> <code>7</code> <code>name</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the export.</p> <code>None</code> <p>Returns:</p> Type Description <code>Export</code> <p>The created Export object (job status \"pending\"). Call :meth:<code>Export.wait</code> and then :meth:<code>Export.to_file</code> to download the file.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p> <code>UnprocessableEntityException</code> <p>If the inventory is not in \"completed\" status.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.get_data_metadata","title":"get_data_metadata","text":"<pre><code>get_data_metadata() -&gt; InventoryDataMetadata\n</code></pre> <p>Get the partition layout of the inventory's tree records.</p> <p>The tree records are served in fixed-size partitions. Use this to discover how many partitions exist before retrieving them with :meth:<code>get_data_partition</code> (or call :meth:<code>to_dataframe</code> to retrieve everything at once).</p> <p>Returns:</p> Type Description <code>InventoryDataMetadata</code> <p>With attributes <code>num_partitions</code>, <code>total_rows</code>, <code>columns</code> (column names), and <code>partitions</code> (per-partition row counts). An inventory with no data has <code>num_partitions</code> 0.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p> <code>UnprocessableEntityException</code> <p>If the inventory is not in \"completed\" status.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; metadata = inventory.get_data_metadata()\n&gt;&gt;&gt; metadata.num_partitions\n1\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.column_summary","title":"column_summary","text":"<pre><code>column_summary(column: str)\n</code></pre> <p>Return summary statistics for one column without downloading records.</p> <p>The server computes a per-column summary when an inventory completes, so this provides a cheap overview without fetching the inventory's tree records (unlike :meth:<code>to_dataframe</code>).</p> <p>Parameters:</p> Name Type Description Default <code>column</code> <code>str</code> <p>The column key to summarize (see :attr:<code>columns</code> for available keys).</p> required <p>Returns:</p> Type Description <code>ContinuousColumnSummary or CategoricalColumnSummary or None</code> <p>The column's summary, discriminated by its <code>type_</code>: <code>\"continuous\"</code> carries <code>count</code>, <code>null_count</code>, <code>min_</code>, <code>max_</code>, <code>mean</code>, and <code>std</code>; <code>\"categorical\"</code> carries <code>count</code>, <code>null_count</code>, and <code>unique_count</code>. <code>None</code> until the inventory completes (call :meth:<code>wait</code> first).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>column</code> is not one of the inventory's columns.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; inventory = ff.get_inventory(domain, \"abc123\").wait()\n&gt;&gt;&gt; inventory.column_summary(\"dbh\").type_\n'continuous'\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.get_data_partition","title":"get_data_partition","text":"<pre><code>get_data_partition(partition_index: int, columns: Optional[List[str]] = None, json_orientation: str = 'split') -&gt; InventoryDataResponse\n</code></pre> <p>Get one partition of the inventory's tree records.</p> <p>Parameters:</p> Name Type Description Default <code>partition_index</code> <code>int</code> <p>Zero-indexed partition number. Must be less than the <code>num_partitions</code> reported by :meth:<code>get_data_metadata</code>.</p> required <code>columns</code> <code>List[str]</code> <p>Column subset to retrieve (default: all columns).</p> <code>None</code> <code>json_orientation</code> <code>(split, records)</code> <p>JSON layout. <code>\"split\"</code> (default) returns rows as lists of values in <code>partition.columns</code> order; <code>\"records\"</code> returns self-describing row mappings.</p> <code>\"split\"</code> <p>Returns:</p> Type Description <code>InventoryDataResponse</code> <p>With attributes <code>partition</code>, <code>num_rows</code>, <code>columns</code> (column names), and <code>data</code> (row lists for <code>\"split\"</code> or row mappings for <code>\"records\"</code>).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p> <code>UnprocessableEntityException</code> <p>If <code>partition_index</code> is past the last partition, or the inventory is not in \"completed\" status.</p> <code>ValueError</code> <p>If <code>json_orientation</code> is not <code>\"split\"</code> or <code>\"records\"</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; partition = inventory.get_data_partition(0)\n&gt;&gt;&gt; partition.num_rows\n1392\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.to_dataframe","title":"to_dataframe","text":"<pre><code>to_dataframe(columns: Optional[List[str]] = None) -&gt; pd.DataFrame\n</code></pre> <p>Retrieve the inventory's tree records as a pandas DataFrame.</p> <p>Retrieves every partition as CSV, parses it with :func:<code>pandas.read_csv</code>, and concatenates the partitions into a single DataFrame with one row per tree, preserving source order.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>List[str]</code> <p>Column subset to retrieve (default: all columns).</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>DataFrame with one row per tree. Empty if the inventory contains no trees.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the inventory no longer exists.</p> <code>UnprocessableEntityException</code> <p>If the inventory is not in \"completed\" status.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; inventory.wait()\n&gt;&gt;&gt; trees = inventory.to_dataframe()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.Inventory.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; str\n</code></pre> <p>Serialize the complete Inventory object to a JSON string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The Inventory as a pretty-printed JSON string.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.create_tree_inventory_from_pim_grid","title":"create_tree_inventory_from_pim_grid","text":"<pre><code>create_tree_inventory_from_pim_grid(domain, pim_grid, seed: Optional[int] = None, point_process: Optional[str] = None, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None, treatments: Optional[list] = None) -&gt; Inventory\n</code></pre> <p>Create a tree inventory by expanding a PIM (TreeMap) grid.</p> <p>Each pixel of the completed PIM grid imputes an FIA plot; expansion generates individual tree records from those plots and assigns each tree a coordinate with a spatial point process.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the inventory in.</p> required <code>pim_grid</code> <code>Grid or str</code> <p>A completed PIM grid (or its id) to expand. See :func:<code>fastfuels_sdk.v2.grids.create_pim_grid_from_treemap</code>.</p> required <code>seed</code> <code>int</code> <p>Random seed for reproducibility. Generated randomly if omitted.</p> <code>None</code> <code>point_process</code> <code>str</code> <p>Spatial point process for tree coordinate assignment (<code>PointProcess</code> member or its string key, e.g. \"inhomogeneous_poisson\").</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the inventory.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules (<code>InventoryModification</code>) applied after expansion.</p> <code>None</code> <code>treatments</code> <code>list</code> <p>Silvicultural treatments (<code>InventoryBasalAreaTreatment</code> or <code>InventoryDiameterTreatment</code>) applied after modifications.</p> <code>None</code> <p>Returns:</p> Type Description <code>Inventory</code> <p>The created Inventory object (job status \"pending\" or \"running\").</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk as ff\n&gt;&gt;&gt; pim = ff.grids.create_pim_grid_from_treemap(domain)\n&gt;&gt;&gt; inventory = ff.inventories.create_tree_inventory_from_pim_grid(\n...     domain, pim.wait(), seed=42\n... )\n&gt;&gt;&gt; inventory.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.create_tree_inventory_from_chm_grid","title":"create_tree_inventory_from_chm_grid","text":"<pre><code>create_tree_inventory_from_chm_grid(domain, chm_grid, algorithm=None, name: str = '', description: str = '', tags: Optional[List[str]] = None, modifications: Optional[list] = None, treatments: Optional[list] = None) -&gt; Inventory\n</code></pre> <p>Create a tree inventory by isolating stems in a canopy height model.</p> <p>Detects individual trees in a completed canopy height model (CHM) grid and derives a tree record from each detected stem.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the inventory in.</p> required <code>chm_grid</code> <code>Grid or str</code> <p>A completed canopy height model grid (or its id). See :func:<code>fastfuels_sdk.v2.grids.create_canopy_height_grid_from_meta</code> and :func:<code>fastfuels_sdk.v2.grids.create_canopy_height_grid_from_naip_chm</code>.</p> required <code>algorithm</code> <code>StemIsolationLmf or StemIsolationVwf</code> <p>Stem isolation algorithm and its parameters: local maximum filtering (<code>StemIsolationLmf</code>) or variable window filtering (<code>StemIsolationVwf</code>). Defaults to the API's standard algorithm.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the inventory.</p> <code>None</code> <code>modifications</code> <code>list</code> <p>Modification rules (<code>InventoryModification</code>) applied after stem isolation.</p> <code>None</code> <code>treatments</code> <code>list</code> <p>Silvicultural treatments (<code>InventoryBasalAreaTreatment</code> or <code>InventoryDiameterTreatment</code>) applied after modifications.</p> <code>None</code> <p>Returns:</p> Type Description <code>Inventory</code> <p>The created Inventory object (job status \"pending\" or \"running\").</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk as ff\n&gt;&gt;&gt; chm = ff.grids.create_canopy_height_grid_from_meta(domain)\n&gt;&gt;&gt; inventory = ff.inventories.create_tree_inventory_from_chm_grid(\n...     domain, chm.wait()\n... )\n&gt;&gt;&gt; inventory.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.create_tree_inventory_from_file","title":"create_tree_inventory_from_file","text":"<pre><code>create_tree_inventory_from_file(domain, path: str, format: Optional[str] = None, columns=None, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Inventory\n</code></pre> <p>Create a tree inventory by uploading your own tree records.</p> <p>Creates the inventory resource, uploads the file to the returned signed URL, and returns the (pending) Inventory. Coordinates must be in the domain's CRS.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the inventory in.</p> required <code>path</code> <code>str</code> <p>Path to the local file: <code>.csv</code>, <code>.geojson</code>/<code>.json</code>, or <code>.gpkg</code>.</p> required <code>format</code> <code>str</code> <p>Upload format: \"csv\", \"geojson\", or \"geopackage\". Inferred from the file extension if omitted.</p> <code>None</code> <code>columns</code> <code>dict or InventoryColumnMapping</code> <p>Mapping from the standard column roles to your file's column names, e.g. <code>{\"x\": \"X_UTM\", \"y\": \"Y_UTM\", \"dbh\": \"DBH_CM\"}</code>. Roles: \"x\", \"y\", \"height\", \"dbh\", \"crown_ratio\", \"fia_species_code\", \"fia_status_code\". Columns whose names already match need no mapping.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the inventory.</p> <code>None</code> <p>Returns:</p> Type Description <code>Inventory</code> <p>The created Inventory object (job status \"pending\"). Call :meth:<code>Inventory.wait</code> to block until the uploaded file is processed.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>format</code> is omitted and the file extension is not one of <code>.csv</code>, <code>.geojson</code>, <code>.json</code>, or <code>.gpkg</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk as ff\n&gt;&gt;&gt; inventory = ff.inventories.create_tree_inventory_from_file(\n...     domain, \"plot_trees.csv\", columns={\"x\": \"X_UTM\", \"y\": \"Y_UTM\"}\n... )\n&gt;&gt;&gt; inventory.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.create_tree_inventory_from_gdam","title":"create_tree_inventory_from_gdam","text":"<pre><code>create_tree_inventory_from_gdam(domain, source_inventory, impute_columns: Optional[list] = None, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Inventory\n</code></pre> <p>Create a tree inventory by imputing morphology with GDAM allometry.</p> <p>GDAM fills in missing per-tree morphology \u2014 diameter at breast height, crown ratio, and FIA species code \u2014 for a completed source tree inventory (for example one uploaded with coordinates and heights only). Existing values are preserved; only missing cells are imputed. The result is a new inventory; the source is left unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the inventory in.</p> required <code>source_inventory</code> <code>Inventory or str</code> <p>A completed tree inventory (or its id) whose missing morphology columns GDAM will fill in.</p> required <code>impute_columns</code> <code>list</code> <p>Which morphology columns to impute (<code>CreateGdamInventoryRequestImputeColumnsItem</code> members or their string keys: \"dbh\", \"crown_ratio\", \"fia_species_code\"). Defaults to all three. Narrow it to impute fewer columns; columns left out keep the source's values.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the inventory.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the inventory.</p> <code>None</code> <p>Returns:</p> Type Description <code>Inventory</code> <p>The created Inventory object (job status \"pending\"). Call :meth:<code>Inventory.wait</code> to block until imputation finishes.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk as ff\n&gt;&gt;&gt; sparse = ff.inventories.create_tree_inventory_from_file(\n...     domain, \"stems.csv\"  # x, y, height only\n... )\n&gt;&gt;&gt; sparse.wait()\n&gt;&gt;&gt; full = ff.inventories.create_tree_inventory_from_gdam(domain, sparse)\n&gt;&gt;&gt; full.wait()\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.inventories.list_inventories","title":"list_inventories","text":"<pre><code>list_inventories(domain=None, page: int = 0, size: int = 100, sort_by: Optional[str] = None, sort_order: Optional[str] = None, source: Optional[str] = None, tag: Optional[str] = None) -&gt; List[Inventory]\n</code></pre> <p>List inventories in a domain, or across all domains (single page).</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to list inventories in. If omitted, inventories from all the user's domains are listed.</p> <code>None</code> <code>page</code> <code>int</code> <p>The page number to retrieve, zero-indexed (default 0).</p> <code>0</code> <code>size</code> <code>int</code> <p>The number of inventories per page (default 100).</p> <code>100</code> <code>sort_by</code> <code>str</code> <p>Field to sort by: \"name\", \"created_on\", or \"modified_on\".</p> <code>None</code> <code>sort_order</code> <code>str</code> <p>Sort direction: \"ascending\" or \"descending\".</p> <code>None</code> <code>source</code> <code>str</code> <p>Only return inventories from this source (e.g. \"pim\").</p> <code>None</code> <code>tag</code> <code>str</code> <p>Only return inventories carrying this tag.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Inventory]</code> <p>The requested page of Inventory objects.</p>"},{"location":"reference/#fastfuels_sdk.v2.inventories.get_inventory","title":"get_inventory","text":"<pre><code>get_inventory(domain, inventory_id: str) -&gt; Inventory\n</code></pre> <p>Retrieve a single inventory by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) the inventory belongs to.</p> required <code>inventory_id</code> <code>str</code> <p>The unique identifier of the inventory.</p> required <p>Returns:</p> Type Description <code>Inventory</code> <p>The requested Inventory object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no inventory exists with the given IDs, or the user does not have access.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds","title":"fastfuels_sdk.v2.point_clouds","text":"<p>fastfuels_sdk/v2/point_clouds.py</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud","title":"PointCloud","text":"<p>               Bases: <code>PointCloud</code></p> <p>Point cloud resource for the FastFuels v2 API.</p> <p>A point cloud is a 3D LiDAR dataset within a domain, either fetched from USGS 3DEP or uploaded from your own ALS (airborne) or TLS (terrestrial) scan. Point clouds are asynchronous job resources \u2014 creation starts a background job and returns a pending record; call :meth:<code>wait</code> to block until the data is processed.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Unique identifier for the point cloud.</p> <code>domain_id</code> <code>str</code> <p>Identifier of the domain the point cloud belongs to.</p> <code>type_</code> <code>PointCloudType</code> <p>Scan type: \"als\" (airborne) or \"tls\" (terrestrial).</p> <code>status</code> <code>JobStatus</code> <p>Job status: \"pending\", \"running\", \"completed\", or \"failed\".</p> <code>source</code> <code>PointCloudSource</code> <p>Where the point cloud data comes from.</p> <code>name</code> <code>str</code> <p>Human-readable name for the point cloud.</p> <code>description</code> <code>str</code> <p>Detailed description of the point cloud.</p> <code>progress</code> <code>(JobProgress, optional)</code> <p>Progress info while the job is running.</p> <code>checksum</code> <code>(str, optional)</code> <p>Version marker for the point cloud's content; changes each time the data is rebuilt, unaffected by metadata-only edits.</p> <code>georeference</code> <code>(PointCloudGeoreference, optional)</code> <p>Spatial reference (CRS and bounds); populated when the job completes.</p> <code>summary</code> <code>(PointCloudSummary, optional)</code> <p>Summary statistics of the points; populated when the job completes.</p> <code>error</code> <code>(JobError, optional)</code> <p>Error details if the job failed.</p> <code>tags</code> <code>(List[str], optional)</code> <p>User-defined tags for organization.</p> <code>created_on</code> <code>datetime</code> <p>When the point cloud was created.</p> <code>modified_on</code> <code>datetime</code> <p>When the point cloud was last modified.</p> <p>Examples:</p> <p>Upload a point cloud and wait for it to process:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; pc = ff.point_clouds.create_point_cloud_from_file(\n...     domain, \"scan.laz\", point_cloud_type=\"als\"\n... )\n&gt;&gt;&gt; pc.wait()\n</code></pre> <p>Get a point cloud by ID:</p> <pre><code>&gt;&gt;&gt; pc = ff.get_point_cloud(domain, \"abc123\")\n</code></pre> See Also <p>create_point_cloud_from_3dep : Fetch public airborne LiDAR from USGS 3DEP. create_point_cloud_from_file : Upload a local LiDAR file. list_point_clouds : List point clouds in a domain or across all domains.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.from_id","title":"from_id  <code>classmethod</code>","text":"<pre><code>from_id(domain_id: str, point_cloud_id: str) -&gt; PointCloud\n</code></pre> <p>Retrieve an existing PointCloud resource by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain_id</code> <code>str</code> <p>The unique identifier of the domain the point cloud belongs to.</p> required <code>point_cloud_id</code> <code>str</code> <p>The unique identifier of the point cloud to retrieve.</p> required <p>Returns:</p> Type Description <code>PointCloud</code> <p>The requested PointCloud object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no point cloud exists with the given IDs, or the user does not have access to it.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.refresh","title":"refresh","text":"<pre><code>refresh() -&gt; PointCloud\n</code></pre> <p>Update this PointCloud in place with the latest data from the API.</p> <p>Returns:</p> Type Description <code>PointCloud</code> <p><code>self</code>, updated with the latest data (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the point cloud no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.wait","title":"wait","text":"<pre><code>wait(timeout: Optional[float] = None, verbose: bool = False) -&gt; PointCloud\n</code></pre> <p>Poll the point cloud job until it reaches a terminal status.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Maximum seconds to wait. <code>None</code> (default) waits indefinitely; the job runs server-side regardless, so a bounded wait is resumable.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If True, print the job status at each poll.</p> <code>False</code> <p>Returns:</p> Type Description <code>PointCloud</code> <p><code>self</code>, updated to its terminal state (so calls chain).</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If <code>timeout</code> is set and elapses before a terminal status.</p> <code>JobFailedError</code> <p>If the job finished with status \"failed\".</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.update","title":"update","text":"<pre><code>update(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; PointCloud\n</code></pre> <p>Update the point cloud's metadata (name, description, tags) in place.</p> <p>Only provided fields are sent. If no fields are provided, no API call is made.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>New name for the point cloud.</p> <code>None</code> <code>description</code> <code>str</code> <p>New description for the point cloud.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>New tags for the point cloud (replaces existing tags).</p> <code>None</code> <p>Returns:</p> Type Description <code>PointCloud</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the point cloud no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.delete","title":"delete","text":"<pre><code>delete() -&gt; None\n</code></pre> <p>Delete this point cloud and its data.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the point cloud no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.metadata","title":"metadata","text":"<pre><code>metadata() -&gt; PointCloudDataMetadata\n</code></pre> <p>Return the point cloud's tile index without downloading any points.</p> <p>The metadata is the entry point for reading data: it lists the occupied tiles, the stored columns and their dtypes, the coordinate encoding (<code>scales</code>/<code>offsets</code>), and the cumulative point count at each level of detail. :meth:<code>to_numpy</code> and :meth:<code>to_dataframe</code> page over the tiles it reports.</p> <p>Returns:</p> Type Description <code>PointCloudDataMetadata</code> <p>The tile catalogue: <code>tile_m</code>, <code>lod_levels</code>, <code>crs</code>, <code>bounds</code>, <code>scales</code>, <code>offsets</code>, <code>columns</code>, and <code>tiles</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the point cloud is not completed.</p> <code>NotFoundException</code> <p>If the point cloud no longer exists.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; pc = ff.get_point_cloud(domain, \"abc123\").wait()\n&gt;&gt;&gt; meta = pc.metadata()\n&gt;&gt;&gt; len(meta.tiles)\n4\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.to_dataframe","title":"to_dataframe","text":"<pre><code>to_dataframe(lod: Optional[int] = None, classes: Optional[Union[int, Sequence[int], str]] = None, columns: Optional[Sequence[str]] = None, decode_coordinates: bool = True)\n</code></pre> <p>Read this point cloud's points into an in-memory pandas DataFrame.</p> <p>Pages over every occupied tile and stacks them into one frame, one row per point and one column per stored attribute (<code>X</code>, <code>Y</code>, <code>Z</code>, <code>classification</code>, and any source columns such as <code>intensity</code>).</p> <p>Parameters:</p> Name Type Description Default <code>lod</code> <code>int</code> <p>Inclusive level-of-detail ceiling. <code>0</code> is the coarsest sample and each higher value adds finer points; <code>None</code> (default) reads every point. Valid values are <code>0</code> through <code>lod_levels - 1</code> from :meth:<code>metadata</code>.</p> <code>None</code> <code>classes</code> <code>int or sequence of int or str</code> <p>ASPRS classification codes to keep (e.g. <code>[2, 5]</code> for ground and high vegetation). <code>None</code> (default) keeps every class.</p> <code>None</code> <code>columns</code> <code>sequence of str</code> <p>Stored columns to read, in the returned order. <code>None</code> (default) reads every stored column (see :meth:<code>metadata</code>).</p> <code>None</code> <code>decode_coordinates</code> <code>bool</code> <p>If True (default), <code>X</code>/<code>Y</code>/<code>Z</code> are decoded to CRS coordinates (floats). If False, they stay the stored scaled integers.</p> <code>True</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>One row per point, columns in <code>columns</code> order (or the point cloud's stored order).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the point cloud is not completed.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; pc = ff.get_point_cloud(domain, \"abc123\").wait()\n&gt;&gt;&gt; df = pc.to_dataframe(classes=[2])\n&gt;&gt;&gt; df.columns.tolist()\n['X', 'Y', 'Z', 'classification']\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.to_numpy","title":"to_numpy","text":"<pre><code>to_numpy(lod: Optional[int] = None, classes: Optional[Union[int, Sequence[int], str]] = None, columns: Sequence[str] = _XYZ, decode_coordinates: bool = True) -&gt; np.ndarray\n</code></pre> <p>Read this point cloud's points into an in-memory NumPy array.</p> <p>Pages over every occupied tile and stacks the requested columns into a single <code>(N, k)</code> array, where <code>N</code> is the total number of points and <code>k</code> is the number of columns. All columns share the <code>float64</code> dtype of the returned array.</p> <p>Parameters:</p> Name Type Description Default <code>lod</code> <code>int</code> <p>Inclusive level-of-detail ceiling. <code>0</code> is the coarsest sample and each higher value adds finer points; <code>None</code> (default) reads every point. Valid values are <code>0</code> through <code>lod_levels - 1</code> from :meth:<code>metadata</code>.</p> <code>None</code> <code>classes</code> <code>int or sequence of int or str</code> <p>ASPRS classification codes to keep (e.g. <code>[2, 5]</code> for ground and high vegetation). <code>None</code> (default) keeps every class.</p> <code>None</code> <code>columns</code> <code>sequence of str</code> <p>Columns to stack into the array, in order. Defaults to the <code>(\"X\", \"Y\", \"Z\")</code> coordinates.</p> <code>_XYZ</code> <code>decode_coordinates</code> <code>bool</code> <p>If True (default), <code>X</code>/<code>Y</code>/<code>Z</code> are decoded to CRS coordinates. If False, they stay the stored scaled integers.</p> <code>True</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>A <code>(N, k)</code> <code>float64</code> array of the requested columns.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the point cloud is not completed.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; pc = ff.get_point_cloud(domain, \"abc123\").wait()\n&gt;&gt;&gt; points = pc.to_numpy()\n&gt;&gt;&gt; points.shape\n(48213, 3)\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.PointCloud.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; str\n</code></pre> <p>Serialize the complete PointCloud object to a JSON string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The PointCloud as a pretty-printed JSON string.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.check_3dep_coverage","title":"check_3dep_coverage","text":"<pre><code>check_3dep_coverage(domain) -&gt; PointCloudThreeDepCoverageResponse\n</code></pre> <p>Check USGS 3DEP LiDAR coverage before creating a point cloud.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to check.</p> required <p>Returns:</p> Type Description <code>PointCloudThreeDepCoverageResponse</code> <p>Availability, coverage fraction, estimated point count and budget, and the contributing acquisitions.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.create_point_cloud_from_3dep","title":"create_point_cloud_from_3dep","text":"<pre><code>create_point_cloud_from_3dep(domain, datasets: Optional[List[str]] = None, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; PointCloud\n</code></pre> <p>Create an airborne point cloud from USGS 3DEP LiDAR.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the point cloud in.</p> required <code>datasets</code> <code>List[str]</code> <p>Acquisition names to read, in priority order. Use :func:<code>check_3dep_coverage</code> to discover names. If omitted, the API selects acquisitions automatically.</p> <code>None</code> <code>name</code> <code>str</code> <p>Metadata for the point cloud.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the point cloud.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the point cloud.</p> <code>None</code> <p>Returns:</p> Type Description <code>PointCloud</code> <p>The created airborne PointCloud (job status \"pending\" or \"running\").</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.create_point_cloud_from_file","title":"create_point_cloud_from_file","text":"<pre><code>create_point_cloud_from_file(domain, path: str, point_cloud_type: str, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; PointCloud\n</code></pre> <p>Create a point cloud by uploading a local LiDAR file.</p> <p>Creates the point cloud resource, uploads the file to the returned signed URL, and returns the (pending) PointCloud.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to create the point cloud in.</p> required <code>path</code> <code>str</code> <p>Path to the local LiDAR file (e.g. <code>.las</code>/<code>.laz</code>).</p> required <code>point_cloud_type</code> <code>str</code> <p>Scan type: \"als\" (airborne) or \"tls\" (terrestrial). A <code>PointCloudType</code> member is also accepted.</p> required <code>name</code> <code>str</code> <p>Metadata for the point cloud.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the point cloud.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the point cloud.</p> <code>None</code> <p>Returns:</p> Type Description <code>PointCloud</code> <p>The created PointCloud object (job status \"pending\"). Call :meth:<code>PointCloud.wait</code> to block until the uploaded file is processed.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.list_point_clouds","title":"list_point_clouds","text":"<pre><code>list_point_clouds(domain=None, page: int = 0, size: int = 100, sort_by: Optional[str] = None, sort_order: Optional[str] = None, point_cloud_type: Optional[str] = None, source: Optional[str] = None, tag: Optional[str] = None) -&gt; List[PointCloud]\n</code></pre> <p>List point clouds in a domain, or across all domains (single page).</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) to list point clouds in. If omitted, point clouds from all the user's domains are listed.</p> <code>None</code> <code>page</code> <code>int</code> <p>The page number to retrieve, zero-indexed (default 0).</p> <code>0</code> <code>size</code> <code>int</code> <p>The number of point clouds per page (default 100).</p> <code>100</code> <code>sort_by</code> <code>str</code> <p>Field to sort by: \"name\", \"created_on\", or \"modified_on\".</p> <code>None</code> <code>sort_order</code> <code>str</code> <p>Sort direction: \"ascending\" or \"descending\".</p> <code>None</code> <code>point_cloud_type</code> <code>str</code> <p>Only return point clouds of this scan type: \"als\" or \"tls\".</p> <code>None</code> <code>source</code> <code>str</code> <p>Only return point clouds from this source.</p> <code>None</code> <code>tag</code> <code>str</code> <p>Only return point clouds carrying this tag.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[PointCloud]</code> <p>The requested page of PointCloud objects.</p>"},{"location":"reference/#fastfuels_sdk.v2.point_clouds.get_point_cloud","title":"get_point_cloud","text":"<pre><code>get_point_cloud(domain, point_cloud_id: str) -&gt; PointCloud\n</code></pre> <p>Retrieve a single point cloud by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) the point cloud belongs to.</p> required <code>point_cloud_id</code> <code>str</code> <p>The unique identifier of the point cloud.</p> required <p>Returns:</p> Type Description <code>PointCloud</code> <p>The requested PointCloud object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no point cloud exists with the given IDs, or the user does not have access.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports","title":"fastfuels_sdk.v2.exports","text":"<p>fastfuels_sdk/v2/exports.py</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export","title":"Export","text":"<p>               Bases: <code>Export</code></p> <p>Export resource for the FastFuels v2 API.</p> <p>An export packages a resource's data \u2014 a grid, an inventory, or a multi-grid QUIC-Fire bundle \u2014 into a downloadable file. Exports are asynchronous job resources: creation returns a pending record, and the signed download URL is populated when the job completes. Call :meth:<code>wait</code> and then :meth:<code>to_file</code> to download.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Unique identifier for the export.</p> <code>domain_id</code> <code>str</code> <p>Identifier of the domain the exported resource belongs to.</p> <code>status</code> <code>JobStatus</code> <p>Job status: \"pending\", \"running\", \"completed\", or \"failed\".</p> <code>source</code> <code>ExportSource</code> <p>What was exported and in which format.</p> <code>name</code> <code>str</code> <p>Human-readable name for the export.</p> <code>description</code> <code>str</code> <p>Detailed description of the export.</p> <code>progress</code> <code>(JobProgress, optional)</code> <p>Progress info while the job is running.</p> <code>signed_url</code> <code>(str, optional)</code> <p>Download URL; populated when the job completes.</p> <code>expires_on</code> <code>(datetime, optional)</code> <p>When the signed URL stops working.</p> <code>error</code> <code>(JobError, optional)</code> <p>Error details if the job failed.</p> <code>tags</code> <code>(List[str], optional)</code> <p>User-defined tags for organization.</p> <code>created_on</code> <code>datetime</code> <p>When the export was created.</p> <code>modified_on</code> <code>datetime</code> <p>When the export was last modified.</p> <p>Examples:</p> <p>Export a grid and download the result:</p> <pre><code>&gt;&gt;&gt; export = grid.export(format=\"geotiff\")\n&gt;&gt;&gt; export.wait().to_file(\"elevation.tif\")\n</code></pre> <p>Get an export by ID:</p> <pre><code>&gt;&gt;&gt; export = ff.get_export(\"abc123\")\n</code></pre> See Also <p>create_landscape_export : Assemble an 8-band fire-behavior landscape. create_quicfire_export : Bundle fuel grids into a QUIC-Fire archive. list_exports : List your exports.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export.from_id","title":"from_id  <code>classmethod</code>","text":"<pre><code>from_id(export_id: str) -&gt; Export\n</code></pre> <p>Retrieve an existing Export resource by its ID.</p> <p>Exports are addressed by their ID alone (no domain in the path).</p> <p>Parameters:</p> Name Type Description Default <code>export_id</code> <code>str</code> <p>The unique identifier of the export to retrieve.</p> required <p>Returns:</p> Type Description <code>Export</code> <p>The requested Export object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no export exists with the given ID, or the user does not have access to it.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export.refresh","title":"refresh","text":"<pre><code>refresh() -&gt; Export\n</code></pre> <p>Update this Export in place with the latest data from the API.</p> <p>Returns:</p> Type Description <code>Export</code> <p><code>self</code>, updated with the latest data (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the export no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export.wait","title":"wait","text":"<pre><code>wait(timeout: Optional[float] = None, verbose: bool = False) -&gt; Export\n</code></pre> <p>Poll the export job until it reaches a terminal status.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Maximum seconds to wait. <code>None</code> (default) waits indefinitely; the job runs server-side regardless, so a bounded wait is resumable.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If True, print the job status at each poll.</p> <code>False</code> <p>Returns:</p> Type Description <code>Export</code> <p><code>self</code>, updated to its terminal state (so calls chain).</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If <code>timeout</code> is set and elapses before a terminal status.</p> <code>JobFailedError</code> <p>If the job finished with status \"failed\".</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export.update","title":"update","text":"<pre><code>update(name: Optional[str] = None, description: Optional[str] = None, tags: Optional[List[str]] = None) -&gt; Export\n</code></pre> <p>Update the export's mutable metadata (name, description, tags) in place.</p> <p>Only provided fields are sent. If no fields are provided, no API call is made.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>New name for the export.</p> <code>None</code> <code>description</code> <code>str</code> <p>New description for the export.</p> <code>None</code> <code>tags</code> <code>List[str]</code> <p>New tags for the export (replaces existing tags).</p> <code>None</code> <p>Returns:</p> Type Description <code>Export</code> <p><code>self</code>, updated (so calls chain).</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the export no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export.delete","title":"delete","text":"<pre><code>delete() -&gt; None\n</code></pre> <p>Delete this export and its packaged file.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If the export no longer exists.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export.to_file","title":"to_file","text":"<pre><code>to_file(path: Union[str, Path]) -&gt; Path\n</code></pre> <p>Download the export's packaged file.</p> <p>Streams the signed URL to disk. The export must be completed \u2014 call :meth:<code>wait</code> first.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str or Path</code> <p>Destination file path. If <code>path</code> is an existing directory, the file is saved inside it under the export's default filename.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The path of the written file.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the export is not completed or carries no download URL.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; export = grid.export(format=\"geotiff\")\n&gt;&gt;&gt; export.wait().to_file(\"elevation.tif\")\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.exports.Export.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; str\n</code></pre> <p>Serialize the complete Export object to a JSON string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The Export as a pretty-printed JSON string.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.create_landscape_export","title":"create_landscape_export","text":"<pre><code>create_landscape_export(domain, *, fire_behavior_fuel_model: str, elevation: Tuple, slope: Tuple, aspect: Tuple, fuel_model: Tuple, canopy_cover: Tuple, canopy_height: Tuple, canopy_base_height: Tuple, canopy_bulk_density: Tuple, resolution_m: Optional[float] = None, align_to=None, expiration_days: int = 7, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Export\n</code></pre> <p>Assemble terrain, fuel-model, and canopy grids into a landscape export.</p> <p>Produces an 8-band LANDFIRE-style GeoTIFF for FlamMap, IFTDSS, and WFDSS. Every role is a <code>(grid, band)</code> pair. Role grids must already share the selected 2D lattice and cover its full extent; the exporter never resamples or reprojects them.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) the source grids belong to.</p> required <code>fire_behavior_fuel_model</code> <code>(fbfm40, fbfm13)</code> <p>How codes in <code>fuel_model</code> should be interpreted.</p> <code>\"fbfm40\"</code> <code>elevation</code> <code>tuple</code> <p><code>(grid, band)</code> roles for terrain elevation (m), slope (degrees), and aspect (degrees).</p> required <code>slope</code> <code>tuple</code> <p><code>(grid, band)</code> roles for terrain elevation (m), slope (degrees), and aspect (degrees).</p> required <code>aspect</code> <code>tuple</code> <p><code>(grid, band)</code> roles for terrain elevation (m), slope (degrees), and aspect (degrees).</p> required <code>fuel_model</code> <code>tuple</code> <p><code>(grid, band)</code> for categorical FBFM40 or FBFM13 codes.</p> required <code>canopy_cover</code> <code>tuple</code> <p><code>(grid, band)</code> for canopy cover (%).</p> required <code>canopy_height</code> <code>tuple</code> <p><code>(grid, band)</code> for canopy height (m).</p> required <code>canopy_base_height</code> <code>tuple</code> <p><code>(grid, band)</code> for canopy base height (m).</p> required <code>canopy_bulk_density</code> <code>tuple</code> <p><code>(grid, band)</code> for canopy bulk density (kg/m\u00b3).</p> required <code>resolution_m</code> <code>float</code> <p>Domain-anchored landscape cell size. Defaults to 30 m. Mutually exclusive with <code>align_to</code>.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Match an existing grid's CRS, transform, and shape. Mutually exclusive with <code>resolution_m</code>.</p> <code>None</code> <code>expiration_days</code> <code>int</code> <p>Days until the signed download URL expires (1-7, default 7).</p> <code>7</code> <code>name</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the export.</p> <code>None</code> <p>Returns:</p> Type Description <code>Export</code> <p>The new pending Export. Call :meth:<code>Export.wait</code> and then :meth:<code>Export.to_file</code> to download <code>landscape.tif</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>align_to</code> and <code>resolution_m</code> are combined, a role is not a field-source pair, or the fuel-model declaration is invalid.</p> <code>UnprocessableEntityException</code> <p>If a source band has the wrong unit or dimensionality, or a role grid is not aligned with or does not cover the landscape lattice.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; export = ff.exports.create_landscape_export(\n...     domain,\n...     fire_behavior_fuel_model=\"fbfm40\",\n...     elevation=(topography, \"elevation\"),\n...     slope=(topography, \"slope\"),\n...     aspect=(topography, \"aspect\"),\n...     fuel_model=(fuel_models, \"fbfm\"),\n...     canopy_cover=(canopy, \"cc\"),\n...     canopy_height=(canopy, \"chm\"),\n...     canopy_base_height=(canopy, \"cbh\"),\n...     canopy_bulk_density=(canopy, \"cbd\"),\n... )\n&gt;&gt;&gt; export.wait().to_file(\"landscape.tif\")\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.exports.create_quicfire_export","title":"create_quicfire_export","text":"<pre><code>create_quicfire_export(domain, canopy_bulk_density: Tuple, canopy_moisture: Tuple, surface_fuel_load: Tuple, surface_fuel_depth: Tuple, surface_moisture: Tuple, topography: Optional[Tuple] = None, canopy_savr: Optional[Tuple] = None, surface_savr: Optional[Tuple] = None, horizontal_resolution_m: Optional[float] = None, vertical_resolution_m: Optional[float] = None, align_to=None, moist_merge: Optional[str] = None, expiration_days: int = 7, name: str = '', description: str = '', tags: Optional[List[str]] = None) -&gt; Export\n</code></pre> <p>Bundle fuel and topography grids into a QUIC-Fire export.</p> <p>Packages the grids into a QUIC-Fire-loadable zip archive containing <code>treesrhof.dat</code>, <code>treesmoist.dat</code>, <code>treesfueldepth.dat</code>, <code>metadata.json</code>, and <code>domain.geojson</code> \u2014 plus <code>topo.dat</code> when <code>topography</code> is given, and <code>treesss.dat</code> when both SAVR roles are given.</p> <p>Each grid role is a <code>(grid, band)</code> pair naming the grid (or its id) and the band to read. Every role grid must be lattice-aligned with the fire grid and cover its full extent; the exporter crops oversized grids but never resamples.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>The domain (or its id) the grids belong to.</p> required <code>canopy_bulk_density</code> <code>tuple</code> <p><code>(grid, band)</code> for 3D canopy bulk density (kg/m\u00b3).</p> required <code>canopy_moisture</code> <code>tuple</code> <p><code>(grid, band)</code> for 3D canopy fuel moisture.</p> required <code>surface_fuel_load</code> <code>tuple</code> <p><code>(grid, band)</code> for 2D surface fuel load (kg/m\u00b2).</p> required <code>surface_fuel_depth</code> <code>tuple</code> <p><code>(grid, band)</code> for 2D surface fuel depth (m).</p> required <code>surface_moisture</code> <code>tuple</code> <p><code>(grid, band)</code> for 2D surface fuel moisture.</p> required <code>topography</code> <code>tuple</code> <p><code>(grid, band)</code> for elevation; produces <code>topo.dat</code>.</p> <code>None</code> <code>canopy_savr</code> <code>tuple</code> <p><code>(grid, band)</code> for surface-area-to-volume ratio. Give both or neither; together they produce <code>treesss.dat</code>.</p> <code>None</code> <code>surface_savr</code> <code>tuple</code> <p><code>(grid, band)</code> for surface-area-to-volume ratio. Give both or neither; together they produce <code>treesss.dat</code>.</p> <code>None</code> <code>horizontal_resolution_m</code> <code>float</code> <p>Fire-grid cell size (x and y) in meters when anchoring to the domain bounding box (API default 2.0). Mutually exclusive with <code>align_to</code>.</p> <code>None</code> <code>vertical_resolution_m</code> <code>float</code> <p>Fire-grid vertical cell size in meters (API default 1.0). Mutually exclusive with <code>align_to</code>.</p> <code>None</code> <code>align_to</code> <code>Grid or str</code> <p>Define the fire grid by an existing grid's lattice instead.</p> <code>None</code> <code>moist_merge</code> <code>str</code> <p>How surface and canopy moisture merge in shared cells: \"max\" (API default) or \"weighted_avg\".</p> <code>None</code> <code>expiration_days</code> <code>int</code> <p>Days until the signed download URL expires (max 7, default 7).</p> <code>7</code> <code>name</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>description</code> <code>str</code> <p>Metadata for the export.</p> <code>''</code> <code>tags</code> <code>List[str]</code> <p>Tags for the export.</p> <code>None</code> <p>Returns:</p> Type Description <code>Export</code> <p>The created Export object (job status \"pending\"). Call :meth:<code>Export.wait</code> and then :meth:<code>Export.to_file</code> to download the archive.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>align_to</code> is combined with a resolution argument.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; export = ff.exports.create_quicfire_export(\n...     domain,\n...     canopy_bulk_density=(voxels, \"bulk_density.foliage.live\"),\n...     canopy_moisture=(canopy_moist, \"fuel_moisture\"),\n...     surface_fuel_load=(surface, \"fuel_load.1hr\"),\n...     surface_fuel_depth=(surface, \"fuel_depth\"),\n...     surface_moisture=(surface_moist, \"fuel_moisture\"),\n... )\n&gt;&gt;&gt; export.wait().to_file(\"quicfire.zip\")\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.exports.list_exports","title":"list_exports","text":"<pre><code>list_exports(domain=None, page: int = 0, size: int = 100, sort_by: Optional[str] = None, sort_order: Optional[str] = None, source: Optional[str] = None, tag: Optional[str] = None) -&gt; List[Export]\n</code></pre> <p>List your exports (single page), most filters optional.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>Domain or str</code> <p>Only return exports of resources in this domain (or its id). If omitted, exports from all your domains are listed.</p> <code>None</code> <code>page</code> <code>int</code> <p>The page number to retrieve, zero-indexed (default 0).</p> <code>0</code> <code>size</code> <code>int</code> <p>The number of exports per page (default 100).</p> <code>100</code> <code>sort_by</code> <code>str</code> <p>Field to sort by: \"name\", \"created_on\", or \"modified_on\".</p> <code>None</code> <code>sort_order</code> <code>str</code> <p>Sort direction: \"ascending\" or \"descending\".</p> <code>None</code> <code>source</code> <code>str</code> <p>Only return exports with this source name \u2014 the format for single-resource exports (e.g. \"geotiff\", \"csv\"), \"landscape\" for landscape GeoTIFFs, or \"quicfire\" for bundles.</p> <code>None</code> <code>tag</code> <code>str</code> <p>Only return exports carrying this tag.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Export]</code> <p>The requested page of Export objects.</p>"},{"location":"reference/#fastfuels_sdk.v2.exports.get_export","title":"get_export","text":"<pre><code>get_export(export_id: str) -&gt; Export\n</code></pre> <p>Retrieve a single export by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>export_id</code> <code>str</code> <p>The unique identifier of the export.</p> required <p>Returns:</p> Type Description <code>Export</code> <p>The requested Export object.</p> <p>Raises:</p> Type Description <code>NotFoundException</code> <p>If no export exists with the given ID, or the user does not have access.</p>"},{"location":"reference/#fastfuels_sdk.v2.modifications","title":"fastfuels_sdk.v2.modifications","text":"<p>fastfuels_sdk/v2/modifications.py</p> <p>Modification primitives for grids and tree inventories.</p> <p>A modification is a rule applied to a resource: it pairs conditions (spatial or value tests, all ANDed) with actions applied to whatever the conditions select. <code>mask</code> builds a grid modification (the v2 replacement for v1's <code>feature_masks</code>); <code>tree_attribute</code> / <code>tree_within</code> / <code>remove_trees</code> / <code>modify_trees</code> build inventory modifications so callers don't hand-assemble the generated models.</p>"},{"location":"reference/#fastfuels_sdk.v2.modifications.mask","title":"mask","text":"<pre><code>mask(feature, band: Union[str, List[str]], value: Union[float, int, str] = 0.0, *, operator: str = 'within', buffer_m: Optional[float] = None, target: Optional[str] = None) -&gt; GridModification\n</code></pre> <p>Build a modification that overwrites a band where cells fall within a feature.</p> <p>Pass a completed feature (road, water, or layerset) in the same domain as the grid, and every grid cell selected by the spatial <code>operator</code> has <code>band</code> replaced with <code>value</code>. The returned modification is suitable for the <code>modifications=</code> argument of any grid creator.</p> <p>Parameters:</p> Name Type Description Default <code>feature</code> <code>Feature or str</code> <p>The feature to mask against, or its id. Must be <code>completed</code> and belong to the same domain as the grid being modified.</p> required <code>band</code> <code>str or list of str</code> <p>The band(s) to overwrite, using dot-notation keys (e.g. <code>\"fbfm\"</code>, <code>\"fuel_load.1hr\"</code>). A list applies the same value to every band.</p> required <code>value</code> <code>float, int, or str</code> <p>The replacement value written to <code>band</code> for matching cells.</p> <code>0.0</code> <code>operator</code> <code>(within, intersects, outside)</code> <p>Which cells to select relative to the feature geometry.</p> <code>\"within\"</code> <code>buffer_m</code> <code>float</code> <p>Buffer applied to the feature geometry, in meters in the domain's projected CRS, before testing. Linestring features such as roads usually need a buffer \u2014 or <code>target=\"cell\"</code> \u2014 to catch the cells they cross. Defaults to no buffer.</p> <code>None</code> <code>target</code> <code>(centroid, cell)</code> <p>Which part of each grid cell is tested against the geometry. Defaults to the API default (<code>\"centroid\"</code>). Use <code>\"cell\"</code> to select every cell a geometry touches.</p> <code>\"centroid\"</code> <p>Returns:</p> Type Description <code>GridModification</code> <p>A modification to pass in a creator's <code>modifications=</code> list.</p> <p>Examples:</p> <p>Mask roads to a non-burnable fuel model code, buffering the linestrings so they cover whole cells:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; roads = ff.features.create_road_feature_from_osm(domain)\n&gt;&gt;&gt; roads.wait()\n&gt;&gt;&gt; grid = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n...     domain,\n...     output_resolution_m=30,\n...     modifications=[ff.mask(roads, \"fbfm\", 91, buffer_m=5)],\n... )\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.modifications.tree_attribute","title":"tree_attribute","text":"<pre><code>tree_attribute(attribute, operator, value, *, unit: Optional[str] = None)\n</code></pre> <p>Build a condition testing a per-tree attribute.</p> <p>Parameters:</p> Name Type Description Default <code>attribute</code> <code>str</code> <p>The tree attribute to test: \"dbh\", \"height\", \"crown_ratio\", or \"fia_species_code\" (an <code>InventoryAttribute</code> member is also accepted).</p> required <code>operator</code> <code>str</code> <p>A comparison: <code>\"&lt;\"</code>, <code>\"&lt;=\"</code>, <code>\"&gt;\"</code>, <code>\"&gt;=\"</code>, <code>\"==\"</code>, <code>\"!=\"</code> (or the names <code>\"lt\"</code>, <code>\"le\"</code>, <code>\"gt\"</code>, <code>\"ge\"</code>, <code>\"eq\"</code>, <code>\"ne\"</code>; an <code>Operator</code> member is also accepted).</p> required <code>value</code> <code>float, int, str, or list</code> <p>The value to compare against.</p> required <code>unit</code> <code>str</code> <p>Unit of <code>value</code> if not the attribute's default.</p> <code>None</code> <p>Returns:</p> Type Description <code>InventoryModificationCondition</code> <p>A condition for <code>remove_trees</code> / <code>modify_trees</code>.</p>"},{"location":"reference/#fastfuels_sdk.v2.modifications.tree_within","title":"tree_within","text":"<pre><code>tree_within(feature, *, buffer_m: Optional[float] = None, operator: str = 'within')\n</code></pre> <p>Build a condition selecting trees by their location relative to a feature.</p> <p>Parameters:</p> Name Type Description Default <code>feature</code> <code>Feature or str</code> <p>The feature to test against, or its id. Must be <code>completed</code> and in the same domain as the inventory.</p> required <code>buffer_m</code> <code>float</code> <p>Buffer applied to the feature geometry, in meters in the domain's projected CRS, before testing.</p> <code>None</code> <code>operator</code> <code>(within, intersects, outside)</code> <p>Which trees to select relative to the feature geometry.</p> <code>\"within\"</code> <p>Returns:</p> Type Description <code>InventoryFeatureSpatialCondition</code> <p>A condition for <code>remove_trees</code> / <code>modify_trees</code>.</p>"},{"location":"reference/#fastfuels_sdk.v2.modifications.remove_trees","title":"remove_trees","text":"<pre><code>remove_trees(*conditions) -&gt; InventoryModification\n</code></pre> <p>Build a modification that removes the trees matching every condition.</p> <p>Parameters:</p> Name Type Description Default <code>*conditions</code> <p>One or more conditions from :func:<code>tree_attribute</code> / :func:<code>tree_within</code> (ANDed). At least one is required.</p> <code>()</code> <p>Returns:</p> Type Description <code>InventoryModification</code> <p>For a creator's <code>modifications=</code> list or :meth:<code>Inventory.apply_modifications</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; inventory.apply_modifications([ff.remove_trees(ff.tree_attribute(\"dbh\", \"&lt;\", 10))])\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.modifications.modify_trees","title":"modify_trees","text":"<pre><code>modify_trees(attribute, modifier, value, *conditions, unit: Optional[str] = None) -&gt; InventoryModification\n</code></pre> <p>Build a modification that changes an attribute on the matching trees.</p> <p>Parameters:</p> Name Type Description Default <code>attribute</code> <code>str</code> <p>The attribute to change (\"dbh\", \"height\", \"crown_ratio\", \"fia_species_code\"; an <code>InventoryAttribute</code> member is also accepted).</p> required <code>modifier</code> <code>str</code> <p>How to change it: \"replace\", \"add\", \"subtract\", \"multiply\", or \"divide\" (a <code>Modifier</code> member is also accepted). To drop trees, use :func:<code>remove_trees</code> instead.</p> required <code>value</code> <code>float, int, or str</code> <p>The operand for <code>modifier</code>.</p> required <code>*conditions</code> <p>One or more conditions from :func:<code>tree_attribute</code> / :func:<code>tree_within</code> (ANDed). At least one is required.</p> <code>()</code> <code>unit</code> <code>str</code> <p>Unit of <code>value</code> if not the attribute's default.</p> <code>None</code> <p>Returns:</p> Type Description <code>InventoryModification</code> <p>For a creator's <code>modifications=</code> list or :meth:<code>Inventory.apply_modifications</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; inventory.apply_modifications([\n...     ff.modify_trees(\"height\", \"multiply\", 0.9, ff.tree_attribute(\"dbh\", \"&gt;\", 0))\n... ])\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.treatments","title":"fastfuels_sdk.v2.treatments","text":"<p>fastfuels_sdk/v2/treatments.py</p> <p>Treatment primitives for tree inventories.</p> <p>A treatment thins a tree inventory by removing stems until the stand reaches a target \u2014 either a residual basal area or a diameter limit. Pass treatments to a tree-inventory creator's <code>treatments=</code> argument, or to :meth:<code>fastfuels_sdk.v2.inventories.Inventory.apply_treatments</code> on an inventory you already hold. These builders construct the generated treatment models so callers don't have to.</p> <p>The optional <code>conditions</code> argument restricts a treatment to a subarea (<code>InventoryFeatureSpatialCondition</code> / <code>InventoryGeometrySpatialCondition</code>); it is passed through as-is \u2014 there is no condition-builder vocabulary yet.</p>"},{"location":"reference/#fastfuels_sdk.v2.treatments.basal_area_treatment","title":"basal_area_treatment","text":"<pre><code>basal_area_treatment(method: str, value: float, *, unit: Optional[str] = None, conditions: Optional[list] = None) -&gt; InventoryBasalAreaTreatment\n</code></pre> <p>Build a treatment that thins an inventory to a residual basal area.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>(from_below, from_above, proportional)</code> <p>How stems are removed: \"from_below\" removes the smallest trees first, \"from_above\" the largest first, \"proportional\" removes across all size classes. An <code>InventoryTreatmentMethod</code> member is also accepted.</p> <code>\"from_below\"</code> <code>value</code> <code>float</code> <p>The residual basal area to thin to (default unit m**2/ha).</p> required <code>unit</code> <code>str</code> <p>Unit of <code>value</code> if not the default.</p> <code>None</code> <code>conditions</code> <code>list</code> <p>Spatial conditions restricting the treatment to a subarea (<code>InventoryFeatureSpatialCondition</code> / <code>InventoryGeometrySpatialCondition</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>InventoryBasalAreaTreatment</code> <p>A treatment for a creator's <code>treatments=</code> list or :meth:<code>Inventory.apply_treatments</code>.</p> <p>Examples:</p> <p>Thin from below to a residual basal area of 25 m**2/ha:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; inventory.apply_treatments([ff.basal_area_treatment(\"from_below\", 25.0)])\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.treatments.diameter_treatment","title":"diameter_treatment","text":"<pre><code>diameter_treatment(method: str, value: float, *, unit: Optional[str] = None, conditions: Optional[list] = None) -&gt; InventoryDiameterTreatment\n</code></pre> <p>Build a treatment that thins an inventory to a diameter limit.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>(from_below, from_above)</code> <p>\"from_below\" removes trees smaller than <code>value</code> (clears suppressed understory stems); \"from_above\" removes those larger than <code>value</code>. An <code>InventoryDiameterTreatmentMethod</code> member is also accepted.</p> <code>\"from_below\"</code> <code>value</code> <code>float</code> <p>The diameter limit (default unit cm dbh).</p> required <code>unit</code> <code>str</code> <p>Unit of <code>value</code> if not the default.</p> <code>None</code> <code>conditions</code> <code>list</code> <p>Spatial conditions restricting the treatment to a subarea (<code>InventoryFeatureSpatialCondition</code> / <code>InventoryGeometrySpatialCondition</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>InventoryDiameterTreatment</code> <p>A treatment for a creator's <code>treatments=</code> list or :meth:<code>Inventory.apply_treatments</code>.</p> <p>Examples:</p> <p>Remove trees smaller than 10 cm dbh:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; inventory.apply_treatments([ff.diameter_treatment(\"from_below\", 10.0)])\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.calibrations","title":"fastfuels_sdk.v2.calibrations","text":"<p>Calibration builders for generated v2 request models.</p>"},{"location":"reference/#fastfuels_sdk.v2.calibrations.duet_calibration","title":"duet_calibration","text":"<pre><code>duet_calibration(*, fuel_load=None, fuel_depth=None, fuel_moisture=None) -&gt; DuetCalibration\n</code></pre> <p>Build calibration targets for a DUET surface-fuel grid.</p> <p>Each argument maps fuel types (<code>\"grass\"</code>, <code>\"coniferous\"</code>, <code>\"deciduous\"</code>, <code>\"litter\"</code>, or <code>\"all\"</code>) to a target. Target fields select the calibration method: <code>value</code> for constant, <code>max</code> and optional <code>min</code> for max-min, or <code>mean</code> and <code>sd</code> for mean-standard deviation. An explicit <code>method</code> field is also accepted.</p> <p>Parameters:</p> Name Type Description Default <code>fuel_load</code> <code>mapping</code> <p>Per-fuel-type load targets.</p> <code>None</code> <code>fuel_depth</code> <code>mapping</code> <p>Per-fuel-type depth targets.</p> <code>None</code> <code>fuel_moisture</code> <code>mapping</code> <p>Per-fuel-type moisture targets.</p> <code>None</code> <p>Returns:</p> Type Description <code>DuetCalibration</code> <p>Calibration for :func:<code>fastfuels_sdk.v2.grids.create_surface_fuel_grid_from_duet</code>.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If a parameter or target is not a mapping or generated target model.</p> <code>ValueError</code> <p>If no parameter is provided, a target is invalid, or mutually exclusive fuel types are combined.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import fastfuels_sdk.v2 as ff\n&gt;&gt;&gt; calibration = ff.duet_calibration(\n...     fuel_load={\n...         \"grass\": {\"mean\": 0.5, \"sd\": 0.25},\n...         \"litter\": {\"max\": 5.0, \"min\": 0.0},\n...     },\n...     fuel_depth={\n...         \"grass\": {\"value\": 0.3},\n...         \"litter\": {\"value\": 0.06},\n...     },\n... )\n</code></pre>"},{"location":"reference/#fastfuels_sdk.v2.compose","title":"fastfuels_sdk.v2.compose","text":"<p>Builders for grid-compose operations.</p>"},{"location":"reference/#fastfuels_sdk.v2.compose.select","title":"select","text":"<pre><code>select(output: str, from_: str, *, conditions: Iterable | None = None, else_=None, name: str | None = None, description: str | None = None) -&gt; ComposeSelect\n</code></pre> <p>Build an operation that copies an input band into the output grid.</p> <p>Parameters:</p> Name Type Description Default <code>output</code> <code>str</code> <p>Key for the output band.</p> required <code>from_</code> <code>str</code> <p>Alias-qualified source band, such as <code>\"base.fbfm\"</code>.</p> required <code>conditions</code> <code>iterable</code> <p>Attribute or spatial conditions, all of which must match. Attribute conditions can be built with :func:<code>condition</code>.</p> <code>None</code> <code>else_</code> <code>optional</code> <p>Fallback band reference, number, typed :func:<code>literal</code>, fuel-model label, or :func:<code>inline_compute</code>. Required when conditions are given.</p> <code>None</code> <code>name</code> <code>str</code> <p>Display metadata for the output band.</p> <code>None</code> <code>description</code> <code>str</code> <p>Display metadata for the output band.</p> <code>None</code> <p>Returns:</p> Type Description <code>ComposeSelect</code> <p>A select operation for :func:<code>fastfuels_sdk.v2.grids.create_grid_from_compose</code>.</p>"},{"location":"reference/#fastfuels_sdk.v2.compose.compute","title":"compute","text":"<pre><code>compute(output: str, operator, operands: Iterable, *, conditions: Iterable | None = None, else_=None, unit: str | None = None, name: str | None = None, description: str | None = None) -&gt; ComposeCompute\n</code></pre> <p>Build an operation that computes an output band.</p> <p>Parameters:</p> Name Type Description Default <code>output</code> <code>str</code> <p>Key for the output band.</p> required <code>operator</code> <code>str or ComposeOperator</code> <p><code>\"add\"</code>, <code>\"subtract\"</code>, <code>\"multiply\"</code>, <code>\"divide\"</code>, <code>\"min\"</code>, <code>\"max\"</code>, or <code>\"average\"</code>.</p> required <code>operands</code> <code>iterable</code> <p>Alias-qualified band references, bare numbers, or typed literals. At least one operand must be a band reference.</p> required <code>conditions</code> <code>iterable</code> <p>Attribute or spatial conditions, all of which must match.</p> <code>None</code> <code>else_</code> <code>optional</code> <p>Fallback value. Required when conditions are given.</p> <code>None</code> <code>unit</code> <code>str</code> <p>Canonical unit in which to express the computed output.</p> <code>None</code> <code>name</code> <code>str</code> <p>Display metadata for the output band.</p> <code>None</code> <code>description</code> <code>str</code> <p>Display metadata for the output band.</p> <code>None</code> <p>Returns:</p> Type Description <code>ComposeCompute</code> <p>A compute operation for :func:<code>fastfuels_sdk.v2.grids.create_grid_from_compose</code>.</p>"},{"location":"reference/#fastfuels_sdk.v2.compose.condition","title":"condition","text":"<pre><code>condition(band: str, operator, value) -&gt; ComposeAttributeCondition\n</code></pre> <p>Build an attribute condition for a compose operation.</p> <p>Parameters:</p> Name Type Description Default <code>band</code> <code>str</code> <p>Alias-qualified input band to test.</p> required <code>operator</code> <code>str or ComposeComparisonOperator</code> <p><code>\"eq\"</code>, <code>\"ne\"</code>, <code>\"gt\"</code>, <code>\"lt\"</code>, <code>\"ge\"</code>, <code>\"le\"</code>, or <code>\"in\"</code>.</p> required <code>value</code> <code>int, float, str, or list</code> <p>Comparison value. <code>\"in\"</code> requires a list; ordering comparisons require a scalar.</p> required <p>Returns:</p> Type Description <code>ComposeAttributeCondition</code> <p>A condition for :func:<code>select</code> or :func:<code>compute</code>.</p>"},{"location":"reference/#fastfuels_sdk.v2.compose.literal","title":"literal","text":"<pre><code>literal(value, unit: str | None = None) -&gt; ComposeLiteral\n</code></pre> <p>Build a typed literal for a compose operand or fallback.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>int, float, or str</code> <p>Literal value.</p> required <code>unit</code> <code>str</code> <p>Canonical unit for a numeric literal. String literals are unitless.</p> <code>None</code> <p>Returns:</p> Type Description <code>ComposeLiteral</code> <p>A typed compose literal.</p>"},{"location":"reference/#fastfuels_sdk.v2.compose.inline_compute","title":"inline_compute","text":"<pre><code>inline_compute(operator, operands: Iterable) -&gt; InlineCompute\n</code></pre> <p>Build a computation for the fallback branch of a compose operation.</p> <p>Parameters:</p> Name Type Description Default <code>operator</code> <code>str or ComposeOperator</code> <p>Arithmetic operator.</p> required <code>operands</code> <code>iterable</code> <p>Alias-qualified band references, bare numbers, or typed literals.</p> required <p>Returns:</p> Type Description <code>InlineCompute</code> <p>A computed fallback for <code>else_=</code>.</p>"},{"location":"reference/#fastfuels_sdk.v2._jobs","title":"fastfuels_sdk.v2._jobs","text":"<p>fastfuels_sdk/v2/_jobs.py</p> <p>Shared polling helpers for job-based v2 resources (Feature, Grid, Inventory). Private module: each resource exposes its own documented <code>wait</code> method that delegates to :func:<code>wait</code> here. :func:<code>wait_all</code> and :class:<code>JobFailedError</code> are re-exported at the package top level.</p>"},{"location":"reference/#fastfuels_sdk.v2._jobs.JobFailedError","title":"JobFailedError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a job-based resource reaches the <code>failed</code> status.</p> <p>Attributes:</p> Name Type Description <code>code</code> <code>str or None</code> <p>Machine-readable error code reported by the API (<code>None</code> when the API returned no error detail).</p> <code>message</code> <code>str or None</code> <p>Human-readable error message reported by the API.</p> <code>suggestion</code> <code>str or None</code> <p>Actionable advice reported by the API, when present.</p>"},{"location":"reference/#fastfuels_sdk.v2._jobs.wait","title":"wait","text":"<pre><code>wait(resource, timeout=None, verbose=False)\n</code></pre> <p>Poll a job resource until it reaches a terminal status.</p> <p>Duck-typed on the resource wrapper: requires <code>refresh()</code>, <code>status</code>, <code>error</code>, and <code>id</code>. Updates the resource in place on every poll and returns it, so calls chain.</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <p>A job-based resource wrapper (Feature, Grid, ...).</p> required <code>timeout</code> <code>float</code> <p>Maximum seconds to wait. <code>None</code> (default) waits indefinitely; the job runs server-side regardless, so a bounded wait is resumable.</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>Print status updates while polling.</p> <code>False</code> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If <code>timeout</code> is set and elapsed before a terminal status.</p> <code>JobFailedError</code> <p>If the job reaches the <code>failed</code> status.</p>"},{"location":"reference/#fastfuels_sdk.v2._jobs.wait_all","title":"wait_all","text":"<pre><code>wait_all(resources, timeout=None, verbose=False)\n</code></pre> <p>Wait for several job resources to reach a terminal status.</p> <p>Jobs run server-side in parallel, so creating resources without waiting and then joining them here runs the work concurrently. Resources are joined in iteration order; the first one to be found <code>failed</code> raises its :class:<code>JobFailedError</code>.</p> <p>Parameters:</p> Name Type Description Default <code>resources</code> <code>iterable</code> <p>Job-based resource wrappers to wait on.</p> required <code>timeout</code> <code>float</code> <p>Per-resource timeout in seconds (<code>None</code> waits indefinitely).</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>Print status updates while polling.</p> <code>False</code> <p>Returns:</p> Type Description <code>list</code> <p>The resources, each updated in place to its terminal state.</p>"},{"location":"reference/#fastfuels_sdk.v2.api","title":"fastfuels_sdk.v2.api","text":"<p>fastfuels_sdk/v2/api.py</p> <p>Client configuration for the FastFuels v2 API: API key management and the shared HTTP client used by the wrapper modules.</p>"},{"location":"reference/#fastfuels_sdk.v2.api.set_api_key","title":"set_api_key","text":"<pre><code>set_api_key(api_key: str) -&gt; None\n</code></pre> <p>Set the API key for the FastFuels v2 SDK.</p> <p>This invalidates the cached client, ensuring that subsequent API calls use the new credentials.</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str</code> <p>The API key to use for authentication.</p> required"},{"location":"reference/#fastfuels_sdk.v2.api.get_client","title":"get_client","text":"<pre><code>get_client() -&gt; Optional[AuthenticatedClient]\n</code></pre> <p>Get the current API client, creating one if necessary.</p> <p>The API key is read from:</p> <ol> <li>The cached client, if :func:<code>set_api_key</code> was called</li> <li>The <code>FASTFUELS_API_KEY</code> environment variable</li> </ol> <p>Returns:</p> Type Description <code>Optional[AuthenticatedClient]</code> <p>The client instance, or None if no API key is configured.</p>"},{"location":"reference/#fastfuels_sdk.v2.api.ensure_client","title":"ensure_client","text":"<pre><code>ensure_client() -&gt; AuthenticatedClient\n</code></pre> <p>Ensure an API client is configured and return it.</p> <p>Returns:</p> Type Description <code>AuthenticatedClient</code> <p>The client instance.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If no API key is configured.</p>"},{"location":"reference/#fastfuels_sdk.v2.api.get_quotas","title":"get_quotas","text":"<pre><code>get_quotas() -&gt; Quotas\n</code></pre> <p>Return the authenticated owner's resolved quotas.</p> <p>Returns:</p> Type Description <code>Quotas</code> <p>Count, concurrency, storage, dispatch, and retention limits for the owner authenticated by the current API key.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If no API key is configured.</p> <code>ApiException</code> <p>If the API request fails.</p>"},{"location":"reference/#fastfuels_sdk.v2.api.get_usage","title":"get_usage","text":"<pre><code>get_usage() -&gt; Usage\n</code></pre> <p>Return the authenticated owner's current usage and limits.</p> <p>Returns:</p> Type Description <code>Usage</code> <p>Usage for job resources, count-only resources, storage, and the owner's resource-retention policy.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If no API key is configured.</p> <code>ApiException</code> <p>If the API request fails.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions","title":"fastfuels_sdk.v2.exceptions","text":"<p>fastfuels_sdk/v2/exceptions.py</p> <p>Typed exceptions for the FastFuels v2 SDK.</p> <p>Every API error surfaces as a subclass of :class:<code>ApiException</code> carrying the HTTP status code and the error detail reported by the API.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.ApiException","title":"ApiException","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for FastFuels v2 API errors.</p> <p>Attributes:</p> Name Type Description <code>status_code</code> <code>int</code> <p>The HTTP status code returned by the API.</p> <code>detail</code> <code>Any</code> <p>The error detail extracted from the response body (FastAPI's <code>detail</code> field when present, otherwise the raw response text).</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.BadRequestException","title":"BadRequestException","text":"<p>               Bases: <code>ApiException</code></p> <p>Raised when the API returns 400 Bad Request.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.UnauthorizedException","title":"UnauthorizedException","text":"<p>               Bases: <code>ApiException</code></p> <p>Raised when the API returns 401 Unauthorized (invalid API key).</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.ForbiddenException","title":"ForbiddenException","text":"<p>               Bases: <code>ApiException</code></p> <p>Raised when the API returns 403 Forbidden.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.NotFoundException","title":"NotFoundException","text":"<p>               Bases: <code>ApiException</code></p> <p>Raised when the API returns 404 Not Found.</p> <p>The v2 API returns 404 both for missing resources and for ownership mismatches, to avoid leaking information about resource existence.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.UnprocessableEntityException","title":"UnprocessableEntityException","text":"<p>               Bases: <code>ApiException</code></p> <p>Raised when the API returns 422 Unprocessable Entity.</p> <p>The <code>detail</code> attribute carries FastAPI's validation error detail: either a human-readable message or a list of per-field errors.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.QuotaExceededException","title":"QuotaExceededException","text":"<p>               Bases: <code>ApiException</code></p> <p>Raised when the API returns 429 Too Many Requests.</p> <p>Attributes:</p> Name Type Description <code>quota</code> <code>str or None</code> <p>The quota field that was exceeded.</p> <code>current</code> <code>int or None</code> <p>The owner's current usage for the quota.</p> <code>limit</code> <code>int or None</code> <p>The quota limit that was reached.</p> <code>window_reset_on</code> <code>datetime or None</code> <p>When a windowed quota resets, if applicable.</p> <code>message</code> <code>str or None</code> <p>A human-readable explanation and suggested next steps.</p> <code>reason</code> <code>str or None</code> <p>The machine-readable error reason.</p> <code>retry_after</code> <code>int or None</code> <p>Seconds the API recommends waiting before retrying. Present only for active-job quota rejections.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.ServiceException","title":"ServiceException","text":"<p>               Bases: <code>ApiException</code></p> <p>Raised when the API returns a 5xx server error.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.raise_for_response","title":"raise_for_response","text":"<pre><code>raise_for_response(response: Response) -&gt; NoReturn\n</code></pre> <p>Translate an error <code>Response</code> into a typed SDK exception.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>A response from a generated <code>sync_detailed()</code> endpoint call.</p> required <p>Raises:</p> Type Description <code>BadRequestException</code> <p>If the response status is 400.</p> <code>UnauthorizedException</code> <p>If the response status is 401.</p> <code>ForbiddenException</code> <p>If the response status is 403.</p> <code>NotFoundException</code> <p>If the response status is 404.</p> <code>QuotaExceededException</code> <p>If the response status is 429.</p> <code>UnprocessableEntityException</code> <p>If the response status is 422.</p> <code>ServiceException</code> <p>If the response status is 5xx.</p> <code>ApiException</code> <p>For any other unexpected status.</p>"},{"location":"reference/#fastfuels_sdk.v2.exceptions.expect","title":"expect","text":"<pre><code>expect(response: Response, expected_status: HTTPStatus = HTTPStatus.OK) -&gt; Any\n</code></pre> <p>Return a response's parsed body, raising typed exceptions on error.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>Response</code> <p>A response from a generated <code>sync_detailed()</code> endpoint call.</p> required <code>expected_status</code> <code>HTTPStatus</code> <p>The success status documented for the endpoint (default 200 OK; pass <code>HTTPStatus.NO_CONTENT</code> for deletes).</p> <code>OK</code> <p>Returns:</p> Type Description <code>Any</code> <p><code>response.parsed</code> \u2014 the generated success model (<code>None</code> for 204 No Content).</p> <p>Raises:</p> Type Description <code>ApiException</code> <p>A subclass matching the response status (see :func:<code>raise_for_response</code>) when it differs from <code>expected_status</code>.</p>"},{"location":"guides/composing-grids/","title":"How to Compose Grids","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>Use grid composition to copy and calculate bands from completed grids on the same two-dimensional lattice. For creating the source grids, see Creating grids; for alignment options, see Align grids to each other.</p>"},{"location":"guides/composing-grids/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>One or more completed 2D grids in the same domain, with the same CRS,   transform, and shape</li> <li>For the example below, a completed FBFM40 grid assigned to <code>fbfm40</code></li> </ul>"},{"location":"guides/composing-grids/#select-and-calculate-output-bands","title":"Select and calculate output bands","text":"<p>Give each input grid an alias. Use that alias when referring to its bands in select and compute operations:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\nfuel_grid = ff.grids.create_fuel_grid_from_fbfm40_lookup(\n    fbfm40,\n    bands=[\"fuel_load.1hr\", \"fuel_depth\"],\n)\nfuel_grid.wait()\n\ncomposed = ff.grids.create_grid_from_compose(\n    {\"fuels\": fuel_grid},\n    select=[\n        ff.compose.select(\"fuel_depth\", \"fuels.fuel_depth\"),\n    ],\n    compute=[\n        ff.compose.compute(\n            \"fuel_load.1hr\",\n            \"multiply\",\n            [\"fuels.fuel_load.1hr\", 0.5],\n            conditions=[\n                ff.compose.condition(\"fuels.fuel_load.1hr\", \"gt\", 0),\n            ],\n            else_=ff.compose.literal(0, unit=\"kg/m**2\"),\n        ),\n    ],\n)\ncomposed.wait()\n</code></pre> <p>The alias mapping can contain more than one grid. Every band reference uses the form <code>alias.band_key</code>; output names must be unique across the <code>select</code> and <code>compute</code> lists.</p> <p>The arithmetic operators are <code>add</code>, <code>subtract</code>, <code>multiply</code>, <code>divide</code>, <code>min</code>, <code>max</code>, and <code>average</code>. Conditions are ANDed together and require an <code>else_</code> fallback. A fallback may be another band, a number, a typed literal, a fuel model label, or an <code>ff.compose.inline_compute(...)</code> result.</p> <p>The API derives output units from compute operands. Pass <code>unit=</code> to <code>ff.compose.compute</code> only to request a dimensionally compatible output unit.</p>"},{"location":"guides/creating-grids/","title":"How to Create Grids in FastFuels SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>A grid is a raster data product within a domain \u2014 topography, surface fuel models, canopy fuels, and more \u2014 generated from a data source. This guide shows how to create grids and assemble them into an aligned dataset for fire modeling. For waiting on, inspecting, exporting, and managing grids you already hold, see Working with grids; for what grids are, see the FastFuels documentation. Coming from the v1 SDK? Start with the migration guide.</p> <p>Creation is functional: call a <code>create_&lt;kind&gt;_grid_from_&lt;source&gt;</code> function on a domain. Each returns a <code>Grid</code> whose generation runs as a background job, so it starts <code>\"pending\"</code>.</p>"},{"location":"guides/creating-grids/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>An existing domain \u2014 every creator's first argument is a   <code>Domain</code> (or a bare domain id string)</li> </ul>"},{"location":"guides/creating-grids/#build-an-aligned-grid-set","title":"Build an aligned grid set","text":"<p>The typical goal is several grids covering one domain on the same lattice, so they stack cell-for-cell. Because each creator can resample to the domain lattice, giving them all the same <code>output_resolution_m</code> is enough \u2014 here, a 30 m terrain + surface fuel + canopy set:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\ntopography = ff.grids.create_topography_grid_from_3dep(\n    domain, source_resolution_m=10, output_resolution_m=30,\n    bands=[\"elevation\", \"slope\", \"aspect\"],\n)\nsurface_fuel = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n    domain, output_resolution_m=30,\n)\ncanopy_fuel = ff.grids.create_canopy_fuel_grid_from_landfire(\n    domain, output_resolution_m=30,\n)\n\n# Jobs run server-side in parallel \u2014 create first, then join.\nff.wait_all([topography, surface_fuel, canopy_fuel])\n</code></pre> <p>Each grid reports the bands it carries and, once complete, its georeference:</p> <pre><code>&gt;&gt;&gt; [b.key for b in topography.bands]\n['elevation', 'slope', 'aspect']\n&gt;&gt;&gt; topography.georeference.crs\n'EPSG:32611'\n&gt;&gt;&gt; topography.georeference.shape   # [rows, cols] on the domain lattice\n[47, 58]\n</code></pre> <p>Because all three share <code>output_resolution_m=30</code> on the same domain, they share a CRS, transform, and shape \u2014 they overlay exactly. The sections below cover each source in detail; Working with grids covers inspecting, resampling, exporting, and managing the results.</p>"},{"location":"guides/creating-grids/#topography-grids","title":"Topography grids","text":"<p>From the USGS 3D Elevation Program (3DEP), which carries <code>elevation</code> (m), <code>slope</code> (deg), and <code>aspect</code> (deg):</p> <pre><code>grid = ff.grids.create_topography_grid_from_3dep(\n    domain,\n    source_resolution_m=10,   # 3DEP native resolution: 1, 10, or 30\n    output_resolution_m=30,\n    bands=[\"elevation\", \"slope\", \"aspect\"],\n)\n</code></pre> <p>3DEP is the higher-resolution source where it has coverage. To check before creating the grid:</p> <pre><code>&gt;&gt;&gt; coverage = ff.grids.check_3dep_coverage(domain, resolution_m=10)\n&gt;&gt;&gt; coverage.available\nTrue\n&gt;&gt;&gt; coverage.tile_count\n1\n</code></pre> <p>Where 3DEP lacks coverage, source topography from LANDFIRE (30 m, CONUS) instead:</p> <pre><code>grid = ff.grids.create_topography_grid_from_landfire(domain, output_resolution_m=30)\n</code></pre>"},{"location":"guides/creating-grids/#surface-fuel-model-grids","title":"Surface fuel model grids","text":"<p>LANDFIRE's 40 Scott &amp; Burgan fire behavior fuel models (FBFM40) is the standard surface fuels source. The grid carries a single categorical <code>fbfm</code> band of fuel model codes (e.g. <code>GR1</code>, <code>TL3</code>, <code>SH5</code>):</p> <pre><code>grid = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n    domain, output_resolution_m=30\n)\n</code></pre> <p>To replace non-burnable codes (urban, water, agriculture, \u2026) with no-data, pass them to <code>remove_non_burnable</code>:</p> <pre><code>grid = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n    domain, output_resolution_m=30, remove_non_burnable=[\"NB1\", \"NB2\"]\n)\n</code></pre> <p>To pin an annual LANDFIRE vintage, pass <code>version</code> (e.g. <code>\"2024\"</code>); it defaults to the API's current version. The grid reports the calendar year its fuels represent on <code>grid.represented_year</code>:</p> <pre><code>grid = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n    domain, version=\"2024\", output_resolution_m=30\n)\n</code></pre> <pre><code>&gt;&gt;&gt; grid.represented_year\n2024\n</code></pre>"},{"location":"guides/creating-grids/#seasonal-fuels","title":"Seasonal fuels","text":"<p>LANDFIRE Seasonal Fuels adjusts FBFM40 for a time of year \u2014 early spring (<code>\"ES\"</code>), spring (<code>\"SP\"</code>), summer (<code>\"SU\"</code>), or fall (<code>\"FA\"</code>). Pass <code>season</code> together with a seasonal <code>version</code> (<code>\"2025\"</code>) to fetch it on demand from the LANDFIRE Product Service instead of the staged annual release:</p> <pre><code>grid = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n    domain, version=\"2025\", season=\"SP\", output_resolution_m=30\n)\n</code></pre> <p><code>grid.represented_year</code> is the projected season year \u2014 version <code>\"2025\"</code> plus <code>\"SP\"</code> is spring 2026:</p> <pre><code>&gt;&gt;&gt; grid.represented_year\n2026\n</code></pre> <p>Seasonal coverage is limited</p> <p>Seasonal Fuels covers a changing region rather than all of CONUS. A request for a domain outside the current coverage raises <code>UnprocessableEntityException</code>. See LANDFIRE Seasonal Fuels for current availability.</p>"},{"location":"guides/creating-grids/#look-up-fuel-parameters-from-fbfm40-codes","title":"Look up fuel parameters from FBFM40 codes","text":"<p>The <code>fbfm</code> band holds codes, not the quantities a fire model consumes. To turn the codes into fuel parameters \u2014 loadings by size class, fuel-bed depth, surface-area-to-volume ratios \u2014 pass the completed grid to <code>ff.grids.create_fuel_grid_from_fbfm40_lookup</code>. It returns a new grid whose bands are the requested parameters:</p> <pre><code>grid.wait()\n\nfuels = ff.grids.create_fuel_grid_from_fbfm40_lookup(\n    grid, bands=[\"fuel_load.1hr\", \"fuel_load.10hr\", \"fuel_depth\"]\n)\nfuels.wait()\n</code></pre> <pre><code>&gt;&gt;&gt; [(b.key, b.unit) for b in fuels.bands]\n[('fuel_load.1hr', 'kg/m**2'), ('fuel_load.10hr', 'kg/m**2'), ('fuel_depth', 'm')]\n</code></pre>"},{"location":"guides/creating-grids/#use-anderson-13-fuel-models","title":"Use Anderson 13 fuel models","text":"<p>To create the Anderson 13 model set instead, select a LANDFIRE version and use the FBFM13 creator:</p> <pre><code>grid = ff.grids.create_fuel_model_grid_from_landfire_fbfm13(\n    domain,\n    version=\"2024\",\n    remove_non_burnable=[\"NB1\", \"NB2\"],\n    output_resolution_m=30,\n)\ngrid.wait()\n</code></pre> <p>Its categorical source band is <code>fbfm13</code>. Convert it to any of the nine FBFM13 parameter bands with the matching lookup:</p> <pre><code>fuels = ff.grids.create_fuel_grid_from_fbfm13_lookup(\n    grid,\n    bands=[\"fuel_load.1hr\", \"fuel_load.live_foliage\", \"fuel_depth\"],\n)\nfuels.wait()\n</code></pre>"},{"location":"guides/creating-grids/#from-fccs-instead","title":"From FCCS instead","text":"<p>To use Fuel Characteristic Classification System (FCCS) fuelbeds, create the categorical source grid and wait for it to complete:</p> <pre><code>fccs = ff.grids.create_fuel_model_grid_from_landfire_fccs(\n    domain, remove_bare_ground=True, output_resolution_m=30\n)\nfccs.wait()\n</code></pre> <p>Then look up any of the 12 available FCCS fuel-parameter bands, including duff and live components:</p> <pre><code>fuels = ff.grids.create_fuel_grid_from_fccs_lookup(\n    fccs,\n    bands=[\n        \"fuel_load.litter\",\n        \"fuel_load.duff\",\n        \"duff_depth\",\n        \"fuel_load.live_shrub\",\n    ],\n)\nfuels.wait()\n</code></pre> <p>FCCS takes the same alignment arguments as the other source grids (<code>output_resolution_m</code>, <code>align_to</code>, <code>align</code>, <code>resampling</code>) \u2014 see Align grids to each other.</p> <p>To select and calculate bands across one or more completed grids, see Compose grids.</p>"},{"location":"guides/creating-grids/#canopy-grids","title":"Canopy grids","text":"<p>For crown-fire inputs, <code>create_canopy_fuel_grid_from_landfire</code> produces the full LANDFIRE canopy set (30 m, CONUS) \u2014 canopy height, bulk density, base height, and cover:</p> <pre><code>grid = ff.grids.create_canopy_fuel_grid_from_landfire(domain, output_resolution_m=30)\n</code></pre> <pre><code>&gt;&gt;&gt; [(b.key, b.unit) for b in grid.bands]\n[('chm', 'm'), ('cbd', 'kg/m**3'), ('cbh', 'm'), ('cc', '%')]\n</code></pre> <p>When you only need canopy height at higher resolution, use a dedicated canopy height model \u2014 the Meta model (\u22481 m, global) or NAIP-CHM (0.6 m, CONUS) \u2014 each producing a single <code>chm</code> band:</p> <pre><code>meta = ff.grids.create_canopy_height_grid_from_meta(domain, output_resolution_m=1)\nnaip = ff.grids.create_canopy_height_grid_from_naip_chm(domain, output_resolution_m=1)\n</code></pre> <p>To rasterize a completed airborne point cloud into the same <code>chm</code> band, pass the point cloud directly. The output defaults to 1 m cells:</p> <pre><code>chm = ff.grids.create_canopy_height_grid_from_point_cloud(point_cloud)\nchm.wait()\n</code></pre> <p>Use <code>output_resolution_m</code> or <code>align_to</code> to choose a different lattice. See the Point clouds guide to create an airborne point cloud from USGS 3DEP.</p> <p>To control how each cell reduces its above-ground returns to a height, set <code>aggregation</code> to <code>\"max\"</code> (the default), <code>\"mean\"</code>, <code>\"median\"</code>, or <code>\"percentile\"</code>; <code>\"percentile\"</code> requires a <code>percentile</code> (0-100). The <code>spike_filter</code> argument removes lone spurious returns: leave it unset for the API's default filter, pass <code>False</code> to keep every return, or pass a mapping of thresholds to tune it:</p> <pre><code>chm = ff.grids.create_canopy_height_grid_from_point_cloud(\n    point_cloud,\n    output_resolution_m=2,\n    aggregation=\"percentile\",\n    percentile=95,\n    spike_filter={\"min_canopy_footprint_m\": 5, \"min_prominence_m\": 30},\n)\nchm.wait()\n</code></pre> <p>NAIP-CHM is a surface model</p> <p>NAIP-CHM is a digital surface model and retains buildings and other infrastructure. To keep only vegetation, mask out built-up areas \u2014 see Mask out features.</p>"},{"location":"guides/creating-grids/#derive-canopy-fuels-from-a-tree-inventory","title":"Derive canopy fuels from a tree inventory","text":"<p>To compute the crown-fire bands directly from a completed tree inventory rather than from LANDFIRE, pass the inventory to <code>create_canopy_fuel_grid_from_inventory</code>. It derives canopy bulk density (<code>cbd</code>), canopy base height (<code>cbh</code>), canopy height (<code>chm</code>), canopy cover (<code>cc</code>), and canopy fuel load (<code>cfl</code>) from the inventory's trees; only live trees contribute. The bands share keys and units with the LANDFIRE canopy source, and the output defaults to 30 m cells:</p> <pre><code>canopy = ff.grids.create_canopy_fuel_grid_from_inventory(\n    inventory,\n    bands=[\"cbd\", \"cbh\", \"chm\", \"cc\", \"cfl\"],\n    output_resolution_m=30,\n)\ncanopy.wait()\n</code></pre> <pre><code>&gt;&gt;&gt; {band.key for band in canopy.bands}\n{'cbd', 'cbh', 'chm', 'cc', 'cfl'}\n</code></pre> <p>The default four bands (<code>cbd</code>, <code>cbh</code>, <code>chm</code>, <code>cc</code>) drop <code>cfl</code>; pass <code>bands</code> to choose. Method keyword arguments tune how each band is derived \u2014 the biomass source (<code>biomass_equations</code> or <code>biomass_column</code>), and per-band reduction methods (<code>cbd</code>, <code>cbh</code>, <code>chm</code>, <code>cc</code>), among others. See <code>create_canopy_fuel_grid_from_inventory</code> in the Reference for the full surface. Pass <code>align_to</code> to match another grid's lattice exactly.</p>"},{"location":"guides/creating-grids/#3d-tree-fuel-grids-voxelization","title":"3D tree fuel grids (voxelization)","text":"<p>The 3D canopy fuel grid \u2014 per-voxel bulk density, the input 3D fire models consume \u2014 is built in three steps:</p> <p>1. Create a Plot Imputation Map (PIM) grid that maps each cell to a TreeMap forest inventory plot:</p> <pre><code>pim = ff.grids.create_pim_grid_from_treemap(\n    domain, output_resolution_m=30, resampling=\"nearest\"\n)\npim.wait()\n</code></pre> <p>By default the grid carries the TreeMap id band (<code>tm_id</code>); request the plot control number as well with <code>bands=[\"tm_id\", \"plt_cn\"]</code>.</p> <p>2. Generate a tree inventory from the PIM grid \u2014 a table of individual trees imputed from the matched plots (see the Inventories guide):</p> <pre><code>inventory = ff.inventories.create_tree_inventory_from_pim_grid(\n    domain, pim, seed=42\n)\ninventory.wait()\n</code></pre> <p>3. Voxelize the inventory, discretizing each tree's crown onto a 3D lattice and computing per-voxel fuel properties:</p> <pre><code>voxels = inventory.voxelize(\n    horizontal_resolution_m=2.0, vertical_resolution_m=1.0\n)\nvoxels.wait()\n</code></pre> <pre><code>&gt;&gt;&gt; voxels.georeference.shape   # [layers, rows, cols]\n[37, 703, 863]\n</code></pre> <p>Because it is a 3D product, a voxel grid supports neither resampling nor post-hoc modifications \u2014 apply any tree modifications on the inventory before voxelizing (see Modify and treat tree inventories).</p> <p>For the concepts behind plot imputation and voxelization, see the FastFuels documentation.</p>"},{"location":"guides/creating-grids/#derive-surface-fuels-with-duet","title":"Derive surface fuels with DUET","text":"<p>To create a two-dimensional DUET surface-fuel grid, include the three DUET input bands when voxelizing the inventory:</p> <pre><code>voxels = inventory.voxelize(\n    horizontal_resolution_m=2,\n    vertical_resolution_m=1,\n    bands=[\n        \"bulk_density.foliage.live\",\n        \"spcd\",\n        \"fuel_moisture.live\",\n    ],\n)\nvoxels.wait()\n</code></pre> <p>Build calibration targets from ordinary mappings, then pass them with the output bands and time since fire:</p> <pre><code>calibration = ff.duet_calibration(\n    fuel_load={\n        \"grass\": {\"mean\": 0.5, \"sd\": 0.25},\n        \"litter\": {\"max\": 5, \"min\": 0},\n    },\n    fuel_depth={\n        \"grass\": {\"value\": 0.3},\n        \"litter\": {\"value\": 0.06},\n    },\n)\n\nsurface = ff.grids.create_surface_fuel_grid_from_duet(\n    voxels,\n    years_since_burn=25,\n    bands=[\n        \"fuel_load.grass\",\n        \"fuel_load.litter\",\n        \"fuel_depth.grass\",\n        \"fuel_depth.litter\",\n    ],\n    calibration=calibration,\n)\nsurface.wait()\n</code></pre> <p>Use a <code>value</code> target to set every occupied cell to a constant, <code>max</code> with an optional <code>min</code> to scale by extrema, or <code>mean</code> and <code>sd</code> to scale by moments. Omit <code>calibration</code> only when you want the raw DUET values.</p>"},{"location":"guides/creating-grids/#solar-irradiance-grids","title":"Solar irradiance grids","text":"<p>Beta</p> <p>LeafLux irradiance grids are a new v2 grid family and still stabilizing.</p> <p>To compute solar irradiance through the canopy at a single instant, pass a completed 3D grid carrying a <code>leaf_area_density</code> band \u2014 a voxel grid from voxelization \u2014 to <code>create_irradiance_grid_from_leaflux</code> with a UTC <code>date_time</code>. Light is attenuated through the leaf area density with a Beer-Lambert <code>extinction_coefficient</code> (default 0.5). For what solar irradiance means, see the FastFuels documentation.</p> <p>The <code>irradiance.canopy.relative</code> band needs no terrain:</p> <pre><code>import datetime\n\nirradiance = ff.grids.create_irradiance_grid_from_leaflux(\n    voxels,\n    date_time=datetime.datetime(2024, 7, 1, 18, 0, tzinfo=datetime.timezone.utc),\n    bands=[\"irradiance.canopy.relative\"],\n)\nirradiance.wait()\n</code></pre> <p>The <code>irradiance.surface.relative</code> band (the default) drapes light onto the ground, so it needs a terrain grid sharing the source grid's exact horizontal lattice and carrying an <code>elevation</code> band. Build that terrain grid with <code>align_to=&lt;source grid&gt;</code> and pass it as <code>source_terrain_grid</code>; without a lattice-matched terrain grid the API returns a 422:</p> <pre><code>topo = ff.grids.create_topography_grid_from_3dep(\n    domain, align_to=voxels, bands=[\"elevation\"]\n)\ntopo.wait()\n\nirradiance = ff.grids.create_irradiance_grid_from_leaflux(\n    voxels,\n    date_time=datetime.datetime(2024, 7, 1, 18, 0, tzinfo=datetime.timezone.utc),\n    source_terrain_grid=topo,\n    bands=[\"irradiance.surface.relative\"],\n)\nirradiance.wait()\n</code></pre> <p>See Align grids to each other for how <code>align_to</code> matches a lattice exactly.</p>"},{"location":"guides/creating-grids/#fuel-moisture-grids","title":"Fuel moisture grids","text":"<p>Beta</p> <p>Fosberg dead fuel moisture grids are a new v2 grid family and still stabilizing.</p> <p>To estimate 1-hour dead fuel moisture with the Fosberg model, pass a topography grid and a surface-irradiance grid to <code>create_dead_fuel_moisture_grid_from_fosberg</code> along with the weather inputs. The result carries a single <code>fuel_moisture.dead.1hr</code> band (percent). For the Fosberg 1-hr dead fuel moisture model itself, see the FastFuels documentation.</p> <p>The topography, leaf-area-density, and surface-irradiance grids all feed the same Fosberg cells, so they must land on one shared lattice \u2014 build the topography and irradiance grids against the voxel grid with <code>align_to=</code>. The topography grid supplies <code>slope</code> and <code>aspect</code> to Fosberg and <code>elevation</code> to the irradiance surface draping:</p> <pre><code>import datetime\nimport fastfuels_sdk.v2 as ff\n\n# A voxel grid carrying leaf area density (see 3D tree fuel grids, above).\nvoxels = inventory.voxelize(\n    horizontal_resolution_m=2, vertical_resolution_m=1, bands=[\"leaf_area_density\"]\n)\nvoxels.wait()\n\ntopo = ff.grids.create_topography_grid_from_3dep(\n    domain, align_to=voxels, bands=[\"elevation\", \"slope\", \"aspect\"]\n)\ntopo.wait()\n\nirradiance = ff.grids.create_irradiance_grid_from_leaflux(\n    voxels,\n    date_time=datetime.datetime(2020, 7, 1, 18, 0, tzinfo=datetime.timezone.utc),\n    source_terrain_grid=topo,\n    bands=[\"irradiance.surface.relative\"],\n)\nirradiance.wait()\n\nmoisture = ff.grids.create_dead_fuel_moisture_grid_from_fosberg(\n    topo, irradiance,\n    dry_bulb_temp=75.0,       # degrees Fahrenheit, &gt;= 10\n    relative_humidity=20.0,   # percent, 0-100\n    time=1200,                # local HHMM, 0800-1959\n    month=\"July\",\n    elevation=\"near\",         # Fosberg correction category: below/near/above\n)\nmoisture.wait()\n</code></pre> <pre><code>&gt;&gt;&gt; [band.key for band in moisture.bands]\n['fuel_moisture.dead.1hr']\n</code></pre> <p>Grids must share one lattice</p> <p>Passing topography, leaf-area-density, and irradiance grids that sit on different lattices is the most common failure. Build the topography and irradiance grids against the voxel grid with <code>align_to=</code> \u2014 see Align grids to each other.</p>"},{"location":"guides/creating-grids/#uniform-grids","title":"Uniform grids","text":"<p>To fill the whole domain with constant band values at a chosen resolution \u2014 useful for testing or for holding a fuel parameter constant:</p> <pre><code>grid = ff.grids.create_uniform_grid(\n    domain,\n    resolution_m=2.0,\n    bands={\"fuel_load\": 0.5, \"fuel_moisture\": 15.0},\n)\n</code></pre>"},{"location":"guides/creating-grids/#grids-from-your-own-raster-files","title":"Grids from your own raster files","text":"<p>To upload a local NetCDF whose CRS matches the domain CRS:</p> <pre><code>grid = ff.grids.create_grid_from_netcdf(domain, \"fuels.nc\")\ngrid.wait()\n</code></pre> <p>GeoTIFF uploads work the same way but additionally take a <code>bands</code> list of <code>UploadBandDefinition</code>s mapping 1:1 to the raster's bands in order:</p> <pre><code>from fastfuels_sdk.v2.client_library.models import UploadBandDefinition\n\ngrid = ff.grids.create_grid_from_geotiff(\n    domain,\n    \"elevation.tif\",\n    bands=[UploadBandDefinition(key=\"elevation\", interpolation=\"bilinear\")],\n)\n</code></pre> <p>The SDK creates the grid, uploads the file to the returned signed URL, and hands back a pending grid.</p>"},{"location":"guides/creating-grids/#align-grids-to-each-other","title":"Align grids to each other","text":"<p>The worked example above gave every grid the same <code>output_resolution_m</code> to land them on the domain lattice. Most creators (everything except FCCS and uploads) accept four arguments that control the output lattice; they are mutually exclusive \u2014 passing more than one raises <code>ValueError</code>:</p> <ul> <li><code>output_resolution_m</code> \u2014 resample to this cell size on the domain   lattice. The common case, and what makes grids of equal resolution stack.</li> <li><code>align=\"native\"</code> \u2014 keep the source's native resolution and lattice.</li> <li><code>align_to=&lt;grid&gt;</code> \u2014 match another grid's lattice exactly (pass a   <code>Grid</code> or its id), so the two stack even at the source's native   resolution.</li> <li><code>resampling</code> \u2014 the method used when changing resolution: <code>nearest</code>,   <code>bilinear</code>, <code>cubic</code>, <code>average</code>, <code>mode</code>, <code>min</code>, <code>max</code>, <code>median</code>, and more.   An unrecognized value raises <code>ValueError</code>.</li> </ul> <pre><code># Stack a canopy grid onto an existing topography grid's lattice exactly\ncanopy = ff.grids.create_canopy_fuel_grid_from_landfire(\n    domain, align_to=topography, resampling=\"nearest\"\n)\n</code></pre>"},{"location":"guides/creating-grids/#mask-out-features","title":"Mask out features","text":"<p>To overwrite grid cells that fall within a feature \u2014 the v2 replacement for v1's <code>feature_masks</code> \u2014 build a mask with <code>ff.mask</code> and pass it in a creator's <code>modifications</code> list. For example, to set the cells under roads to a non-burnable fuel model code (FBFM 91):</p> <pre><code>roads = ff.features.create_road_feature_from_osm(domain)\nroads.wait()\n\ngrid = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n    domain,\n    output_resolution_m=30,\n    modifications=[ff.mask(roads, \"fbfm\", 91, buffer_m=5)],\n)\n</code></pre> <p>The feature must be <code>completed</code> and in the same domain as the grid. Roads and other linestrings usually need a <code>buffer_m</code> (or <code>target=\"cell\"</code>) so the thin geometry covers whole cells. See <code>ff.mask</code> in the Reference for masking multiple bands and the <code>operator</code> and <code>target</code> options.</p> <p>The same masks apply to a grid you already hold via <code>grid.apply_modifications</code>, which re-derives the grid in place.</p>"},{"location":"guides/creating-grids/#error-handling","title":"Error handling","text":"<p>Creators raise typed exceptions from <code>fastfuels_sdk.v2.exceptions</code> on invalid input:</p> <pre><code>from fastfuels_sdk.v2.exceptions import UnprocessableEntityException\n\ntry:\n    grid = ff.grids.create_uniform_grid(domain, resolution_m=2.0, bands={\"bad\": 1})\nexcept UnprocessableEntityException as exc:\n    print(exc.detail)\n</code></pre> <p>See Working with grids for the full exception model.</p>"},{"location":"guides/creating-grids/#next-steps","title":"Next steps","text":"<p>A freshly created grid is still a running job. Working with grids covers waiting on it, inspecting its bands and georeference, resampling, exporting, and managing it.</p>"},{"location":"guides/domains/","title":"How to Work with Domains in FastFuels SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>A domain is the spatial container every other FastFuels resource lives in. This guide covers working with domains from Python; for what domains are and how the platform treats them, see the FastFuels documentation. Coming from the v1 SDK? Start with the migration guide.</p>"},{"location":"guides/domains/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key</li> </ul> <p>The v2 SDK reads your API key from the <code>FASTFUELS_API_KEY</code> environment variable, the same variable the v1 SDK uses:</p> <pre><code>export FASTFUELS_API_KEY=\"your-api-key\"\n</code></pre> <p>Or set it programmatically:</p> <pre><code>from fastfuels_sdk.v2 import set_api_key\n\nset_api_key(\"your-api-key\")\n</code></pre>"},{"location":"guides/domains/#create-a-domain-from-geojson","title":"Create a Domain from GeoJSON","text":"<p>To create a domain from a GeoJSON file:</p> <pre><code>import json\nfrom fastfuels_sdk.v2.domains import Domain\n\nwith open(\"area.geojson\") as f:\n    geojson = json.load(f)\n\ndomain = Domain.from_geojson(\n    geojson=geojson,\n    name=\"My Domain\",\n    description=\"Forest area for analysis\",\n    pad_to_resolution=2.0,\n)\n</code></pre> <p>The v2 API accepts a GeoJSON FeatureCollection; the SDK automatically wraps a single Feature for you.</p>"},{"location":"guides/domains/#create-a-domain-from-a-geodataframe-or-a-file","title":"Create a Domain from a GeoDataFrame or a File","text":"<p>If your spatial data is in a format supported by GeoPandas (Shapefile, KML, GeoPackage, etc.):</p> <pre><code>import geopandas as gpd\nfrom fastfuels_sdk.v2.domains import Domain\n\ngdf = gpd.read_file(\"forest_area.shp\")\n\ndomain = Domain.from_geodataframe(\n    geodataframe=gdf,\n    name=\"Forest Domain\",\n    description=\"Imported from shapefile\",\n)\n\n# Or skip the GeoPandas step entirely:\ndomain = Domain.from_file(\"forest_area.shp\", name=\"Forest Domain\")\n</code></pre>"},{"location":"guides/domains/#preview-a-domain-before-creating-it","title":"Preview a Domain Before Creating It","text":"<p><code>Domain.preview</code> runs the same validation and projection pipeline as creation but persists nothing \u2014 useful for showing a user the projected, padded bounding box before committing:</p> <pre><code>previewed = Domain.preview(geojson, pad_to_resolution=2.0)\n\nprint(previewed.id)    # always \"preview\" \u2014 not a real identifier\nprint(previewed.bbox)  # the projected, padded bounding box\n</code></pre>"},{"location":"guides/domains/#retrieve-an-existing-domain","title":"Retrieve an Existing Domain","text":"<p>To fetch a domain using its ID:</p> <pre><code>domain = Domain.from_id(\"abc123\")\n</code></pre>"},{"location":"guides/domains/#update-domain-properties","title":"Update Domain Properties","text":"<p>To modify a domain's name, description, or tags:</p> <pre><code>domain.update(\n    name=\"New Name\",\n    description=\"Updated description\",\n    tags=[\"forest\", \"analysis\"],\n)\n</code></pre> <p><code>update</code> changes the domain in place and returns it, so calls chain. Only the fields you pass are sent; passing none makes no API call.</p>"},{"location":"guides/domains/#refresh-domain-data","title":"Refresh Domain Data","text":"<p>To reload a domain's latest state from the API in place:</p> <pre><code>domain.refresh()\n</code></pre> <p><code>refresh</code> updates the domain in place and returns it. To fetch a separate copy by ID instead, use <code>Domain.from_id(domain.id)</code>.</p>"},{"location":"guides/domains/#get-the-pixel-lattice-for-a-domain","title":"Get the Pixel Lattice for a Domain","text":"<p>To align a raster with the grids FastFuels will produce for a domain, fetch its pixel lattice (affine transform + shape) at a resolution:</p> <pre><code>lattice = domain.get_lattice(resolution=2.0)\n\nprint(lattice.crs)        # e.g. \"EPSG:32611\"\nprint(lattice.transform)  # affine coefficients [a, b, c, d, e, f]\nprint(lattice.shape)      # [height, width] in pixels\n\n# Expand the lattice by N cells on each side, mirroring the\n# extent_buffer_cells semantics of the grid creation endpoints\nbuffered = domain.get_lattice(resolution=2.0, num_buffer_cells=5)\n</code></pre>"},{"location":"guides/domains/#reproject-geojson","title":"Reproject GeoJSON","text":"<p>A stateless utility \u2014 no resource is created:</p> <pre><code>from fastfuels_sdk.v2.domains import reproject_geojson\n\nprojected = reproject_geojson(geojson, target_epsg=5070)\nprint(projected[\"crs\"][\"properties\"][\"name\"])  # \"EPSG:5070\"\n</code></pre>"},{"location":"guides/domains/#list-available-domains","title":"List Available Domains","text":"<p>To list domains with pagination:</p> <pre><code>from fastfuels_sdk.v2.domains import list_domains\n\n# Get first page with default size (100)\ndomains = list_domains()\n\n# Custom page and size\ndomains = list_domains(\n    page=2,\n    size=50,\n    sort_by=\"name\",\n    sort_order=\"ascending\",\n)\n\nfor domain in domains:\n    print(f\"{domain.id}: {domain.name}\")\n</code></pre>"},{"location":"guides/domains/#delete-a-domain","title":"Delete a Domain","text":"<p>To permanently delete a domain and all resources associated with it:</p> <pre><code>domain.delete()\n</code></pre>"},{"location":"guides/domains/#error-handling","title":"Error Handling","text":"<p>Wrapper methods raise typed exceptions from <code>fastfuels_sdk.v2.exceptions</code>:</p> <pre><code>from fastfuels_sdk.v2.domains import Domain\nfrom fastfuels_sdk.v2.exceptions import (\n    NotFoundException,\n    UnprocessableEntityException,\n)\n\ntry:\n    domain = Domain.from_id(\"does-not-exist\")\nexcept NotFoundException:\n    print(\"No such domain (or you don't have access to it)\")\n\ntry:\n    domain = Domain.from_geojson(too_big_geojson)\nexcept UnprocessableEntityException as exc:\n    print(f\"Invalid domain: {exc.detail}\")\n</code></pre>"},{"location":"guides/exports/","title":"How to Export Data from FastFuels SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>An export packages a resource's data into a downloadable file: a grid as GeoTIFF/NetCDF/zarr, an inventory as Parquet/CSV/GeoJSON/GeoPackage, or several grids assembled into a fire-behavior landscape or QUIC-Fire-loadable archive. Exports run as background jobs and expose a signed download URL on completion.</p> <p>The pattern is the same everywhere: exporting a resource you hold is a method on it (<code>grid.export(...)</code>, <code>inventory.export(...)</code>); the QUIC-Fire bundle and landscape are assembled from many grids, so they are module-level functions (<code>ff.exports.create_quicfire_export(...)</code> and <code>ff.exports.create_landscape_export(...)</code>).</p>"},{"location":"guides/exports/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>A completed resource to export \u2014 a grid or an   inventory</li> </ul>"},{"location":"guides/exports/#export-and-download-a-grid","title":"Export and download a grid","text":"<p>To export a completed grid and save the file, chain create \u2192 <code>wait</code> \u2192 <code>to_file</code>:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\nexport = grid.export(format=\"geotiff\")  # or \"netcdf\", \"zarr\"\nexport.wait().to_file(\"elevation.tif\")\n</code></pre> <p><code>grid.export</code> returns a pending <code>Export</code>; <code>wait()</code> blocks until the file is packaged, and <code>to_file</code> streams it to disk. GeoTIFF applies to 2D grids; export 3D voxel grids as <code>\"netcdf\"</code> or <code>\"zarr\"</code>. Pass <code>bands=</code> to export a subset of the grid's bands.</p> <p>If <code>to_file</code> is given an existing directory, the file lands inside it under the export's default filename:</p> <pre><code>&gt;&gt;&gt; export.wait().to_file(\"outputs/\")\nPosixPath('outputs/export.tif')\n</code></pre>"},{"location":"guides/exports/#export-and-download-an-inventory","title":"Export and download an inventory","text":"<p>The same flow exports an inventory's tree records \u2014 <code>\"parquet\"</code> (zipped, default), <code>\"csv\"</code>, <code>\"geojson\"</code>, or <code>\"geopackage\"</code>, with <code>columns=</code> for a column subset:</p> <pre><code>export = inventory.export(format=\"csv\")\npath = export.wait().to_file(\"trees.csv\")\n</code></pre> <pre><code>&gt;&gt;&gt; path.read_text().splitlines()[0]\n'x,y,fia_species_code,fia_status_code,dbh,height,crown_ratio'\n</code></pre>"},{"location":"guides/exports/#create-a-fire-behavior-landscape","title":"Create a fire-behavior landscape","text":"<p>To create an 8-band LANDFIRE-style GeoTIFF for FlamMap, IFTDSS, or WFDSS, assign the terrain, fuel-model, and canopy roles to completed 30 m grids:</p> <pre><code>export = ff.exports.create_landscape_export(\n    domain,\n    fire_behavior_fuel_model=\"fbfm40\",\n    elevation=(topography, \"elevation\"),\n    slope=(topography, \"slope\"),\n    aspect=(topography, \"aspect\"),\n    fuel_model=(fuel_models, \"fbfm\"),\n    canopy_cover=(canopy, \"cc\"),\n    canopy_height=(canopy, \"chm\"),\n    canopy_base_height=(canopy, \"cbh\"),\n    canopy_bulk_density=(canopy, \"cbd\"),\n    name=\"Landscape\",\n)\nexport.wait().to_file(\"landscape.tif\")\n</code></pre> <p>Use <code>fire_behavior_fuel_model=\"fbfm13\"</code> with an FBFM13 source. To export a different domain-anchored cell size, pass <code>resolution_m=</code> after building all role grids at that resolution. To preserve an existing lattice instead, pass <code>align_to=&lt;grid&gt;</code>. The exporter can crop oversized inputs but does not resample or reproject them; use the grid alignment options before exporting if the API reports an alignment error.</p>"},{"location":"guides/exports/#bundle-grids-for-quic-fire","title":"Bundle grids for QUIC-Fire","text":"<p><code>create_quicfire_export</code> packages surface fuel, canopy fuel, and (optionally) topography grids into a zip archive that QUIC-Fire loads directly. Each role is a <code>(grid, band)</code> pair; the five required roles produce <code>treesrhof.dat</code>, <code>treesmoist.dat</code>, and <code>treesfueldepth.dat</code>:</p> <pre><code>export = ff.exports.create_quicfire_export(\n    domain,\n    canopy_bulk_density=(voxels, \"bulk_density.foliage.live\"),\n    canopy_moisture=(voxels, \"fuel_moisture.live\"),\n    surface_fuel_load=(surface, \"fuel_load.1hr\"),\n    surface_fuel_depth=(surface, \"fuel_depth\"),\n    surface_moisture=(surface, \"fuel_moisture.1hr\"),\n    name=\"QUIC-Fire bundle\",\n)\nexport.wait().to_file(\"outputs/\")\n</code></pre> <pre><code>&gt;&gt;&gt; import zipfile\n&gt;&gt;&gt; sorted(zipfile.ZipFile(\"outputs/QUIC-Fire_bundle.zip\").namelist())\n['domain.geojson', 'metadata.json', 'treesfueldepth.dat', 'treesmoist.dat', 'treesrhof.dat']\n</code></pre> <p>Here <code>voxels</code> is a 3D tree fuel grid carrying bulk density and moisture bands, and <code>surface</code> is any 2D grid carrying the surface roles (the example above pairs naturally with a uniform grid or an FBFM40 lookup grid). For an end-to-end walkthrough that builds all of these grids from real data sources and bundles them, follow the Export QUIC-Fire inputs tutorial.</p> <p>Two optional roles extend the bundle: <code>topography=(grid, \"elevation\")</code> adds <code>topo.dat</code>, and the SAVR pair (<code>canopy_savr=</code> + <code>surface_savr=</code>, both or neither) adds <code>treesss.dat</code>.</p> <p>The fire grid \u2014 the lattice everything is sliced onto \u2014 defaults to the domain bounding box at 2 m horizontal / 1 m vertical. Change it with <code>horizontal_resolution_m=</code> / <code>vertical_resolution_m=</code>, or define it from an existing grid's lattice with <code>align_to=&lt;grid&gt;</code>. Every role grid must be lattice-aligned with the fire grid and cover its full extent \u2014 the exporter crops oversized grids by integer slicing but never resamples, so build the roles on the fire grid's lattice (see Align grids to each other).</p>"},{"location":"guides/exports/#work-with-an-export-you-hold","title":"Work with an export you hold","text":"<p>An <code>Export</code> is a job resource like any other: <code>wait(timeout=, verbose=)</code> polls it to a terminal status (raising <code>JobFailedError</code> on failure), <code>refresh()</code> reloads it in place, <code>update(name=, description=, tags=)</code> edits its metadata, and <code>delete()</code> removes it along with the packaged file.</p> <p>The signed download URL fills in on completion and expires after <code>expiration_days</code> (max 7, the default):</p> <pre><code>&gt;&gt;&gt; export.signed_url[:50]\n'https://storage.googleapis.com/silvx-fastfuels-exp'\n&gt;&gt;&gt; export.expires_on\ndatetime.datetime(2026, 6, 17, 15, 5, 46, 885537, tzinfo=datetime.timezone.utc)\n</code></pre> <p>Downloading before the job completes raises a <code>ValueError</code>:</p> <pre><code>&gt;&gt;&gt; pending_export.to_file(\"outputs/\")\nValueError: Cannot download an export with status 'pending'. Call .wait() until it completes first.\n</code></pre> <p>Exports are addressed by their ID alone \u2014 no domain in the path:</p> <pre><code>export = ff.get_export(\"4a56bae0cd5e481aa1617cb894a9a7f3\")\n</code></pre>"},{"location":"guides/exports/#list-exports","title":"List exports","text":"<p>To list your exports, optionally narrowed to a domain, a source name (the format, <code>\"landscape\"</code>, or <code>\"quicfire\"</code>), or a tag:</p> <pre><code>exports = ff.list_exports(domain)\nlandscapes = ff.list_exports(source=\"landscape\")\nbundles = ff.list_exports(source=\"quicfire\")\ntagged = ff.list_exports(tag=\"run-42\")\n</code></pre>"},{"location":"guides/exports/#error-handling","title":"Error handling","text":"<p>Wrapper functions and methods raise typed exceptions from <code>fastfuels_sdk.v2.exceptions</code>:</p> <pre><code>import fastfuels_sdk.v2 as ff\nfrom fastfuels_sdk.v2.exceptions import NotFoundException\n\ntry:\n    export = ff.get_export(\"does-not-exist\")\nexcept NotFoundException:\n    print(\"No such export (or you don't have access to it)\")\n</code></pre>"},{"location":"guides/exports/#next-steps","title":"Next steps","text":"<ul> <li>Build the grids that feed an export \u2014 see   Creating grids and   Inventories</li> <li>Full signatures \u2014 see the Reference</li> </ul>"},{"location":"guides/features/","title":"How to Work with Features in FastFuels SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>Features are geographic data within a domain: roads and water bodies extracted from OpenStreetMap, or custom layersets of fuelbed polygons you upload yourself. This guide covers working with features from Python; for what features are and how the platform treats them, see the FastFuels documentation. Coming from the v1 SDK? Start with the migration guide.</p> <p>The v2 surface is functional: you create a feature by calling a <code>create_..._feature_from_...</code> function on a domain, and everything you do with a feature you already hold is a method on it.</p> <pre><code>import fastfuels_sdk.v2 as ff\n\nfeature = ff.features.create_road_feature_from_osm(domain)\nfeature.wait()\n</code></pre>"},{"location":"guides/features/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>An existing domain to create features in</li> </ul>"},{"location":"guides/features/#create-a-road-feature-from-openstreetmap","title":"Create a Road Feature from OpenStreetMap","text":"<p>To extract the road network within a domain's extent:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\nfeature = ff.features.create_road_feature_from_osm(\n    domain,\n    name=\"My Roads\",\n    description=\"OSM road network\",\n)\n\nprint(feature.status)  # pending\n</code></pre> <p>Feature generation runs as a background job \u2014 the returned feature starts in <code>\"pending\"</code> status. To include roads just outside the domain, buffer the query extent by up to 100 meters with <code>extent_buffer_m</code>:</p> <pre><code>feature = ff.features.create_road_feature_from_osm(domain, extent_buffer_m=50)\n</code></pre> <p>The first argument accepts either a <code>Domain</code> or a bare domain id string.</p>"},{"location":"guides/features/#create-a-water-feature-from-openstreetmap","title":"Create a Water Feature from OpenStreetMap","text":"<p>To extract water bodies, use the same calling convention:</p> <pre><code>feature = ff.features.create_water_feature_from_osm(domain, name=\"My Water\")\n</code></pre>"},{"location":"guides/features/#wait-for-a-feature-to-complete","title":"Wait for a Feature to Complete","text":"<p>To block until a feature job finishes:</p> <pre><code>feature.wait(verbose=True)\n</code></pre> <pre><code>Feature 36573c0205d147a08011693b7894c0fb: pending (5s)\nFeature 36573c0205d147a08011693b7894c0fb: running (15s)\nFeature 36573c0205d147a08011693b7894c0fb: completed (20s)\n</code></pre> <p><code>wait</code> polls until the job reaches a terminal status and returns the feature (so calls chain). By default it waits indefinitely; pass <code>timeout</code> (seconds) to bound the wait, which raises <code>TimeoutError</code> if exceeded. A failed job raises <code>JobFailedError</code>, carrying the API's <code>code</code>, <code>message</code>, and <code>suggestion</code>. To wait on several jobs at once, use <code>ff.wait_all</code>:</p> <pre><code>roads = ff.features.create_road_feature_from_osm(domain)\nwater = ff.features.create_water_feature_from_osm(domain)\n\nff.wait_all([roads, water])  # jobs run server-side in parallel\n</code></pre> <p>Once completed, the feature's <code>georeference</code> reports the CRS and bounds of the generated data:</p> <pre><code>print(feature.georeference.crs)     # 'EPSG:32611'\nprint(feature.georeference.bounds)  # [720192.0, 5189446.0, 721918.0, 5190852.0]\n</code></pre>"},{"location":"guides/features/#upload-a-custom-layerset","title":"Upload a Custom Layerset","text":"<p>A layerset is a FeatureCollection of fuelbed polygons you supply yourself. Unlike OSM features, the upload is synchronous \u2014 the returned feature is already <code>\"completed\"</code>.</p> <p>Projected CRS required</p> <p>The FeatureCollection's <code>crs</code> member must declare a projected CRS (e.g. EPSG:5070 or a UTM zone). Geographic coordinates (EPSG:4326) are rejected, because rasterization requires cell sizes in meters.</p> <p>Each polygon's <code>properties</code> must carry the fuelbed input columns <code>fuel_type</code>, <code>fuel_loading</code>, <code>fuel_height</code>, <code>percent_cover</code>, and <code>distribution</code> (<code>\"homogeneous\"</code>, <code>\"random_clusters\"</code>, or <code>\"uniform_random\"</code>):</p> <pre><code>import json\nimport fastfuels_sdk.v2 as ff\n\nwith open(\"fuelbeds.geojson\") as f:  # projected CRS + fuelbed properties\n    geojson = json.load(f)\n\nfeature = ff.features.create_layerset_feature_from_geojson(\n    domain, geojson, name=\"My Fuelbeds\"\n)\n\nprint(feature.status)  # completed\n</code></pre> <p>To upload from a GeoPandas GeoDataFrame instead, reproject first if needed \u2014 the GeoDataFrame's CRS is forwarded as-is:</p> <pre><code>import geopandas as gpd\n\ngdf = gpd.read_file(\"fuelbeds.shp\").to_crs(epsg=5070)\nfeature = ff.features.create_layerset_feature_from_geodataframe(domain, gdf)\n</code></pre>"},{"location":"guides/features/#rasterize-a-layerset-into-a-grid","title":"Rasterize a Layerset into a Grid","text":"<p>A completed layerset feature carries vector fuelbed polygons; to burn them onto a raster grid, call <code>rasterize</code>. It returns a pending <code>Grid</code>:</p> <pre><code>grid = feature.rasterize(output_resolution_m=2.0)\ngrid.wait()\n</code></pre> <p><code>rasterize</code> takes the same alignment arguments as the grid creators (<code>output_resolution_m</code>, <code>align_to</code>, <code>align</code>, <code>resampling</code>) plus an <code>overlap_method</code> controlling how overlapping polygons resolve. See Align grids to each other for the alignment model.</p>"},{"location":"guides/features/#retrieve-an-existing-feature","title":"Retrieve an Existing Feature","text":"<p>To fetch a feature using its domain and feature IDs:</p> <pre><code>feature = ff.get_feature(domain, \"36573c0205d147a08011693b7894c0fb\")\n</code></pre>"},{"location":"guides/features/#refresh-feature-data","title":"Refresh Feature Data","text":"<p>To reload a feature's latest state from the API (for example, to check job progress yourself) in place:</p> <pre><code>feature.refresh()\nprint(feature.status)\n</code></pre> <p><code>refresh</code> updates the feature in place and returns it. To fetch a separate copy by ID instead, use <code>ff.get_feature(domain, feature.id)</code>.</p>"},{"location":"guides/features/#access-feature-data","title":"Access Feature Data","text":"<p>The generated geodata is served in partitions. For most workflows, retrieve everything at once as a GeoDataFrame:</p> <pre><code>gdf = feature.to_geodataframe()\n\nprint(len(gdf))   # 43\nprint(gdf.crs)    # EPSG:32611\n</code></pre> <p>Or as a single GeoJSON FeatureCollection:</p> <pre><code>data = feature.get_data()\nprint(len(data[\"features\"]))  # 43\n</code></pre> <p>To control retrieval partition by partition (useful for large features), read the partition layout first:</p> <pre><code>metadata = feature.get_data_metadata()\nprint(metadata.total_features)   # 43\nprint(metadata.partition_count)  # 1\n\nfor index in range(metadata.partition_count):\n    partition = feature.get_data_partition(index)  # GeoJSON FeatureCollection\n</code></pre> <p>Data is only available once the feature is <code>\"completed\"</code> \u2014 earlier calls raise <code>UnprocessableEntityException</code>.</p>"},{"location":"guides/features/#update-feature-properties","title":"Update Feature Properties","text":"<p>To modify a feature's name, description, or tags:</p> <pre><code>feature.update(name=\"New Name\", tags=[\"roads\", \"osm\"])\n</code></pre> <p><code>update</code> changes the feature in place and returns it. Only the fields you pass are sent; passing none makes no API call.</p>"},{"location":"guides/features/#list-features","title":"List Features","text":"<p>To list features in a domain:</p> <pre><code>features = ff.list_features(domain)\n</code></pre> <p>To list features across all your domains, omit the domain:</p> <pre><code>all_features = ff.list_features()\n</code></pre> <p>Narrow the results with filters:</p> <pre><code>roads = ff.list_features(domain, feature_type=\"road\")\nosm_features = ff.list_features(domain, product=\"osm\")\ntagged = ff.list_features(domain, tag=\"roads\")\n</code></pre> <p>Sort a page by name, creation time, or modification time:</p> <pre><code>newest_first = ff.list_features(\n    domain,\n    sort_by=\"created_on\",\n    sort_order=\"descending\",\n)\n</code></pre>"},{"location":"guides/features/#delete-a-feature","title":"Delete a Feature","text":"<p>To permanently delete a feature and its generated data:</p> <pre><code>feature.delete()\n</code></pre> <p>Deleting a domain also deletes all of its features.</p>"},{"location":"guides/features/#error-handling","title":"Error Handling","text":"<p>Wrapper functions and methods raise typed exceptions from <code>fastfuels_sdk.v2.exceptions</code>:</p> <pre><code>import fastfuels_sdk.v2 as ff\nfrom fastfuels_sdk.v2.exceptions import (\n    NotFoundException,\n    UnprocessableEntityException,\n)\n\ntry:\n    feature = ff.get_feature(domain, \"does-not-exist\")\nexcept NotFoundException:\n    print(\"No such feature (or you don't have access to it)\")\n\ntry:\n    metadata = pending_feature.get_data_metadata()\nexcept UnprocessableEntityException as exc:\n    print(exc.detail)\n    # features/36573c0205d147a08011693b7894c0fb status is 'pending',\n    # expected 'completed'.\n</code></pre>"},{"location":"guides/inventories/","title":"How to Work with Tree Inventories in FastFuels SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>An inventory is a table of individual trees within a domain \u2014 one row per tree, with its coordinates, species, diameter, height, and crown ratio. Inventories sit between grids: they are generated from a grid (a PIM grid or a canopy height model) or from your own data, and they are voxelized into the 3D canopy fuel grid that 3D fire models consume. This guide covers working with inventories from Python; for what inventories are, see the FastFuels documentation. Coming from the v1 SDK? Start with the migration guide.</p> <p>The v2 surface is functional: you create an inventory by calling a <code>create_tree_inventory_from_...</code> function on a domain, and everything you do with an inventory you already hold is a method on it.</p> <pre><code>import fastfuels_sdk.v2 as ff\n\ninventory = ff.inventories.create_tree_inventory_from_pim_grid(domain, pim)\ntrees = inventory.wait().to_dataframe()\n</code></pre>"},{"location":"guides/inventories/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>An existing domain \u2014 every creator's first argument is a   <code>Domain</code> (or a bare domain id string)</li> </ul>"},{"location":"guides/inventories/#from-treemap-to-trees","title":"From TreeMap to trees","text":"<p>The most common workflow generates a tree inventory for anywhere in the conterminous US: create a PIM grid that matches each cell to a TreeMap forest inventory plot, then expand the matched plots into individual trees. Pass a <code>seed</code> to make the expansion reproducible:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\npim = ff.grids.create_pim_grid_from_treemap(\n    domain, output_resolution_m=30, resampling=\"nearest\"\n)\npim.wait()\n\ninventory = ff.inventories.create_tree_inventory_from_pim_grid(\n    domain, pim, seed=42, name=\"Blue Mountain trees\"\n)\ninventory.wait()\n</code></pre> <p>Expansion runs as a background job \u2014 the returned inventory starts in <code>\"pending\"</code> status and <code>wait()</code> blocks until it is ready. Once completed, load the trees as a pandas DataFrame:</p> <pre><code>&gt;&gt;&gt; trees = inventory.to_dataframe()\n&gt;&gt;&gt; len(trees)\n71619\n&gt;&gt;&gt; trees.head()\n               x             y  fia_species_code  ...     dbh   height  crown_ratio\n0  721167.447037  5.190627e+06               122  ...  10.160   6.7056         0.10\n1  721169.886263  5.190628e+06               122  ...  22.606  10.3632         0.80\n2  721172.889178  5.190630e+06               122  ...  10.160   6.7056         0.10\n3  721167.558322  5.190629e+06               122  ...   8.128   4.5720         0.08\n4  721177.067389  5.190639e+06               122  ...   8.128   4.5720         0.08\n</code></pre> <p>Coordinates are in the domain CRS in meters, <code>dbh</code> is in centimeters, and <code>height</code> is in meters; the inventory's <code>columns</code> attribute records each column's type and unit.</p>"},{"location":"guides/inventories/#create-an-inventory-from-a-canopy-height-model","title":"Create an inventory from a canopy height model","text":"<p>To detect individual trees in a completed canopy height model grid instead \u2014 useful where you want trees derived from remotely sensed canopy structure:</p> <pre><code>chm = ff.grids.create_canopy_height_grid_from_meta(domain, output_resolution_m=1)\nchm.wait()\n\ninventory = ff.inventories.create_tree_inventory_from_chm_grid(domain, chm)\ninventory.wait()\n</code></pre> <p>Stem isolation defaults to local maximum filtering with a 2 m minimum height. To tune the algorithm, pass a <code>StemIsolationLmf</code> or <code>StemIsolationVwf</code> (variable window filtering) model as <code>algorithm=</code>. A CHM-derived inventory carries only what the canopy surface reveals \u2014 <code>x</code>, <code>y</code>, and <code>height</code> columns \u2014 so it cannot be voxelized directly (voxelization needs the per-tree measurements a PIM expansion or an upload provides).</p>"},{"location":"guides/inventories/#upload-your-own-tree-records","title":"Upload your own tree records","text":"<p>To create an inventory from your own measurements, upload a <code>.csv</code>, <code>.geojson</code>, or <code>.gpkg</code> file. Coordinates must be in the domain's CRS:</p> <pre><code>inventory = ff.inventories.create_tree_inventory_from_file(\n    domain, \"plot_trees.csv\", name=\"Field plot\"\n)\ninventory.wait()\n</code></pre> <p>The standard column roles are <code>x</code>, <code>y</code>, <code>height</code> (m), <code>dbh</code> (cm), <code>crown_ratio</code>, <code>fia_species_code</code>, and <code>fia_status_code</code>. Columns whose names already match need no mapping; otherwise map your file's column names onto the roles:</p> <pre><code>inventory = ff.inventories.create_tree_inventory_from_file(\n    domain,\n    \"plot_trees.csv\",\n    columns={\"x\": \"X_UTM\", \"y\": \"Y_UTM\", \"dbh\": \"DBH_CM\", \"height\": \"HT_M\"},\n)\n</code></pre> <p>The upload itself is synchronous, but processing the file runs as a background job \u2014 <code>wait()</code> before reading data back:</p> <pre><code>&gt;&gt;&gt; inventory.wait().to_dataframe()\n          x          y  height  ...  crown_ratio  fia_species_code  fia_status_code\n0  721055.0  5190149.0    12.0  ...         0.40               122                1\n1  721065.0  5190159.0     8.5  ...         0.50               122                1\n2  721075.0  5190169.0    15.2  ...         0.35               202                1\n</code></pre>"},{"location":"guides/inventories/#impute-missing-morphology-with-gdam","title":"Impute missing morphology with GDAM","text":"<p>An uploaded inventory may carry only some columns \u2014 coordinates and heights, say, without diameters or species. <code>create_tree_inventory_from_gdam</code> fills in the missing morphology (<code>dbh</code>, <code>crown_ratio</code>, <code>fia_species_code</code>) for a completed inventory using GDAM allometry, producing a new inventory:</p> <pre><code>sparse = ff.inventories.create_tree_inventory_from_file(domain, \"stems.csv\")\nsparse.wait()\n\nfull = ff.inventories.create_tree_inventory_from_gdam(domain, sparse)\nfull.wait()\n</code></pre> <p>Existing values are preserved \u2014 only missing cells are imputed. Pass <code>impute_columns=[\"fia_species_code\"]</code> to impute just a subset.</p>"},{"location":"guides/inventories/#wait-for-an-inventory-to-finish","title":"Wait for an inventory to finish","text":"<p><code>wait</code> polls until the job reaches a terminal status and returns the inventory (so calls chain). By default it waits indefinitely; pass <code>timeout</code> (seconds) to bound the wait, which raises <code>TimeoutError</code> if exceeded. A failed job raises <code>JobFailedError</code>. To join several jobs at once, use <code>ff.wait_all</code>:</p> <pre><code>ff.wait_all([pim, inventory], verbose=True)\n</code></pre> <pre><code>Inventory 7b02df6f583a418da3ec9929037d876d: completed (5s)\n</code></pre>"},{"location":"guides/inventories/#access-the-tree-records","title":"Access the tree records","text":"<p>For most workflows, <code>to_dataframe()</code> (shown above) is all you need \u2014 it retrieves each partition through the CSV endpoint, parses it with pandas, and assembles one DataFrame. Pass <code>columns=</code> to retrieve a subset.</p> <p>The records are served in fixed-size partitions; to control retrieval partition by partition (useful for large inventories), read the partition layout first:</p> <pre><code>&gt;&gt;&gt; metadata = inventory.get_data_metadata()\n&gt;&gt;&gt; metadata.total_rows\n71619\n&gt;&gt;&gt; metadata.num_partitions\n4\n&gt;&gt;&gt; partition = inventory.get_data_partition(0)\n&gt;&gt;&gt; partition.num_rows\n34831\n</code></pre> <p><code>get_data_partition</code> uses the compact <code>\"split\"</code> JSON layout by default, where each row in <code>partition.data</code> follows <code>partition.columns</code>. To receive self-describing row mappings instead, request the <code>\"records\"</code> layout:</p> <pre><code>partition = inventory.get_data_partition(\n    0, columns=[\"x\", \"y\", \"height\"], json_orientation=\"records\"\n)\nfirst_tree = partition.data[0]\n</code></pre> <p>Data is only available once the inventory is <code>\"completed\"</code> \u2014 earlier calls raise <code>UnprocessableEntityException</code>.</p>"},{"location":"guides/inventories/#summarize-a-column-without-downloading-records","title":"Summarize a column without downloading records","text":"<p>Once an inventory completes, inspect a column's server-computed summary by passing its key to <code>column_summary</code>:</p> <pre><code>dbh = inventory.column_summary(\"dbh\")\nmean_dbh = dbh.mean\nminimum_dbh = dbh.min_\nmaximum_dbh = dbh.max_\n\nspecies = inventory.column_summary(\"fia_species_code\")\nspecies_count = species.unique_count\n</code></pre> <p>A continuous column reports <code>count</code>, <code>null_count</code>, <code>min_</code>, <code>max_</code>, <code>mean</code>, and <code>std</code>; a categorical column reports <code>count</code>, <code>null_count</code>, and <code>unique_count</code>. The <code>type_</code> field identifies the shape. <code>column_summary</code> returns <code>None</code> when the requested column exists but its summary is unavailable; an unknown column key raises <code>ValueError</code>.</p>"},{"location":"guides/inventories/#inspect-stand-level-forestry-metrics","title":"Inspect stand-level forestry metrics","text":"<p>Once a tree inventory completes, read its server-computed forestry metrics without downloading the tree records:</p> <pre><code>metrics = inventory.forestry_metrics\n\ntree_count = metrics.tree_count\nbasal_area_per_acre = metrics.basal_area_per_area\ntrees_per_acre = metrics.tree_density\nquadratic_mean_diameter_inches = metrics.quadratic_mean_diameter\n\ndominant_groups = [\n    (group.spgrpcd, group.name, group.basal_area_share)\n    for group in metrics.dominant_species_groups\n]\n</code></pre> <p><code>dominant_species_groups</code> is ordered by decreasing basal-area share and contains the leading FIA species groups. Only the leading groups are returned, so their shares may sum to less than one. <code>forestry_metrics</code> is <code>None</code> when metrics are unavailable, including before processing completes.</p>"},{"location":"guides/inventories/#reshape-the-trees","title":"Reshape the trees","text":"<p>Every creator accepts <code>modifications=</code> (rules that filter trees by conditions and act on them) and <code>treatments=</code> (silvicultural thinning to a target), and you can reshape an inventory you already hold with <code>apply_modifications</code> / <code>apply_treatments</code>. See Modify and treat tree inventories for the full workflow.</p>"},{"location":"guides/inventories/#duplicate-an-inventory","title":"Duplicate an inventory","text":"<p><code>duplicate</code> makes an independent copy under a new ID, byte-copying the finished data rather than re-deriving it, so the copy starts identical to the source:</p> <pre><code>&gt;&gt;&gt; copy = inventory.duplicate(name=\"Scenario A\")\n&gt;&gt;&gt; copy.wait()\n&gt;&gt;&gt; copy.checksum == inventory.checksum\nTrue\n</code></pre> <p>The <code>checksum</code> is a version marker for an inventory's content: it changes each time the data is rebuilt and is unaffected by metadata-only edits, so an identical checksum means identical trees. Duplicate before reshaping to keep the original untouched (see Branch a scenario).</p>"},{"location":"guides/inventories/#voxelize-into-a-3d-fuel-grid","title":"Voxelize into a 3D fuel grid","text":"<p>A completed PIM-expanded or uploaded inventory voxelizes into the 3D tree fuel grid \u2014 per-voxel canopy bulk density on a 3D lattice:</p> <pre><code>voxels = inventory.voxelize(\n    horizontal_resolution_m=2.0, vertical_resolution_m=1.0\n)\nvoxels.wait()\n</code></pre> <pre><code>&gt;&gt;&gt; [b.key for b in voxels.bands]\n['bulk_density.foliage.live']\n&gt;&gt;&gt; voxels.georeference.crs\n'EPSG:32611'\n&gt;&gt;&gt; voxels.georeference.shape   # [layers, rows, cols]\n[37, 703, 863]\n</code></pre> <p>By default each tree's foliage biomass is distributed with the Purves crown profile model using NSVB allometry. Request more bands with <code>bands=</code> (e.g. <code>\"bulk_density.branchwood.live\"</code>, <code>\"fuel_moisture.dead\"</code>, <code>\"spcd\"</code>, <code>\"tree_id\"</code>), switch the crown shape with <code>crown_profile_model=\"beta\"</code>, and pass a <code>seed</code> for reproducibility. Because the result is a 3D product, it supports neither resampling nor grid modifications \u2014 modify the trees on the inventory instead, before voxelizing.</p>"},{"location":"guides/inventories/#export-an-inventory","title":"Export an inventory","text":"<p>To export the tree records to a downloadable file and save it, call <code>export</code> with a format \u2014 <code>\"parquet\"</code> (zipped, default), <code>\"csv\"</code>, <code>\"geojson\"</code>, or <code>\"geopackage\"</code> \u2014 and chain the export job's <code>wait</code> into <code>to_file</code>:</p> <pre><code>export = inventory.export(format=\"csv\")\nexport.wait().to_file(\"trees.csv\")\n</code></pre> <p>Pass <code>columns=</code> to export a column subset. For managing exports, see the Exports guide.</p>"},{"location":"guides/inventories/#retrieve-an-existing-inventory","title":"Retrieve an existing inventory","text":"<p>To fetch an inventory using its domain and inventory IDs:</p> <pre><code>inventory = ff.get_inventory(domain, \"7b02df6f583a418da3ec9929037d876d\")\n</code></pre>"},{"location":"guides/inventories/#refresh-and-update","title":"Refresh and update","text":"<p>To reload an inventory's latest state from the API in place:</p> <pre><code>inventory.refresh()\nprint(inventory.status)\n</code></pre> <p>To modify an inventory's name, description, or tags:</p> <pre><code>inventory.update(name=\"New Name\", tags=[\"thinned\"])\n</code></pre> <p>Both update the inventory in place and return it. <code>update</code> sends only the fields you pass; passing none makes no API call.</p>"},{"location":"guides/inventories/#list-inventories","title":"List inventories","text":"<p>To list inventories in a domain:</p> <pre><code>inventories = ff.list_inventories(domain)\n</code></pre> <p>To list inventories across all your domains, omit the domain:</p> <pre><code>all_inventories = ff.list_inventories()\n</code></pre> <p>Narrow the results with filters:</p> <pre><code>pim_inventories = ff.list_inventories(domain, source=\"pim\")\ntagged = ff.list_inventories(domain, tag=\"thinned\")\n</code></pre>"},{"location":"guides/inventories/#delete-an-inventory","title":"Delete an inventory","text":"<p>To permanently delete an inventory and its tree records:</p> <pre><code>inventory.delete()\n</code></pre> <p>Deleting a domain also deletes all of its inventories.</p>"},{"location":"guides/inventories/#error-handling","title":"Error handling","text":"<p>Wrapper functions and methods raise typed exceptions from <code>fastfuels_sdk.v2.exceptions</code>:</p> <pre><code>import fastfuels_sdk.v2 as ff\nfrom fastfuels_sdk.v2.exceptions import (\n    NotFoundException,\n    UnprocessableEntityException,\n)\n\ntry:\n    inventory = ff.get_inventory(domain, \"does-not-exist\")\nexcept NotFoundException:\n    print(\"No such inventory (or you don't have access to it)\")\n\ntry:\n    trees = running_inventory.to_dataframe()\nexcept UnprocessableEntityException as exc:\n    print(exc.detail)\n    # inventories/a6840e0c7cd64de896954d95afc6aea0 status is 'running',\n    # expected 'completed'.\n</code></pre> <p>Deriving from an inventory that is not completed raises a <code>ValueError</code> before any API call:</p> <pre><code>&gt;&gt;&gt; pending_inventory.voxelize(horizontal_resolution_m=2.0, vertical_resolution_m=1.0)\nValueError: Cannot voxelize an inventory with status 'pending'. Call .wait() until it completes first.\n</code></pre>"},{"location":"guides/inventories/#next-steps","title":"Next steps","text":"<ul> <li>Voxelize alongside aligned 2D grids \u2014 see   Creating grids</li> <li>Wait on, inspect, and manage the voxelized grid \u2014 see   Working with grids</li> <li>Full signatures \u2014 see the Reference</li> </ul>"},{"location":"guides/migration/","title":"Migrating from v1 to v2","text":"<p>The FastFuels v1 and v2 APIs are separate live services. Resources created in v1 are not visible to v2 (and vice versa) \u2014 migrating a workflow means creating new resources through v2. The SDK ships both interfaces as versioned subpackages, so you can keep v1 working while you migrate to v2 one workflow at a time:</p> <pre><code>from fastfuels_sdk import Domain      # v1 (the default today)\nfrom fastfuels_sdk.v1 import Domain   # v1, explicit\nfrom fastfuels_sdk.v2 import Domain   # v2\n</code></pre> <p>Both subpackages read the same <code>FASTFUELS_API_KEY</code> environment variable. v1 and v2 are separate deployments that issue different keys, though, so a v1 key will not authenticate against v2 \u2014 set <code>FASTFUELS_API_KEY</code> to the key for the version you are calling.</p> <p>One key per process</p> <p>Because both subpackages read the single <code>FASTFUELS_API_KEY</code> variable, using v1 and v2 at the same time in one process with different keys is not supported. Migrate one workflow at a time, pointing <code>FASTFUELS_API_KEY</code> at the appropriate key for each run.</p>"},{"location":"guides/migration/#at-a-glance","title":"At a glance","text":"v1 v2 What changed <code>Domain.from_geojson(..., horizontal_resolution=2.0, vertical_resolution=1.0)</code> <code>Domain.from_geojson(..., pad_to_resolution=2.0)</code> Resolution belongs to grids now; domains only pad their extent for grid alignment <code>domain.export()</code> grid exports No domain-level export in v2; data exports hang off grids \u2014 <code>Domain.preview</code>, <code>Domain.get_lattice</code>, <code>reproject_geojson</code> New domain capabilities <code>Grids</code>, <code>SurfaceGrid</code>, <code>TreeGrid</code>, <code>TopographyGrid</code>, <code>FeatureGrid</code> + builders unified <code>Grid</code> resource One job-based resource per grid, distinguished by data source <code>Features</code>, <code>RoadFeature</code>, <code>WaterFeature</code> unified <code>Feature</code> resource One job-based resource for OSM roads, OSM water, and custom layersets <code>feature.get_data()</code>, <code>feature.get_all_data()</code> <code>feature.get_data_metadata()</code>, <code>feature.get_data_partition()</code>, <code>feature.get_data()</code>, <code>feature.to_geodataframe()</code> Page-based data retrieval becomes partition-based <code>Inventories</code>, <code>TreeInventory</code> unified <code>Inventory</code> resource One job-based resource, created from a PIM grid, a canopy height model, or an upload"},{"location":"guides/migration/#domains","title":"Domains","text":"<p>Available now \u2014 see the Domains guide and the Reference.</p>"},{"location":"guides/migration/#what-changed-from-v1","title":"What changed from v1","text":"<ul> <li>Resolution moved to grids. Domains no longer take   <code>horizontal_resolution</code>/<code>vertical_resolution</code>. Use the optional   <code>pad_to_resolution</code> argument to pad the domain bounding box outward so   grids of that resolution align with it.</li> <li>Responses carry the working extent. A v2 domain contains one named   GeoJSON feature, <code>\"domain\"</code>: the projected bounding box used by child   resources. Keep the original input geometry separately if you need it.</li> <li>GeoDataFrame CRS is honored. <code>from_geodataframe</code> forwards the   GeoDataFrame's CRS to the API, so projected inputs (e.g. EPSG:5070)   are interpreted correctly. The v1 SDK assumed EPSG:4326.</li> <li>No <code>Domain.export()</code>. The v2 API has no domain export endpoint;   data is exported through grid and inventory exports   instead.</li> <li>New endpoints. <code>Domain.preview</code> validates and projects a domain   without creating it, <code>Domain.get_lattice</code> returns the pixel lattice   for grid alignment, and <code>reproject_geojson</code> is a stateless   reprojection utility.</li> <li>Typed exceptions. Errors raise <code>fastfuels_sdk.v2.exceptions</code>   classes (<code>NotFoundException</code>, <code>UnprocessableEntityException</code>, ...)   carrying the HTTP status code and API error detail.</li> </ul>"},{"location":"guides/migration/#before-and-after","title":"Before and after","text":"v1v2 <pre><code>from fastfuels_sdk import Domain\n\ndomain = Domain.from_geojson(\n    geojson,\n    name=\"My Domain\",\n    horizontal_resolution=2.0,\n    vertical_resolution=1.0,\n)\n</code></pre> <pre><code>from fastfuels_sdk.v2 import Domain\n\ndomain = Domain.from_geojson(\n    geojson,\n    name=\"My Domain\",\n    pad_to_resolution=2.0,\n)\n</code></pre>"},{"location":"guides/migration/#grids","title":"Grids","text":"<p>Available now \u2014 see Creating grids, Working with grids, and the Reference.</p>"},{"location":"guides/migration/#what-changed-from-v1_1","title":"What changed from v1","text":"<ul> <li>One unified <code>Grid</code> resource. v1's per-type resources   (<code>SurfaceGrid</code>, <code>TreeGrid</code>, <code>TopographyGrid</code>, <code>FeatureGrid</code>) and their   builders collapse into a single job-based <code>Grid</code> distinguished by its   data source. The <code>Grids.from_domain_id(...)</code> container is replaced by   the module-level <code>list_grids(domain)</code>.</li> <li>Creation is a function per source. Instead of a builder, call a   <code>create_&lt;kind&gt;_grid_from_&lt;source&gt;</code> function on the domain \u2014 e.g.   <code>ff.grids.create_topography_grid_from_3dep(...)</code> or   <code>ff.grids.create_fuel_model_grid_from_landfire_fbfm40(...)</code>.</li> <li>Alignment is explicit. Resolution and lattice are set per grid with   <code>output_resolution_m</code>, <code>align=\"native\"</code>, <code>align_to=&lt;grid&gt;</code>, and   <code>resampling</code> (see   Align grids to each other).</li> <li><code>feature_masks</code> becomes <code>modifications</code>. v1's   <code>feature_masks=[\"road\", \"water\"]</code> becomes   <code>modifications=[ff.mask(feature, band, value)]</code>, which overwrites the   cells a feature covers.</li> <li>Universal transforms are methods; type-specific ones are functions.   Transforms that apply to any grid you hold are methods \u2014   <code>grid.resample(...)</code>, <code>grid.export(...)</code>. Deriving a fuel-parameter grid   from FBFM40 codes only applies to FBFM40 grids, so it is a function:   <code>ff.grids.create_fuel_grid_from_fbfm40_lookup(fbfm_grid, ...)</code>.</li> </ul>"},{"location":"guides/migration/#before-and-after_1","title":"Before and after","text":"v1v2 <pre><code>from fastfuels_sdk import Grids\n\ngrids = Grids.from_domain_id(domain.id)\ntopo = grids.create_topography_grid(\n    attributes=[\"elevation\", \"slope\", \"aspect\"]\n)\ntopo.wait_until_completed()\n</code></pre> <pre><code>import fastfuels_sdk.v2 as ff\n\ntopo = ff.grids.create_topography_grid_from_3dep(\n    domain, output_resolution_m=10, bands=[\"elevation\", \"slope\", \"aspect\"]\n)\ntopo.wait()\n</code></pre>"},{"location":"guides/migration/#features","title":"Features","text":"<p>Available now \u2014 see the Features guide and the Reference.</p>"},{"location":"guides/migration/#what-changed-from-v1_2","title":"What changed from v1","text":"<ul> <li>Road and water unify into one resource. v1's <code>Features</code> container   with <code>.road</code>/<code>.water</code> sub-resources becomes a single job-based   <code>Feature</code> distinguished by its type (\"road\", \"water\", or \"layerset\").   The <code>Features.from_domain_id(...)</code> container is replaced by the   module-level <code>list_features(domain_id)</code>.</li> <li>Creation is a function per source. The v1   <code>features.create_road_feature_from_osm()</code> becomes the module-level   <code>ff.features.create_road_feature_from_osm(domain)</code>, and likewise   <code>create_water_feature_from_osm</code> for water. The new <code>extent_buffer_m</code>   argument buffers the OSM query extent by up to 100 meters.</li> <li>User-supplied geometry becomes layersets. v1's road-from-GeoJSON   path is gone. Instead, v2 accepts custom layersets \u2014 fuelbed   polygons carrying rasterizer properties \u2014 via   <code>ff.features.create_layerset_feature_from_geojson</code> and   <code>create_layerset_feature_from_geodataframe</code>. Layersets require a   projected CRS and upload synchronously, and a completed layerset can be   burned onto a grid with <code>feature.rasterize(...)</code>.</li> <li>Data access is partitioned. v1's <code>get_data(page, size)</code> /   <code>get_all_data()</code> become <code>get_data_metadata()</code> (partition layout),   <code>get_data_partition(index)</code> (one partition), and <code>get_data()</code> (all   partitions assembled) \u2014 plus <code>to_geodataframe()</code> for the common case   of loading everything into GeoPandas.</li> <li>Cross-domain listing. <code>list_features()</code> without a domain ID lists   features across all your domains, with <code>feature_type</code>, <code>product</code>, and   <code>tag</code> filters.</li> <li>Typed exceptions. Errors raise <code>fastfuels_sdk.v2.exceptions</code>   classes, the same as domains.</li> </ul>"},{"location":"guides/migration/#before-and-after_2","title":"Before and after","text":"v1v2 <pre><code>from fastfuels_sdk import Features\n\nfeatures = Features.from_domain_id(domain.id)\nroad = features.create_road_feature_from_osm()\nroad.wait_until_completed(verbose=True)\n\ndata = road.get_all_data()\n</code></pre> <pre><code>import fastfuels_sdk.v2 as ff\n\nroad = ff.features.create_road_feature_from_osm(domain)\nroad.wait(verbose=True)\n\nroads = road.to_geodataframe()\n</code></pre>"},{"location":"guides/migration/#inventories","title":"Inventories","text":"<p>Available now \u2014 see the Inventories guide and the Reference.</p>"},{"location":"guides/migration/#what-changed-from-v1_3","title":"What changed from v1","text":"<ul> <li>TreeMap generation is a two-resource workflow. v1's   <code>create_tree_inventory_from_treemap()</code> did the plot matching and the   tree expansion in one call. v2 splits them: create a PIM grid   (<code>ff.grids.create_pim_grid_from_treemap</code>), wait for it, then expand it   with <code>ff.inventories.create_tree_inventory_from_pim_grid(domain, pim)</code>.   The intermediate PIM grid is reusable \u2014 expand it several times with   different seeds or modifications without re-matching plots.</li> <li>Creation is a function per source. The <code>Inventories.from_domain_id</code>   container is gone; call <code>create_tree_inventory_from_pim_grid</code>,   <code>create_tree_inventory_from_chm_grid</code> (new \u2014 stem isolation on a canopy   height model), or <code>create_tree_inventory_from_file</code> on the domain.</li> <li>Upload columns changed. v1 uploads required <code>TREE_ID</code>, <code>SPCD</code>,   <code>STATUSCD</code>, <code>DIA</code>, <code>HT</code> columns. v2 uses the roles <code>x</code>, <code>y</code>, <code>height</code>,   <code>dbh</code>, <code>crown_ratio</code>, <code>fia_species_code</code>, <code>fia_status_code</code>, and a   <code>columns={role: your_name}</code> mapping replaces renaming your file.</li> <li>Modifications and treatments are typed models. v1's dict syntax   (<code>{\"attribute\": \"HT\", ...}</code>) becomes the generated   <code>InventoryModification</code> / treatment models, passed to <code>modifications=</code> /   <code>treatments=</code> at creation or to <code>inventory.apply_modifications(...)</code>   in place. v1's <code>feature_masks=[\"road\", \"water\"]</code> becomes a modification   whose condition is a feature spatial condition   (<code>InventoryFeatureSpatialCondition</code>) and whose action is <code>RemoveAction</code>.</li> <li>Data access is direct. v1 exposed tree records only through file   exports (<code>create_export</code> \u2192 <code>to_file</code>). v2 streams them from the API:   <code>get_data_metadata()</code> / <code>get_data_partition(index)</code> \u2014 plus   <code>to_dataframe()</code> for the common case of loading every tree into pandas.   File exports remain available via <code>inventory.export(...)</code>.</li> <li>Scenario branching is first-class. <code>inventory.duplicate()</code> clones a   completed inventory, and <code>checksum</code> marks the data version, so derived   resources can detect a stale source.</li> <li>Voxelization is a method. The v1 tree grid built from an inventory   becomes <code>inventory.voxelize(horizontal_resolution_m=...,   vertical_resolution_m=...)</code>, returning a 3D <code>Grid</code>.</li> </ul>"},{"location":"guides/migration/#exports","title":"Exports","text":"<p>Available now \u2014 see the Exports guide and the Reference.</p>"},{"location":"guides/migration/#what-changed-from-v1_4","title":"What changed from v1","text":"<ul> <li>Exports hang off grids and inventories. v1's <code>domain.export()</code> and   per-resource <code>create_export</code>/<code>get_export</code> pairs become   <code>grid.export(format=...)</code> and <code>inventory.export(format=...)</code>, each   returning a job-based <code>Export</code>; chain <code>export.wait().to_file(path)</code> to   download. The lifecycle renames match the other resources   (<code>wait_until_completed</code> \u2192 <code>wait</code>).</li> <li><code>export_roi</code> becomes explicit creation + the QUIC-Fire bundle. The   v1 convenience built every resource from an ROI and exported in one   call. In v2 you create the domain, grids, and inventory explicitly   (see the other guides), then bundle them with   <code>ff.exports.create_quicfire_export(domain, ...)</code> \u2014 naming the exact   grid and band filling each QUIC-Fire role (<code>canopy_bulk_density</code>,   <code>surface_fuel_load</code>, ...). The API packages the <code>.dat</code> archive   server-side, replacing v1's client-side zarr-to-QUIC-Fire conversion.</li> <li>Exports are cross-domain resources. <code>ff.get_export(export_id)</code> and   <code>ff.list_exports(...)</code> address exports by ID alone, with domain,   source, and tag filters.</li> </ul>"},{"location":"guides/migration/#before-and-after_3","title":"Before and after","text":"v1v2 <pre><code>export = tree_inventory.create_export(\"csv\")\nexport = export.wait_until_completed()\nexport.to_file(\"trees.csv\")\n</code></pre> <pre><code>inventory.export(format=\"csv\").wait().to_file(\"trees.csv\")\n</code></pre>"},{"location":"guides/migration/#before-and-after_4","title":"Before and after","text":"v1v2 <pre><code>from fastfuels_sdk import Inventories\n\ninventories = Inventories.from_domain_id(domain.id)\ntrees = inventories.create_tree_inventory_from_treemap(seed=42)\ntrees.wait_until_completed()\n\nexport = trees.create_export(\"csv\")\nexport.wait_until_completed().to_file(\"trees.csv\")  # read the file back\n</code></pre> <pre><code>import fastfuels_sdk.v2 as ff\n\npim = ff.grids.create_pim_grid_from_treemap(domain, output_resolution_m=30)\npim.wait()\n\ntrees = ff.inventories.create_tree_inventory_from_pim_grid(domain, pim, seed=42)\ntrees.wait()\n\ndata = trees.to_dataframe()\n</code></pre>"},{"location":"guides/modify-treat-inventories/","title":"How to Modify and Treat Tree Inventories","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>Two tools reshape a tree inventory's stems:</p> <ul> <li>Modifications \u2014 general rules that filter trees by conditions and   apply actions (remove, multiply, replace, \u2026) to the matching rows.</li> <li>Treatments \u2014 silvicultural thinning prescriptions that remove stems   until the stand reaches a target basal area or diameter.</li> </ul> <p>Both can be applied two ways: at creation, via a creator's <code>modifications=</code> / <code>treatments=</code> argument, or to an inventory you already hold, via <code>apply_modifications</code> / <code>apply_treatments</code>. This guide covers both. For creating and reading inventories in the first place, see Inventories.</p>"},{"location":"guides/modify-treat-inventories/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>An inventory (held or being created)</li> </ul>"},{"location":"guides/modify-treat-inventories/#modifications","title":"Modifications","text":"<p>A modification is a rule with two parts: <code>conditions</code> (all ANDed \u2014 a tree must satisfy every one) and <code>actions</code> (applied to the matching trees). Build conditions with <code>ff.tree_attribute</code> (a per-tree attribute test) or <code>ff.tree_within</code> (trees inside a feature), then assemble the rule with <code>ff.remove_trees</code> (drop the matching trees) or <code>ff.modify_trees</code> (change an attribute on them):</p> <pre><code>import fastfuels_sdk.v2 as ff\n\n# Remove every tree under 10 cm DBH\nff.remove_trees(ff.tree_attribute(\"dbh\", \"&lt;\", 10))\n\n# Shrink crowns on the largest trees inside a stand boundary\nff.modify_trees(\n    \"crown_ratio\", \"multiply\", 0.8,\n    ff.tree_attribute(\"dbh\", \"&gt;\", 40),\n    ff.tree_within(stand),\n)\n</code></pre> <p><code>tree_attribute</code> takes an attribute (<code>\"dbh\"</code>, <code>\"height\"</code>, <code>\"crown_ratio\"</code>, <code>\"fia_species_code\"</code>) and a comparison (<code>\"&lt;\"</code>, <code>\"&lt;=\"</code>, <code>\"&gt;\"</code>, <code>\"&gt;=\"</code>, <code>\"==\"</code>, <code>\"!=\"</code>); pass several conditions to AND them. <code>modify_trees</code>'s modifier is <code>\"replace\"</code>, <code>\"add\"</code>, <code>\"subtract\"</code>, <code>\"multiply\"</code>, or <code>\"divide\"</code>.</p>"},{"location":"guides/modify-treat-inventories/#treatments","title":"Treatments","text":"<p>A treatment thins to a target. Build one with <code>ff.basal_area_treatment</code> (residual basal area) or <code>ff.diameter_treatment</code> (diameter limit) rather than hand-building the model:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\n# Thin from below to a residual basal area of 25 m**2/ha\ntreatment = ff.basal_area_treatment(\"from_below\", 25.0)\n\n# Or remove every stem under 10 cm dbh\ntreatment = ff.diameter_treatment(\"from_below\", 10.0)\n</code></pre> <p><code>method</code> is <code>\"from_below\"</code> (smallest first), <code>\"from_above\"</code> (largest first), or \u2014 for basal area only \u2014 <code>\"proportional\"</code> (across all size classes).</p>"},{"location":"guides/modify-treat-inventories/#apply-at-creation","title":"Apply at creation","text":"<p>Every <code>create_tree_inventory_from_*</code> function accepts <code>modifications=</code> and <code>treatments=</code>; modifications run first, then treatments, while the inventory is derived:</p> <pre><code>thinned = ff.inventories.create_tree_inventory_from_pim_grid(\n    domain,\n    pim,\n    seed=42,\n    modifications=[ff.remove_trees(ff.tree_attribute(\"dbh\", \"&lt;\", 10))],\n    treatments=[ff.basal_area_treatment(\"from_below\", 25.0)],\n    name=\"Thinned\",\n)\nthinned.wait()\n</code></pre>"},{"location":"guides/modify-treat-inventories/#apply-to-an-inventory-you-hold","title":"Apply to an inventory you hold","text":"<p><code>apply_modifications</code> and <code>apply_treatments</code> reshape an inventory in place: its ID is kept and the data is re-derived as a background job. The submitted rules are queued while the inventory is <code>\"pending\"</code>, then appended to the cumulative <code>modifications</code> / <code>treatments</code> list when processing completes. Call <code>wait()</code> before using the inventory again or reading those ledgers.</p> <pre><code>inventory.apply_treatments([ff.basal_area_treatment(\"from_below\", 25.0)])\ninventory.wait()\n\ninventory.apply_modifications([ff.remove_trees(ff.tree_attribute(\"dbh\", \"&lt;\", 10))])\ninventory.wait()\n</code></pre> <p>Both must be called on a <code>completed</code> inventory. Re-deriving overwrites the inventory's data, so to keep the original, duplicate it first and reshape the copy.</p>"},{"location":"guides/modify-treat-inventories/#branch-a-scenario","title":"Branch a scenario","text":"<p>To compare scenarios \u2014 a thinned stand against the original \u2014 keep the original untouched and work on copies. <code>duplicate</code> makes an independent copy under a new ID, byte-copying the finished data rather than re-deriving it, so the copy starts identical (same <code>checksum</code>):</p> <pre><code>copy = inventory.duplicate(name=\"Thinning scenario\")\ncopy.wait()\ncopy.apply_treatments([ff.basal_area_treatment(\"from_below\", 25.0)])\ncopy.wait()\n</code></pre> <p>The <code>checksum</code> is a version marker for an inventory's content: it changes each time the data is rebuilt and is unaffected by metadata-only edits, so an identical checksum means identical trees.</p>"},{"location":"guides/modify-treat-inventories/#next-steps","title":"Next steps","text":"<ul> <li>Create and read inventories \u2014 see Inventories</li> <li>Voxelize a reshaped inventory into a 3D fuel grid \u2014 see   Inventories</li> <li>Full signatures \u2014 see the Reference</li> </ul>"},{"location":"guides/point-clouds/","title":"How to Work with Point Clouds in FastFuels SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>A point cloud is a 3D LiDAR dataset within a domain, fetched from USGS 3DEP or uploaded from your own airborne (ALS) or terrestrial (TLS) scan. For what point clouds are and how they fit the platform, see the FastFuels documentation; this guide covers creating and managing them from Python.</p> <p>The v2 surface is functional: you create a point cloud from a domain and data source, and everything you do with one you already hold is a method on it.</p> <pre><code>import fastfuels_sdk.v2 as ff\n\npc = ff.point_clouds.create_point_cloud_from_file(\n    domain, \"scan.laz\", point_cloud_type=\"als\"\n)\npc.wait()\n</code></pre>"},{"location":"guides/point-clouds/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>An existing domain \u2014 the first argument is a <code>Domain</code> (or a   bare domain id string)</li> <li>A local LiDAR file (<code>.las</code>/<code>.laz</code>) when uploading your own scan</li> </ul>"},{"location":"guides/point-clouds/#create-a-point-cloud-from-usgs-3dep","title":"Create a point cloud from USGS 3DEP","text":"<p>Check coverage and the per-fetch point budget before starting the background job:</p> <pre><code>coverage = ff.point_clouds.check_3dep_coverage(domain)\n\nif not coverage.available:\n    raise RuntimeError(\"No 3DEP LiDAR covers this domain\")\nif coverage.exceeds_point_budget:\n    raise RuntimeError(\"Shrink the domain before fetching 3DEP LiDAR\")\n</code></pre> <p>Create the point cloud with automatic acquisition selection:</p> <pre><code>pc = ff.point_clouds.create_point_cloud_from_3dep(\n    domain,\n    name=\"USGS 3DEP\",\n)\npc.wait()\n</code></pre> <p>To pin specific acquisitions, pass names returned by the coverage check in priority order:</p> <pre><code>pc = ff.point_clouds.create_point_cloud_from_3dep(\n    domain,\n    datasets=[coverage.datasets[0].name],\n)\n</code></pre> <p>The returned point cloud is always airborne (<code>type_ == \"als\"</code>).</p>"},{"location":"guides/point-clouds/#upload-a-point-cloud","title":"Upload a point cloud","text":"<p><code>create_point_cloud_from_file</code> creates the resource, uploads the file to a signed URL, and returns the (pending) point cloud. Set <code>point_cloud_type</code> to <code>\"als\"</code> for an airborne scan or <code>\"tls\"</code> for a terrestrial one:</p> <pre><code>pc = ff.point_clouds.create_point_cloud_from_file(\n    domain,\n    \"scan.laz\",\n    point_cloud_type=\"als\",\n    name=\"North stand ALS\",\n)\n</code></pre> <p>Processing runs as a background job \u2014 the returned point cloud starts in <code>\"pending\"</code> status and <code>wait()</code> blocks until it is ready:</p> <pre><code>pc.wait(verbose=True)\n</code></pre> <p>Once completed, the point cloud's <code>georeference</code> (CRS and bounds) and <code>summary</code> (point statistics) are populated.</p>"},{"location":"guides/point-clouds/#work-with-a-point-cloud-you-hold","title":"Work with a point cloud you hold","text":"<p>A point cloud is a job resource like any other: <code>wait(timeout=, verbose=)</code> polls it to a terminal status (raising <code>JobFailedError</code> on failure), <code>refresh()</code> reloads it in place, <code>update(name=, description=, tags=)</code> edits its metadata, and <code>delete()</code> removes it along with its data.</p> <pre><code>pc.update(name=\"North stand (2024)\", tags=[\"als\", \"2024\"])\npc.delete()\n</code></pre> <p>Point clouds are addressed by their domain and id:</p> <pre><code>pc = ff.get_point_cloud(domain, \"4a56bae0cd5e481aa1617cb894a9a7f3\")\n</code></pre>"},{"location":"guides/point-clouds/#read-point-cloud-data","title":"Read point cloud data","text":"<p>To read a completed point cloud's points into memory, call <code>to_numpy()</code> or <code>to_dataframe()</code>; both page over every occupied tile and stack them into one array or frame. <code>to_numpy()</code> returns an <code>(N, k)</code> <code>float64</code> array of the requested <code>columns</code>, defaulting to the <code>X</code>/<code>Y</code>/<code>Z</code> coordinates:</p> <pre><code>pc = ff.get_point_cloud(domain, \"4a56bae0cd5e481aa1617cb894a9a7f3\").wait()\n\npoints = pc.to_numpy()\n</code></pre> <pre><code>&gt;&gt;&gt; points.shape\n(48213, 3)\n</code></pre> <p><code>to_dataframe()</code> returns one row per point and one column per stored attribute (<code>X</code>, <code>Y</code>, <code>Z</code>, <code>classification</code>, and any source columns such as <code>intensity</code>):</p> <pre><code>df = pc.to_dataframe()\n</code></pre> <pre><code>&gt;&gt;&gt; df.columns.tolist()\n['X', 'Y', 'Z', 'classification']\n</code></pre> <p>To read fewer points, narrow the output with these arguments (shared by both methods):</p> <ul> <li><code>lod</code> \u2014 an inclusive level-of-detail ceiling. <code>0</code> is the coarsest sample   and each higher value adds finer points; omit it to read every point. Valid   values are <code>0</code> through <code>lod_levels - 1</code> (see <code>metadata()</code> below).</li> <li><code>classes</code> \u2014 ASPRS classification codes to keep, e.g. <code>[2, 5]</code> for ground   and high vegetation.</li> <li><code>columns</code> \u2014 the stored columns to read, in the returned order.</li> <li><code>decode_coordinates</code> \u2014 <code>True</code> (default) decodes <code>X</code>/<code>Y</code>/<code>Z</code> to CRS   coordinates; <code>False</code> keeps them as the stored scaled integers.</li> </ul> <pre><code># Ground returns only, as CRS coordinates.\nground = pc.to_dataframe(classes=[2], columns=[\"X\", \"Y\", \"Z\", \"classification\"])\n</code></pre> <p>To inspect the tile index \u2014 occupied tiles, stored columns and dtypes, the coordinate encoding, and the point count at each level of detail \u2014 without downloading any points, call <code>metadata()</code>:</p> <pre><code>meta = pc.metadata()\n</code></pre> <pre><code>&gt;&gt;&gt; meta.lod_levels\n5\n&gt;&gt;&gt; len(meta.tiles)\n4\n</code></pre> <p>Wait before reading</p> <p><code>metadata()</code>, <code>to_numpy()</code>, and <code>to_dataframe()</code> require a completed point cloud; call <code>wait()</code> first or they raise <code>ValueError</code>.</p>"},{"location":"guides/point-clouds/#list-point-clouds","title":"List point clouds","text":"<p>To list your point clouds, optionally narrowed to a domain, a scan type, a source, or a tag:</p> <pre><code>clouds = ff.list_point_clouds(domain)\nals_only = ff.list_point_clouds(domain, point_cloud_type=\"als\")\ntagged = ff.list_point_clouds(tag=\"2024\")\n</code></pre> <p>Omit <code>domain</code> to list point clouds across all of your domains.</p>"},{"location":"guides/point-clouds/#error-handling","title":"Error handling","text":"<p>Wrapper functions and methods raise typed exceptions from <code>fastfuels_sdk.v2.exceptions</code>:</p> <pre><code>import fastfuels_sdk.v2 as ff\nfrom fastfuels_sdk.v2.exceptions import NotFoundException\n\ntry:\n    pc = ff.get_point_cloud(domain, \"does-not-exist\")\nexcept NotFoundException:\n    print(\"No such point cloud (or you don't have access to it)\")\n</code></pre>"},{"location":"guides/point-clouds/#next-steps","title":"Next steps","text":"<ul> <li>Turn a completed airborne point cloud into a canopy-height grid with   <code>create_canopy_height_grid_from_point_cloud</code></li> <li>Full signatures \u2014 see the Reference</li> </ul>"},{"location":"guides/quotas/","title":"How to Check Usage and Handle Quota Rejections","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p>"},{"location":"guides/quotas/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> </ul> <p>Use this guide to inspect the current owner's limits and usage and to handle quota rejections from SDK resource creators.</p>"},{"location":"guides/quotas/#check-your-limits","title":"Check your limits","text":"<p>Call <code>get_quotas</code> to retrieve the limits resolved for the owner authenticated by the current API key:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\nquotas = ff.get_quotas()\n\nprint(quotas.max_active_grids)\nprint(quotas.max_weekly_grid_dispatches)\nprint(quotas.max_grid_storage_bytes)\n</code></pre>"},{"location":"guides/quotas/#check-your-current-usage","title":"Check your current usage","text":"<p>Call <code>get_usage</code> to compare current usage with the corresponding limits:</p> <pre><code>usage = ff.get_usage()\n\nprint(usage.grids.active.usage, usage.grids.active.limit)\nprint(usage.grids.total.usage, usage.grids.total.limit)\nprint(usage.grids.storage.usage_bytes, usage.grids.storage.limit_bytes)\n</code></pre> <p>Count-only resources are available through <code>usage.domains</code>, <code>usage.applications</code>, and <code>usage.api_keys</code>. The active, total, and storage fields are also available for exports, inventories, features, and point clouds.</p> <p>Inspect <code>usage.lifecycle</code> for the resource-retention policy currently applied to the owner:</p> <pre><code>print(usage.lifecycle.resource_ttl_days)\nprint(usage.lifecycle.failed_resource_ttl_days)\nprint(usage.lifecycle.next_expiry_on)\n</code></pre>"},{"location":"guides/quotas/#handle-a-quota-rejection","title":"Handle a quota rejection","text":"<p>Catch <code>QuotaExceededException</code> around any operation that creates or re-derives a resource:</p> <pre><code>import fastfuels_sdk.v2 as ff\nfrom fastfuels_sdk.v2.exceptions import QuotaExceededException\n\ntry:\n    grid = ff.grids.create_topography_grid_from_3dep(\n        domain,\n        output_resolution_m=30,\n    )\nexcept QuotaExceededException as exc:\n    print(exc.quota, exc.current, exc.limit)\n    print(exc.message)\n    if exc.retry_after is not None:\n        print(f\"Retry in {exc.retry_after} seconds\")\n</code></pre> <p>For an active-job limit, <code>retry_after</code> contains the number of seconds the API recommends waiting before another attempt.</p> <p>For a weekly dispatch limit, <code>window_reset_on</code> contains the reset time. Count and storage limits provide neither retry field; delete unneeded resources or request a higher limit before retrying.</p>"},{"location":"guides/working-with-grids/","title":"How to Work with Grids in FastFuels SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>This guide covers what you do with a grid you already hold: wait on its job, inspect its bands and georeference, resample it, export it, and list or delete grids. To create grids in the first place, see Creating grids. For what grids are, see the FastFuels documentation.</p> <p>You get a grid handle either from a creator (see Creating grids) or by fetching one by id:</p> <pre><code>import fastfuels_sdk.v2 as ff\n\ngrid = ff.get_grid(domain, \"0eeed67e33df450f943a528fb1447dab\")\n</code></pre> <p>Operations on a grid you hold are methods on it (<code>grid.wait()</code>, <code>grid.resample(...)</code>, <code>grid.delete()</code>); listing and fetching are top-level functions (<code>ff.list_grids</code>, <code>ff.get_grid</code>).</p>"},{"location":"guides/working-with-grids/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>A grid \u2014 created per Creating grids, or fetched by id</li> </ul>"},{"location":"guides/working-with-grids/#wait-for-a-grid-to-finish","title":"Wait for a grid to finish","text":"<p>Because creators return a pending grid, block on the job before using its data:</p> <pre><code>grid.wait()\n</code></pre> <pre><code>&gt;&gt;&gt; grid.status\n&lt;JobStatus.COMPLETED: 'completed'&gt;\n</code></pre> <p><code>wait</code> polls until the job reaches a terminal status and returns the grid, so calls chain. It waits indefinitely by default; pass <code>timeout</code> (seconds) to bound it, which raises <code>TimeoutError</code>. A failed job raises <code>JobFailedError</code>, carrying the API's <code>code</code>, <code>message</code>, and <code>suggestion</code>.</p> <p>Jobs run server-side in parallel, so create everything first and join with <code>ff.wait_all</code>. With <code>verbose=True</code> it prints a line per poll for each grid still running when it is reached:</p> <pre><code>ff.wait_all([topography, surface_fuel, canopy_fuel], verbose=True)\n</code></pre> <pre><code>Grid 0eeed67e33df450f943a528fb1447dab: running (5s)\nGrid 0eeed67e33df450f943a528fb1447dab: completed (10s)\n</code></pre> <p>(Small domains finish fast \u2014 grids already complete when <code>wait_all</code> reaches them print nothing.)</p>"},{"location":"guides/working-with-grids/#inspect-a-grids-bands-and-georeference","title":"Inspect a grid's bands and georeference","text":"<p>A grid reports its bands as soon as it is created \u2014 their keys, names, and units \u2014 so you know what data it holds:</p> <pre><code>&gt;&gt;&gt; [b.key for b in topography.bands]\n['elevation', 'slope', 'aspect']\n&gt;&gt;&gt; topography.bands[1].name, topography.bands[1].unit\n('Slope', 'deg')\n</code></pre> <p>Its georeference is populated once the job completes (it is <code>None</code> while pending), reporting the CRS, affine transform, and pixel shape of the output:</p> <pre><code>&gt;&gt;&gt; topography.georeference.crs\n'EPSG:32611'\n&gt;&gt;&gt; topography.georeference.transform   # affine [a, b, c, d, e, f]\n[30.0, 0.0, 720192.0, 0.0, -30.0, 5190856.0]\n&gt;&gt;&gt; topography.georeference.shape       # [rows, cols]\n[47, 58]\n</code></pre> <p>Two grids built on the same domain at the same resolution share this georeference, which is what lets them overlay cell-for-cell.</p>"},{"location":"guides/working-with-grids/#summarize-a-band-without-downloading-it","title":"Summarize a band without downloading it","text":"<p>Once a grid completes, the server attaches summary statistics to each band, so you can get a quick overview without fetching the cells (which is what <code>to_numpy</code> does). Pass a band key to <code>band_summary</code>:</p> <pre><code>summary = topography.band_summary(\"elevation\")\n</code></pre> <p>A continuous band reports <code>count</code>, <code>nodata_count</code>, <code>min_</code>, <code>max_</code>, <code>mean</code>, and <code>std</code>; a categorical band reports <code>count</code>, <code>nodata_count</code>, and <code>unique_count</code>. The <code>type_</code> field (<code>\"continuous\"</code> or <code>\"categorical\"</code>) tells you which. <code>band_summary</code> returns <code>None</code> until the grid completes \u2014 call <code>wait</code> first.</p>"},{"location":"guides/working-with-grids/#retrieve-a-grid-by-id","title":"Retrieve a grid by id","text":"<p>To fetch an existing grid using its domain and grid IDs:</p> <pre><code>grid = ff.get_grid(domain, \"0eeed67e33df450f943a528fb1447dab\")\n</code></pre>"},{"location":"guides/working-with-grids/#refresh-and-update","title":"Refresh and update","text":"<p>To reload a grid's latest state in place (for example, to check job progress yourself):</p> <pre><code>grid.refresh()\n</code></pre> <p>To change a grid's name, description, or tags:</p> <pre><code>&gt;&gt;&gt; grid.update(name=\"Topography (30 m)\", tags=[\"terrain\"])\n&gt;&gt;&gt; grid.name, grid.tags\n('Topography (30 m)', ['terrain'])\n</code></pre> <p><code>refresh</code> and <code>update</code> both change the grid in place and return it. <code>update</code> sends only the fields you pass; passing none makes no API call.</p>"},{"location":"guides/working-with-grids/#resample-a-grid","title":"Resample a grid","text":"<p>To resample a completed grid onto a new lattice, producing a new grid:</p> <pre><code>coarser = grid.resample(output_resolution_m=90, resampling=\"average\")\n</code></pre> <p><code>resample</code> takes the same alignment arguments as the creators (see Align grids to each other). The source grid must be <code>completed</code>.</p>"},{"location":"guides/working-with-grids/#duplicate-a-grid","title":"Duplicate a grid","text":"<p>To branch from a completed grid, <code>duplicate</code> makes an independent copy under a new ID. The finished data is byte-copied rather than re-derived, so the copy carries the source's <code>checksum</code> verbatim \u2014 only its <code>id</code> and timestamps differ:</p> <pre><code>copy = grid.duplicate(name=\"Scenario A\")\ncopy.wait()\n</code></pre>"},{"location":"guides/working-with-grids/#apply-modifications-to-a-grid","title":"Apply modifications to a grid","text":"<p>To modify a grid you already hold \u2014 rather than at creation \u2014 call <code>apply_modifications</code> with the same <code>ff.mask(...)</code> rules a creator's <code>modifications=</code> argument accepts. The rules are appended to the grid's cumulative modifications and the data is re-derived in place: the grid keeps its ID and returns to <code>\"pending\"</code> while it rebuilds.</p> <pre><code>grid.apply_modifications([ff.mask(roads, \"fbfm\", 91, buffer_m=5)])\ngrid.wait()\n</code></pre> <p>The grid must be <code>completed</code> first. Re-deriving overwrites the grid's data, so to keep the original, <code>duplicate</code> it first and modify the copy. See Mask out features for the masking model.</p>"},{"location":"guides/working-with-grids/#read-grid-data-into-python","title":"Read grid data into Python","text":"<p>To pull a single band's values into a NumPy array, pass the band key to <code>to_numpy</code>:</p> <pre><code>elevation = topography.to_numpy(\"elevation\")\n</code></pre> <pre><code>&gt;&gt;&gt; elevation.shape       # matches grid.georeference.shape\n(47, 58)\n&gt;&gt;&gt; elevation.dtype\ndtype('float32')\n</code></pre> <p>The array is shaped like the grid \u2014 <code>(rows, cols)</code> for a 2D raster, or <code>(z, rows, cols)</code> for a 3D voxel grid. Cells with no data hold the band's <code>nodata</code> value, or <code>NaN</code> when the band defines none.</p> <p>To read every band at once into an <code>xarray.Dataset</code> \u2014 one variable per band, with <code>x</code>/<code>y</code> (and <code>z</code>) coordinates derived from the grid's affine transform and the CRS on <code>.attrs</code> \u2014 call <code>to_xarray</code>:</p> <pre><code>&gt;&gt;&gt; topography.to_xarray()\n&lt;xarray.Dataset&gt; Size: 34kB\nDimensions:    (y: 47, x: 58)\nCoordinates:\n  * y          (y) float64 376B 5.191e+06 5.191e+06 ... 5.189e+06 5.189e+06\n  * x          (x) float64 464B 7.202e+05 7.202e+05 ... 7.219e+05 7.219e+05\nData variables:\n    elevation  (y, x) float32 11kB 1.015e+03 1.013e+03 ... 1.02e+03 1.019e+03\n    slope      (y, x) float32 11kB 7.587 8.345 8.092 7.41 ... 3.42 3.008 3.094\n    aspect     (y, x) float32 11kB 25.29 28.79 40.35 51.47 ... 126.0 135.2 138.1\nAttributes:\n    crs:      EPSG:32611\n</code></pre> <p>Both methods download the grid's data and require a <code>completed</code> grid. To write the data to a file on disk instead of loading it into memory, see Export a grid.</p>"},{"location":"guides/working-with-grids/#export-a-grid","title":"Export a grid","text":"<p>To export a completed grid to a downloadable file and save it, chain the export job's <code>wait</code> into <code>to_file</code>:</p> <pre><code>export = grid.export(format=\"geotiff\")  # or \"netcdf\", \"zarr\"\nexport.wait().to_file(\"elevation.tif\")\n</code></pre> <p>The export runs as its own background job; the signed download URL fills in once it completes and stays valid for seven days. To load a grid's values into memory instead of a file, see Read grid data into Python. For exporting band subsets, the multi-grid QUIC-Fire bundle, and managing exports, see the Exports guide.</p>"},{"location":"guides/working-with-grids/#list-grids","title":"List grids","text":"<p>To list grids in a domain, or across all your domains by omitting it:</p> <pre><code>grids = ff.list_grids(domain)\nall_grids = ff.list_grids()\n</code></pre> <p>Narrow the results by source, source product (requires <code>source</code>), or tag \u2014 the source and product names are the ones a grid reports in <code>grid.source</code>:</p> <pre><code>topo_grids = ff.list_grids(domain, source=\"3dep\")\nfbfm_grids = ff.list_grids(domain, source=\"landfire\", product=\"fbfm40\")\ntagged = ff.list_grids(domain, tag=\"terrain\")\n</code></pre>"},{"location":"guides/working-with-grids/#delete-a-grid","title":"Delete a grid","text":"<p>To permanently delete a grid and its generated data:</p> <pre><code>grid.delete()\n</code></pre> <p>Deleting a domain also deletes all of its grids.</p>"},{"location":"guides/working-with-grids/#error-handling","title":"Error handling","text":"<p>Functions and methods raise typed exceptions from <code>fastfuels_sdk.v2.exceptions</code>:</p> <pre><code>import fastfuels_sdk.v2 as ff\nfrom fastfuels_sdk.v2.exceptions import (\n    NotFoundException,\n    UnprocessableEntityException,\n)\n\ntry:\n    grid = ff.get_grid(domain, \"does-not-exist\")\nexcept NotFoundException:\n    print(\"No such grid (or you don't have access to it)\")\n\ntry:\n    grid.resample(output_resolution_m=30)  # source not completed yet\nexcept UnprocessableEntityException as exc:\n    print(exc.detail)\n</code></pre>"},{"location":"tutorials/export_to_quicfire/","title":"Tutorial: Export QUIC-Fire Inputs with the v2 SDK","text":"<p>Beta</p> <p>The v2 SDK targets the FastFuels v2 API and is under active development. The v1 SDK remains the default \u2014 import v2 explicitly from <code>fastfuels_sdk.v2</code>.</p> <p>In this tutorial we'll build a complete set of QUIC-Fire fuel inputs for a small region in the Blue Mountain Recreation Area and package them into a QUIC-Fire-loadable archive. Along the way we'll touch every kind of v2 resource \u2014 a domain, OpenStreetMap features, raster grids, a tree inventory, and a 3D voxel grid \u2014 and assemble them with a single QUIC-Fire export.</p> <p>We'll follow the v2 SDK's functional style throughout: we create a resource by calling a <code>create_\u2026</code> function on a domain, and everything we do with a resource we already hold (waiting, looking up values, voxelizing, exporting) is a method on it. Because every creation runs as a background job, we'll create work in batches and join each batch with <code>ff.wait_all</code>, letting the server do the jobs in parallel.</p>"},{"location":"tutorials/export_to_quicfire/#what-well-build","title":"What we'll build","text":"<p>A QUIC-Fire export reads five required fuel fields \u2014 plus optional topography, which we'll include. We'll build each from a real data source:</p> Field Source we'll use v2 resource Canopy bulk density TreeMap \u2192 tree inventory \u2192 voxels 3D voxel grid Canopy fuel moisture voxelization moisture model 3D voxel grid Surface fuel load LANDFIRE FBFM40 lookup 2D grid Surface fuel depth LANDFIRE FBFM40 lookup 2D grid Surface fuel moisture a uniform value 2D grid Topography (elevation) USGS 3DEP 2D grid <p>The exporter slices these into the QUIC-Fire <code>.dat</code> arrays we'll inspect in Step 9.</p>"},{"location":"tutorials/export_to_quicfire/#prerequisites","title":"Prerequisites","text":"<ul> <li>The FastFuels SDK installed: <code>pip install fastfuels-sdk</code></li> <li>A FastFuels API key in the <code>FASTFUELS_API_KEY</code> environment variable</li> <li>Basic familiarity with Python and GeoPandas</li> </ul> <p>For background on what each of these resources is, see the FastFuels documentation. Coming from the v1 SDK? The migration guide maps the v1 builder classes to the v2 functions used here.</p>"},{"location":"tutorials/export_to_quicfire/#step-1-authenticate","title":"Step 1: Authenticate","text":"<p>The v2 SDK reads your API key from the <code>FASTFUELS_API_KEY</code> environment variable:</p> <pre><code>export FASTFUELS_API_KEY=\"your-api-key\"\n</code></pre> <p>Then import the package under the conventional <code>ff</code> alias \u2014 everything in this tutorial hangs off it:</p> <pre><code>import fastfuels_sdk.v2 as ff\n</code></pre> <p>If you'd rather set the key in code, call <code>ff.set_api_key(\"your-api-key\")</code> before anything else.</p>"},{"location":"tutorials/export_to_quicfire/#step-2-define-a-region-of-interest","title":"Step 2: Define a region of interest","text":"<p>We'll describe our area as a GeoDataFrame holding a single polygon in WGS 84 (EPSG:4326):</p> <pre><code>import geopandas as gpd\nfrom shapely.geometry import Polygon\n\ncoordinates = [\n    [-114.09957018646286, 46.82933208815811],\n    [-114.10141707482919, 46.828370407248826],\n    [-114.10010954324228, 46.82690548814563],\n    [-114.09560673134018, 46.8271123684554],\n    [-114.09592544216444, 46.829058122675065],\n    [-114.09957018646286, 46.82933208815811],\n]\n\nroi = gpd.GeoDataFrame(geometry=[Polygon(coordinates)], crs=\"EPSG:4326\")\n</code></pre>"},{"location":"tutorials/export_to_quicfire/#step-3-create-a-domain","title":"Step 3: Create a domain","text":"<p>A domain is the spatial container every other resource lives in. We'll create one from the GeoDataFrame, padding its bounding box out to a whole number of 2 m cells so the grids we build later tile it exactly:</p> <pre><code>domain = ff.Domain.from_geodataframe(\n    geodataframe=roi,\n    name=\"Blue Mountain QUIC-Fire\",\n    description=\"Tutorial region in the Blue Mountain Recreation Area\",\n    pad_to_resolution=2.0,\n)\n</code></pre> <pre><code>&gt;&gt;&gt; domain.id\n'0d77a5b1525b497c829fceeadba5f958'\n</code></pre> <p>The domain reprojects our lat/lon polygon into a local metric CRS. We can see the 2 m pixel lattice every grid will share:</p> <pre><code>&gt;&gt;&gt; lattice = domain.get_lattice(resolution=2.0)\n&gt;&gt;&gt; lattice.crs\n'EPSG:32611'\n&gt;&gt;&gt; lattice.shape   # [rows, cols]\n[136, 225]\n</code></pre>"},{"location":"tutorials/export_to_quicfire/#step-4-add-roads-and-water-from-openstreetmap","title":"Step 4: Add roads and water from OpenStreetMap","text":"<p>Roads and open water aren't fuel \u2014 we'll pull them from OpenStreetMap now so we can carve them out of the fuels later. Both extractions are background jobs, so we create them together and join with <code>ff.wait_all</code>:</p> <pre><code>roads = ff.features.create_road_feature_from_osm(domain, name=\"Roads\")\nwater = ff.features.create_water_feature_from_osm(domain, name=\"Water\")\n\nff.wait_all([roads, water], verbose=True)\n</code></pre> <pre><code>Feature 07e6407aedd44d7e8a09fbc5e48c3f3e: pending (5s)\nFeature 07e6407aedd44d7e8a09fbc5e48c3f3e: pending (15s)\nFeature 07e6407aedd44d7e8a09fbc5e48c3f3e: completed (20s)\n</code></pre> <p>(Only one feature prints here \u2014 the other finished before <code>wait_all</code> reached it. Small regions extract fast.)</p> <p>No separate feature grid in v2</p> <p>In the v1 SDK you created a standalone feature grid to mask trees and fuels. In v2 there's no such resource: you mask a feature directly into each grid by passing <code>ff.mask(feature, \u2026)</code> in that creator's <code>modifications=</code> list, as we do for the surface fuels in Step 6. See Mask out features for the full masking model.</p>"},{"location":"tutorials/export_to_quicfire/#step-5-build-the-topography-grid","title":"Step 5: Build the topography grid","text":"<p>Elevation comes from the USGS 3D Elevation Program (3DEP). We resample its 10 m source onto our 2 m domain lattice:</p> <pre><code>topography = ff.grids.create_topography_grid_from_3dep(\n    domain,\n    source_resolution_m=10,\n    output_resolution_m=2,\n    bands=[\"elevation\"],\n)\n</code></pre> <p>We won't wait on it yet \u2014 it's a pending job we'll join with the other raster grids in the next step.</p>"},{"location":"tutorials/export_to_quicfire/#step-6-build-the-surface-fuel-grids","title":"Step 6: Build the surface fuel grids","text":"<p>QUIC-Fire's surface needs fuel load, fuel depth, and fuel moisture. Load and depth come from LANDFIRE's 40 Scott &amp; Burgan fire behavior fuel models (FBFM40). First we build the FBFM40 grid, masking the road network to a non-burnable code (FBFM 91) as we go \u2014 this is the v2 replacement for v1's <code>feature_masks</code>:</p> <pre><code>fbfm = ff.grids.create_fuel_model_grid_from_landfire_fbfm40(\n    domain,\n    output_resolution_m=2,\n    modifications=[ff.mask(roads, \"fbfm\", 91, buffer_m=5)],\n)\n</code></pre> <p>The FBFM40 grid holds categorical fuel-model codes. We also start the PIM grid we'll need for trees in Step 7, then join all three pending raster jobs:</p> <pre><code>pim = ff.grids.create_pim_grid_from_treemap(\n    domain, output_resolution_m=2, resampling=\"nearest\"\n)\n\nff.wait_all([topography, fbfm, pim], verbose=True)\n</code></pre> <p>With the FBFM40 codes in hand, we look up the actual fuel quantities QUIC-Fire consumes \u2014 1-hour fuel load and fuel-bed depth \u2014 which produces a new grid:</p> <pre><code>surface = ff.grids.create_fuel_grid_from_fbfm40_lookup(\n    fbfm, bands=[\"fuel_load.1hr\", \"fuel_depth\"]\n)\n</code></pre> <pre><code>&gt;&gt;&gt; [(b.key, b.unit) for b in surface.bands]\n[('fuel_load.1hr', 'kg/m**2'), ('fuel_depth', 'm')]\n</code></pre> <p>LANDFIRE doesn't carry fuel moisture (it's a weather-driven scenario input), so we set a uniform 15% surface moisture on a matching 2 m grid:</p> <pre><code>moisture = ff.grids.create_uniform_grid(\n    domain, resolution_m=2.0, bands={\"fuel_moisture.1hr\": 15.0}\n)\n</code></pre>"},{"location":"tutorials/export_to_quicfire/#step-7-build-the-canopy-fuel-grid","title":"Step 7: Build the canopy fuel grid","text":"<p>Canopy fuel takes three moves: match each cell to a TreeMap forest plot (the PIM grid we already started), expand those plots into individual trees, then voxelize the trees into a 3D bulk-density grid. We expand the trees from the completed PIM grid, using a fixed <code>seed</code> so the result is reproducible:</p> <pre><code>inventory = ff.inventories.create_tree_inventory_from_pim_grid(\n    domain, pim, seed=42\n)\n\nff.wait_all([surface, moisture, inventory], verbose=True)\n</code></pre> <pre><code>&gt;&gt;&gt; len(inventory.to_dataframe())\n2068\n</code></pre> <p>Now we voxelize the inventory onto the same 2 m horizontal lattice, with 1 m vertical layers, asking for both the live foliage bulk density and the live fuel moisture QUIC-Fire needs:</p> <pre><code>voxels = inventory.voxelize(\n    horizontal_resolution_m=2.0,\n    vertical_resolution_m=1.0,\n    bands=[\"bulk_density.foliage.live\", \"fuel_moisture.live\"],\n)\nvoxels.wait(verbose=True)\n</code></pre> <pre><code>&gt;&gt;&gt; [b.key for b in voxels.bands]\n['bulk_density.foliage.live', 'fuel_moisture.live']\n&gt;&gt;&gt; voxels.georeference.shape   # [layers, rows, cols]\n[27, 136, 225]\n</code></pre>"},{"location":"tutorials/export_to_quicfire/#step-8-export-to-quic-fire","title":"Step 8: Export to QUIC-Fire","text":"<p>Everything is now on the same 2 m lattice. <code>create_quicfire_export</code> bundles the fields into a QUIC-Fire archive \u2014 each role is a <code>(grid, band)</code> pair naming the grid and the band to read from it:</p> <pre><code>export = ff.exports.create_quicfire_export(\n    domain,\n    canopy_bulk_density=(voxels, \"bulk_density.foliage.live\"),\n    canopy_moisture=(voxels, \"fuel_moisture.live\"),\n    surface_fuel_load=(surface, \"fuel_load.1hr\"),\n    surface_fuel_depth=(surface, \"fuel_depth\"),\n    surface_moisture=(moisture, \"fuel_moisture.1hr\"),\n    topography=(topography, \"elevation\"),\n    name=\"Blue Mountain QUIC-Fire\",\n)\n\nexport.wait(verbose=True)\n</code></pre> <pre><code>Export 67af943ef13e49a1b71c97c90c3fa4d9: pending (5s)\nExport 67af943ef13e49a1b71c97c90c3fa4d9: running (20s)\nExport 67af943ef13e49a1b71c97c90c3fa4d9: completed (25s)\n</code></pre> <p>The export is its own background job; once it completes, a signed download URL is filled in. We stream the archive to a local directory:</p> <pre><code>path = export.to_file(\"quicfire_export/\")\n</code></pre> <pre><code>&gt;&gt;&gt; path\nPosixPath('quicfire_export/Blue_Mountain_QUIC-Fire.zip')\n</code></pre>"},{"location":"tutorials/export_to_quicfire/#step-9-inspect-the-export","title":"Step 9: Inspect the export","text":"<p>The archive is a zip that QUIC-Fire loads directly. Let's confirm the inputs are all there:</p> <pre><code>&gt;&gt;&gt; import zipfile\n&gt;&gt;&gt; sorted(zipfile.ZipFile(path).namelist())\n['domain.geojson', 'metadata.json', 'topo.dat', 'treesfueldepth.dat', 'treesmoist.dat', 'treesrhof.dat']\n</code></pre> <p>The <code>.dat</code> files are the QUIC-Fire fuel arrays:</p> <ul> <li><code>treesrhof.dat</code> \u2014 canopy bulk density</li> <li><code>treesmoist.dat</code> \u2014 canopy fuel moisture</li> <li><code>treesfueldepth.dat</code> \u2014 surface fuel-bed depth</li> <li><code>topo.dat</code> \u2014 elevation</li> </ul> <p>alongside <code>metadata.json</code> (the grid geometry and band provenance) and <code>domain.geojson</code> (the domain footprint).</p>"},{"location":"tutorials/export_to_quicfire/#recap","title":"Recap","text":"<p>In one pass we created a domain, pulled road and water features, built topography from 3DEP and surface fuels from LANDFIRE FBFM40, expanded a TreeMap tree inventory into a 3D canopy fuel grid, and bundled it all into a QUIC-Fire archive \u2014 masking roads out of the fuels along the way.</p>"},{"location":"tutorials/export_to_quicfire/#next-steps","title":"Next steps","text":"<ul> <li>Swap in your own region by changing the polygon in   Step 2.</li> <li>Mask water (and other features) into the fuels the same way we masked   roads \u2014 see Mask out features.</li> <li>Tune the tree inventory before voxelizing \u2014 thinning, treatments, and   modifications \u2014 see   Modify and treat tree inventories.</li> <li>Define the fire grid from an existing grid's lattice, or change its   resolution, with the export's alignment options \u2014 see   Bundle grids for QUIC-Fire.</li> <li>Full signatures for every function used here are in the   Reference. ```</li> </ul>"}]}